

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v8.21.0 -->
<!-- Updated: 2026-07-27T00:13:52.944Z -->
<!-- sigmap: version=8.21.0 -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures
## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| To rank files by topic | `sigmap --query "<topic>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

Always run `sigmap ask` (or `sigmap --query`) before searching for files relevant to a task.

## deps
```
.ida_scratch\probes\attlib_chain_probe.py ← runpy
.ida_scratch\probes\attlib_dump_probe.py ← importlib
.ida_scratch\probes\ida_q14.py ← ida_mcp_client
```

## todos
```
src\data_interface\db_model.rs:93  # TODO: ，需要配置各个db对应的映射, 不同区域对应不同的db
src\data_interface\db_model.rs:113  # TODO: 如果没有需要新加数据
src\data_interface\increment_manager.rs:1383  # TODO: 暂时通过查询surreal来获取最终得值
src\data_interface\interface.rs:74  # TODO: 后面要去掉，不需要使用这个，直接用上memcache + arangodb
src\data_interface\tidb_manager.rs:103  # TODO: 修改为图数据库，尽可能避免使用TIDB
src\data_interface\tidb_manager.rs:264  # TODO: 这里还需要将project的信息利用起来
src\fast_model\gen_model.rs:642  # TODO: 检查两个类型是否有可能在一个层级树里，如果不需要可以跳过
src\fast_model\loop_model.rs:36  # TODO: 暂时不用多线程，有一些问题
src\fast_model\loop_model.rs:140  # TODO: 关于justline，可能需要jusline的信息才能判断中心点
src\fast_model\occ_generate.rs:409  # TODO: 排除已经生成了的模型
src\fast_model\occ_generate.rs:561  # TODO: edge 这里取中点就可以了
src\fast_model\occ_generate.rs:731  # TODO: 如果没有transform，直接按None处理，都是默认Transform::IDENTITY
src\fast_model\query.rs:23  # TODO: visible 不应该在这里执行过滤
src\fast_model\query.rs:29  # TODO: add other types
src\fast_model\room_model.rs:42  # TODO: need figure out
src\graph_db\structs.rs:162  # TODO: use custom serde
src\lib.rs:203  # TODO: 还有个问题，可能需要通过队列来排队任务
src\lib.rs:292  # TODO: 如何处理初始化的同步，第一次启动一定要同步一次，首先生成archive文件，然后再同步
src\versioned_db\database.rs:316  # TODO: 改成一对多的实现
src\versioned_db\database.rs:782  # TODO: 按照文件大小排序，只有小于多少的能开启多线程，模型一大就不合适了
```

## changes (last 10 commits — 50 minutes ago)
```
output\audit_ref_gap_probe.py                 +table
output\gen_dchc_fixture.py                    +table
output\ref_edge_delta_probe.py                +curated_table  +db_option  +Surreal  +__init__
```

## .ida_scratch

### .ida_scratch\analysis\ATGTIX_builder_55F4FFC.c
```
while(2)
while(1)
if(++*a7 > *a6)
if(!v15)
if(v15 != -1)
```

### .ida_scratch\analysis\ATNLOG_getter_55BC98B.c
```
sub_55BC98B(int *a1, _DWORD *a2, int *a3, _DWORD *a4) → int __cdecl
if(v11 == 4 && dword_6C21070[v16 - 1] == 11) → else
if(v11 == dword_6C21070[v16 - 1]) → else
for(i = v12; ; i = dword_6C3F6C0[512 * v22 - 513 + v26])
if(v19 <= 0)
if(v22 <= 0)
if(v23 != -1)
switch(v11)
if(v31 > 0)
if(v25 > 0)
switch(v11)
if(v33 > 0)
```

### .ida_scratch\analysis\ATRDRC_pagecache_5391D48.c
```
if(!*a2)
if(dword_6394FA8 && dword_6392BA0[dword_6394FA8 - 1] == *a2)
if(v8 <= 0)
if(unk_6B44CE0 == 1)
while(dword_6392BA0[*a3 - 1] != *a2)
if(dword_6393DA0[*a3 - 1] < v7)
```

### .ida_scratch\analysis\case10_12_531E237.c
```
sub_531E237(_DWORD *a1, _DWORD *a2) → int __cdecl
if(v7 != 3 && v7 != 2)
if(v50 > 0)
if(v57 > 0)
if(v49 != -1)
if(v55 != -1)
if(v68 == 1)
if(v72 <= 0)
while(1)
if(v77 > 0)
```

### .ida_scratch\analysis\case11_5311498.c
```
sub_5311498(int a1, _DWORD *a2) → int __cdecl
if(v16 == 2)
if(v11 < v6)
if(v27 != -1)
if(v30 != -1)
if(v37 > 0)
```

### .ida_scratch\analysis\case13_5331E59.c
```
if(v18 > 0)
if(v21 != -1)
if(v25 != -1)
if(v32 > 0)
```

### .ida_scratch\analysis\case14_5330D63.c
```
sub_5330D63(_DWORD *a1, int a2, _DWORD *a3) → int __cdecl
```

### .ida_scratch\analysis\case2_5323EEB.c
```
if(v16 != -1)
if(v19 != -1)
```

### .ida_scratch\analysis\case29_531AEA4.c
```
if(v7 > 0)
if(v20 > 0)
if(v23 > 0)
if(v38 != -1)
if(v41 != -1)
if(!v44)
if(!v47)
if(v50 > 0)
```

### .ida_scratch\analysis\case30_533890E.c
```
if(v14 == 1)
if(v19 != -1)
if(v22 != -1)
if(!v26)
if(!v29)
if(v32 > 0)
```

### .ida_scratch\analysis\case31_5334C68.c
```
if(v20 > 0)
if(v23 > 0)
if(v26 != -1)
if(v29 != -1)
if(!v32)
if(!v35)
if(v38 > 0)
if(v17 == 1)
if(v41)
```

### .ida_scratch\analysis\case32_52F688A.c
```
if(v18 == 1)
if(v30 > 0)
if(v33 != -1)
if(v36 != -1)
if(!v39)
if(!v42)
if(v45 > 0)
```

### .ida_scratch\analysis\case4_530DFD4.c
```
if(v13 == 1)
switch(v16)
if(v23)
if(v14 != 5)
if(v25 > 0)
if(v26 > 0)
if(v34 != -1)
if(v39 != -1)
```

### .ida_scratch\analysis\case6_530A6C1.c
```
if(v21 > 1)
if(v21 > 1)
if(v21 > 1)
if(v22 != -1)
if(v25 != -1)
if(v29 > 0)
if(v32 > 0)
if(v34 > 0)
```

### .ida_scratch\analysis\case7_53141FD.c
```
sub_53141FD(int a1, _DWORD *a2) → int __cdecl
if(v22 > 0)
if(v25 > 0)
if(v27 != -1)
if(v30 != -1)
if(v48 > 0)
```

### .ida_scratch\analysis\case8_530C65F.c
```
if(v15 == 1)
if(v21 != -1)
if(v24 != -1)
if(v29 == 1)
if(v30 > 1)
if(v30 > 1)
if(v30 > 1)
if(v35 > 0)
```

### .ida_scratch\analysis\case9_532CCD1.c
```
if(v13 <= 0)
if(v30 > 0)
if(v35 > 0)
if(*a3)
if(v20 != -1)
if(v28 != -1)
if(v40 > 0)
```

### .ida_scratch\analysis\db_userchanges.c
```
if(isElementCreated)
while(v17 != v13)
if(!isElementCreated)
if(v11 != &v36)
if(v32 != v33)
if(v21)
if(v37 == v38)
if(v38 != v39)
if(*v9 == ATT_OWNER)
if(v38)
```

### .ida_scratch\analysis\F3BDAD_add_5297CCF.c
```
if(v12 == 3) → else
for(ii = 1; ii <= 3; ii += 2)
for(ii = 0; ii <= 4; ++ii)
for(ii = 0; ii <= 4; ++ii)
```

### .ida_scratch\analysis\FDF3DV_52949A4.c
```
for(i = 1; i <= 5; ++i)
for(i = 1; i <= 6; ++i)
for(i = 1; i <= 15; ++i)
```

### .ida_scratch\analysis\FHDBRN_reader_5B9B400.c
```
if(!v6)
if(v9 != 3)
```

### .ida_scratch\analysis\FHDBRN_wrap_5B8D4F0.c
```
FHDBRN(_DWORD *a1, _DWORD *a2, int a3, _DWORD *a4) → int __cdecl
```

### .ida_scratch\analysis\FZ3SGL_5297141.c
```
if(*a3 != 41)
```

### .ida_scratch\analysis\FZBG3D_case3_16_5296A17.c
```
if(v14 == 1)
if(v17 != -1)
if(v20 != -1)
for(i = 1; i <= 5; ++i)
```

### .ida_scratch\analysis\HQTYPE_5CBC3D0.c
```
HQTYPE(_DWORD *a1) → int __cdecl
if(v2)
```

### .ida_scratch\analysis\sub_52973DB.c
```
sub_52973DB(int a1, int *a2, _DWORD *a3, int a4, _DWORD *a5, int a6) → int __cdecl
for(i = 1; i <= 5; ++i)
for(i = 1; i <= 5; ++i)
```

### .ida_scratch\analysis\sub_52977DE.c
```
sub_52977DE(int *a1) → int __cdecl
```

### .ida_scratch\analysis\V3MAP.c
```
V3MAP(int *a1) → int __cdecl
```

### .ida_scratch\attr_hash_solve.py
```
def load_pairs(path)  :12-15  # For each name length, derive per-position weight from pairs 
def fit_weights(pairs)  :18-44  # For each name length, derive per-position weight from pairs 
def main()  :47-109
```

### .ida_scratch\decode_lookup_table.py
```
def load_names()  :18-28
def unhash(h)  :31-45  # Invert h = 27**4 + sum(val_i * 27**i), val 1
def label(h, names)  :48-55
def main()  :58-118
```

### .ida_scratch\out_attr_core.c
```
for(i = 0; i < v11[0]; ++i)
if(v14 > 0)
if(!dword_61EBF8C)
if(a2 == -1)
```

### .ida_scratch\out_attr_getter.c
```
sub_55BB60C(int a1, int a2, _DWORD *a3) → void __cdecl
sub_55BB6C3(int *a1, int *a2, int *a3, _DWORD *a4) → int __cdecl
if(*v7 < 0)
if(v15 != dword_6BCA6C8)
if(dword_6C08EB4 > 0)
while(v15 != dword_6BE26E0[v17 - 1])
if(v11 == dword_6BE2870[v17 - 1] || v11 == 3) → LABEL_12:
if(*v7 <= 387951929)
for(i = *v7; ; i = dword_6C3F6C0[512 * v26 - 513 + v32])
if(i == dword_6BCA6C0)
if(v22 <= 0)
if(v26 <= 0)
if(v30 == -1)
switch(v11)
if(v38 > 0)
switch(v11)
if(v37 > 0)
if(v19 <= 0)
switch(v11)
if(v28 > 0)
```

### .ida_scratch\out_attr_read.c
```
if(dword_61EBF7C)
if(v2)
if(v19)
if(!*v3)
```

### .ida_scratch\out_attrlist.c
```
db_get_attribute_list(int a1, int a2, int a3, int a4) → BOOL __cdecl
sub_5AADCD0(int a1, int a2, int a3, int a4, int a5) → BOOL __cdecl
```

### .ida_scratch\out_conn_pairing_table.md
```
h1 Core3D.dll `desdblib/LOOKUP` connection-pairing table
```

### .ida_scratch\out_noun_readdata.c
```
if(dword_61EBF7C)
if(v2)
if(v3)
```

### .ida_scratch\out_pair_lookup.c
```
sub_5AAB330(int a1, int a2, int a3, int a4) → BOOL __cdecl
if(dword_6453B98)
if(dword_6A54028)
if(dword_6A5402C)
if(v5 != v4)
```

### .ida_scratch\probes\attlib_chain_probe.py
```
def own_array(tbl, key, fid, cap)  :24-40  # Array read WITHOUT base_type inheritance (own slot only)
def base_of(tbl, key)  :43-52
def chain_list(key, fid)  :55-71  # base-first concatenation over the base_type chain, de-duplic
```

### .ida_scratch\probes\attlib_dump_probe.py
```
def split_pages(data)  :13-15
def l2r(x)  :18-19
def build_key_index(pages, raw_start)  :22-39
```

### .ida_scratch\probes\attlib_layout_probe.py
```
class Table  :76-134
  def __init__(pages, ent_start, fld_start)
  def cell(rp, i)
  def resolve(key, fid)
  def get(key, fid)
  def get_array(key, fid, cap)
def split_pages(data)  :16-18
def l2r(x)  :21-22
def build_key_index(pages, raw_start)  :25-42
def build_field_index(pages, raw_start)  :45-73
```

### .ida_scratch\probes\attlib_schema_probe.py
```
class Table  :74-143
  def __init__(pages, ent_start, fld_start)
  def cell(rp, idx)
  def slot(s, col)
  def at(s, off)
  def resolve(key, field_id)
  def get(key, field_id)
  def get_array(key, field_id, cap)
def split_pages(data)  :12-14
def l2r(logical)  :17-18
def build_key_index(pages, raw_start)  :21-39  # ATGTIX: (key, addr) 2-int pairs -> (raw_page, row_base)
def build_field_index(pages, raw_start)  :42-71  # sub_55F53B8 (ATGTDF): variable-length [key, type, hasDef, de
def main()  :146-185
```

### .ida_scratch\probes\attlib_tablec.py
```
def walk3(raw_start, raw_end)  :15-26
```

### .ida_scratch\probes\bitindex.py
```
def pair(nh_s, attrs)  :14-29
```

### .ida_scratch\probes\crosscheck_slots.py
```
def pair(nh_s, attrs)  :14-26
```

### .ida_scratch\probes\defprobe.py
```
def pair(nh_s, attrs)  :14-29
def cls(v)  :54-56
```

### .ida_scratch\probes\diagnose_nofit.py
```
def find_type(nh)  :14-19
```

### .ida_scratch\probes\firstbad.py
```
def wlen(a)  :9-18
```

### .ida_scratch\probes\holes.py
```
def pair(nh_s, attrs)  :14-29
```

### .ida_scratch\probes\ida_q14.py
```
def call(tool, args)  :8-13
def show(out)  :15-16
def main()  :18-46
```

### .ida_scratch\probes\learnblack.py
```
def pair(nh_s, attrs)  :14-29
```

### .ida_scratch\probes\noun_layout_simulate.py
```
def scalar_words(a)  :38-45
def match_nouns(layout, snap, hash_to_name)  :48-70
def main()  :73-154
```

### .ida_scratch\probes\noun_layout_validate.py
```
def word_len(a)  :22-28
def match_nouns(layout, snap, hash_to_name)  :31-54
def main()  :57-129
```

### .ida_scratch\probes\noun_len_learn.py
```
def pair(nh_s, attrs)  :26-41
```

### .ida_scratch\probes\noun_order_probe.py
```
def pair(nh_s)  :20-36
```

### .ida_scratch\probes\noun_slot_predicate.py
```
def pair(nh_s, attrs)  :23-38
def report(title, keyfn, limit)  :55-65
```

### .ida_scratch\probes\prefixmodel.py
```
def pair(nh_s, attrs)  :14-29
```

### .ida_scratch\probes\purp.py
```
def pair(nh_s, attrs)  :14-29
```

### .ida_scratch\probes\repair_slots.py
```
def wlen(a)  :11-20
def subsets_summing(items, need, cap)  :27-35  # items: [(idx, words)] -> \u6240\u6709\u548c\u4e3a need \u768
```

### .ida_scratch\probes\slotcombo.py
```
def pair(nh_s, attrs)  :15-30
def fv(a, fname)  :42-47
def score(keyfn)  :52-58
def getter(n)  :63-64
```

### .ida_scratch\probes\slotfield.py
```
def pair(nh_s, attrs)  :15-30
```

### .ida_scratch\probes\solve_slots.py
```
def wlen(a)  :14-24
def pair(nh_s, attrs)  :30-42
def find_type(nh)  :60-65
```

### .ida_scratch\probes\validate_impl_len.py
```
def wlen(a)  :12-17
def pair(nh_s, attrs)  :23-35
```

## output

### output\audit_ref_gap_probe.py
```
def table(name: str) → set  :16-19
```

### output\gen_dchc_fixture.py
```
def table(name: str)  :40-43
```

### output\ref_edge_delta_probe.py
```
class Surreal  :41-69
  def __init__(url: str, ns: str, db: str, user: str, pw: str)
  def query(sql: str)
  def count(sql: str)
def curated_table(src: str, name: str) → set  :26-29
def db_option(key: str, default: str) → str  :32-38
```

### output\dchc_coverage_probe.py
```
def load(*parts)  :21-22
def curated_affects()  :29-30
def const_set(name)  :33-37
```

### output\increment-test\README.md
```
h1 dbnum 8000 incremental move verification
```

## src

### src\data_interface\manual_update.rs
```
pub struct NetChange  :149-155  # Net change for one `refno` after merging the whole window
pub struct NetChangeDetail  :162-166  # Net change of one `refno` with its identity kept for graph r
pub struct DeliveryUnitSummary  :228-260  # One affected minimal delivery unit (spec §预览结构)
pub struct ZoneSummary  :266-279  # Net change statistics grouped by the nearest owning ZONE
pub struct OwnerNode  :283-288  # One node of the ownership graph used for delivery-unit resol
pub struct OwnershipSnapshot  :297-308  # Pre/post ownership snapshot for the pending window
pub struct ResolvedUnit  :481-486  # A resolved delivery unit for one change in one state
pub struct ReverseRefEdges  :661-669  # One changed element's contribution to the ADR-003 reverse-re
pub struct ReverseIndexRebuildReport  :744-755  # Result of a full `ref_rev` rebuild from the current live `pe
pub struct DataBatchResult  :1605-1620  # Result of one `dbnum` data batch within a manual execution
pub struct ModelUnitResult  :1633-1650  # Result of one model delivery unit within a manual execution
pub struct ManualUpdateResult  :1654-1660  # Full result of one manual update execution
pub struct PendingModelUnit  :1716-1727  # One persisted model pending-retry task: `dbnum` + delivery-u
pub struct UnitTask  :1807-1819  # One model delivery-unit generation task inside an execution 
pub struct SessionPreview  :1982-1987  # Raw per-session change counts (spec §预览结构: 会话层保留原始变化记录)
pub struct DbnumPreview  :1991-2027  # Preview for one `dbnum` batch
pub struct ManualUpdatePreview  :2031-2044  # Full read-only preview of a project's pending manual update
pub enum IncomingKind  :86-90  # One incoming element operation kind within a session (drops 
pub enum NetOp  :107-114  # Net operation for one `refno` after merging all its ops acro
pub enum ManualUpdateStatus  :1579-1589  # Final status of one manual update run (spec §失败与重试 最终任务状态)
pub enum BatchStatus  :1594-1601  # Outcome of one `dbnum` data batch
pub enum UnitGenStatus  :1625-1629  # Outcome of one model delivery-unit generation
pub enum ManualUpdateEvent  :1676-1698  # Two-stage progress events (spec §任务与进度: 数据批次 + 模型交付单元，无百分比)
impl IncomingKind  :92-101
impl OwnershipSnapshot  :310-323
```

### src\data_interface\model_impact.rs
```
pub struct OperationEffectSummary  :73-88  # 一次操作的「效果汇总」：动作标志 + 观察到的效果集合 + 保留的原始 DCHC code + 变更属性名。
pub enum AttributeModelImpact  :27-31  # 属性对模型生成输入的影响（三态，保留作向后兼容 / 粗判）。
pub enum AttributeEffect  :37-50  # 更细的「效果分类」——把 bool/三态升级为 effect（对齐 core
pub enum OperationImpact  :62-69  # 一次操作对模型的处理动作。
pub enum ChangeBucket  :489-503  # core
impl AttributeEffect  :52-58
  pub fn affects_model(self) → bool  :55-55  # 是否影响模型（需重生成或变换更新）。仅 `DataOnly` 为否。
impl OperationEffectSummary  :90-101
  pub fn impact(&self) → OperationImpact  :92-92  # 归约为调度用的三态动作。
impl ChangeBucket  :505-515
pub fn normalize_attribute_name(raw_name: &str) → String  :192-201  # 归一化属性名：去掉 `att
pub fn classify_attribute_effect(raw_name: &str) → AttributeEffect  :204-214  # 细粒度效果分类：查 [`ATTRIBUTE_EFFECT_TABLES`] 合并出的映射，未命中则 `Unknown`。
pub fn classify_attribute_effect_with_meta(raw_name: &str, is_reference: bool) → AttributeEffect  :225-232  # 带 att_meta 提示的效果分类（A2 · ELEMENT 自动级联）。
pub fn attribute_is_reference(raw_name: &str) → bool  :254-256  # 某属性名是否为**引用类型**（schema `att_type == ELEMENT`）——A2 ELEMENT 自动
pub fn classify_attribute_model_impact(raw_name: &str) → AttributeModelImpact  :259-265  # 三态分类（向后兼容）：由细粒度效果归约——DataOnly→KnownNeutral、Unknown→Unknown、其
pub fn raw_dchc_code(raw_name: &str) → Option<i32>  :273-279  # 保留的 Core3D 原始设计变化码（DCHC，字段 596407）。
pub fn attribute_affects_model(raw_name: &str) → bool  :296-301  # 属性是否命中「已知会影响模型」的分类——除 `DataOnly` 与 `Unknown` 之外的一切。
pub fn classify_operation_effects(op: &EleOperationData) → OperationEffectSummary  :336-399  # 汇总一次操作的效果（宁多勿漏），并保留原始 DCHC code。
pub fn classify_operation_impact(op: &EleOperationData) → OperationImpact  :402-404  # 判定一次操作应如何处理模型（宁多勿漏）：由 [`classify_operation_effects`] 归约为三态动作
pub fn is_loop_container_noun(noun: &str) → bool  :408-420  # 是否为 loop/坐标辅助类型（LOOP/PLOO/VERT/PAVE、*DATU 等）：这些自身不是
pub fn named_attr_refno(value: &NamedAttrValue) → Option<RefnoEnum>  :422-429
pub fn owner_change(op: &EleOperationData) → (Option<RefnoEnum>, Option<...  :437-472  # Extract the OLD and NEW OWNER references from a modify op (e
pub fn changed_owner_refnos(op: &EleOperationData) → Vec<RefnoEnum>  :475-478  # 提取一次修改里 OWNER 属性的新旧引用（元素被搬迁时，新旧 owner 两侧都需重生成）。
pub fn added_owner(op: &EleOperationData) → Option<RefnoEnum>  :522-532  # 一个 `Add` 操作里新元素的 owner（G2：新建元素时其 owner 必须记 MemberChanged，
pub fn user_change_buckets(op: &EleOperationData) → Vec<(ChangeBucket, RefnoEnum)>  :555-598  # 一次元素操作产生的 `(桶, refno)` 分配，忠实于 core
```

### src\data_interface\model_update_plan.rs
```
pub struct ModelWorkItem  :36-45
pub struct ModelUpdatePlan  :48-51
pub enum ModelWorkAction  :17-22
impl ModelWorkAction  :24-33
```

### src\admin.rs
```
pub async fn filter_sys_file(project_dir: &str) → anyhow::Result<()>  :4-41
```

### src\api\element.rs
```
pub fn gen_pdms_element_insert_sql(att: &NamedAttrMap, dbno: i32, children_map: &HashMap<RefU64, Vec<RefU64>>,) → String  :9-40  # 生成树结构的sql
pub fn cal_default_name(refno: RefU64, attr: &NamedAttrMap, children_map: &HashMap<RefU64, Vec<RefU64>>,) → String  :43-63  # 如果名称未给定，根据属性列表和children列表获得当前的元素的名称
pub fn get_order(refno: RefU64, attr: &NamedAttrMap, children_map: &HashMap<RefU64, Vec<RefU64>>,) → usize  :67-80  # 获得顺序值
```

### src\data_interface\cata_closure.rs
```
pub struct InMemoryCataLocator  :68-71  # 就地内存定位器：`ref0→dbnum` + `dbnum→(db_type, project, path)`。
pub struct ParsedCataEle  :459-470  # 部分解析得到的单个 CATA 元素（闭包扩展所需的最小信息）。
pub struct CataClosureConfig  :582-599  # 闭包行为配置。
pub struct CataClosureManifest  :647-658  # 闭包结果：每个 CATA dbnum 需解析的 refno 集合 + 统计。
pub struct CataClosureResolver  :663-674  # refno 级 CATA 引用闭包引擎（BFS）。
pub struct LazyFallbackOutcome  :994-997  # 惰性兜底结果统计。
pub struct GeoSmokeReport  :1207-1217  # 单根几何冒烟报告：对一组设计参考号逐个 `resolve_desi_comp` 算确定性摘要。
pub trait CataDbLocator  :46-53  # 把一个引用（`RefU64`）定位到它所属的 db —— 闭包引擎唯一的外部依赖。
impl InMemoryCataLocator  :76-242
  pub fn from_parts(ref0_to_dbnum: HashMap<u32, u32>, dbnum_files: HashMap<u32, (String, String, PathBuf) → Self  :78-81  # 直接注入（单测 / 自定义来源）。`dbnum_files`：dbnum -> (db_type, project, p
  pub fn ref0_count(&self) → usize  :102-102  # 已登记的 `ref0` 数（诊断用）。
  pub fn dbnum_count(&self) → usize  :107-107  # 已登记的 dbnum 数（诊断用）。
  pub async fn build_for_project(project: &str) → anyhow::Result<Self>  :114-114  # 端到端构建：读 `dbnum_watermark` 得各库文件身份 → 扫描各库 `ref0` 集（带磁盘指纹缓存）。
  pub fn build_from_dir(project: &str, root_dir: &Path) → Self  :200-200  # 目录扫描构建（无需 SurrealDB）：遍历工程目录下全部 db 文件，建
impl InMemoryCataLocator  :270-284
impl Ref0IndexCache  :423-451
impl CataClosureConfig  :601-614
impl CataClosureConfig  :616-643
  pub fn precise() → Self  :619-619  # 精确模式（refno 级按需 / 惰性小闭包）：children 仅对几何与点集容器展开
  pub fn excluding_dbnum(mut self, dbnum: u32) → Self  :634-634
  pub fn excluding_dbnums(mut self, dbnums: impl IntoIterator<Item = u32>) → Self  :639-639
impl CataClosureResolver  :676-813
  pub fn new(locator: &'a L, cfg: CataClosureConfig) → Self  :677-677
  pub fn with_retain_attmaps(mut self, retain: bool) → Self  :690-690  # 开启属性表保留（小闭包惰性兜底场景；大闭包慎用，内存随 visited 线性增长）。
  pub fn take_attmaps(&mut self) → HashMap<RefU64, (NamedAttrM...  :696-696  # 取走保留的属性表（`retain_attmaps` 开启时在 `resolve()` 后调用）。
```

### src\data_interface\db_manager.rs
```
impl AiosDBManager  :35-59
  pub async fn get_gen_model_root_refnos(&self, db_nos: &[i32]) → anyhow::Result<Vec<RefU64>>  :37-37  # 获得db number 对应的site参考号
  pub async fn get_gen_model_target_refnos(&self, geo_type: GeoEnum, db_nos: &[i32], is_parent: bool,) → anyhow::Result<Vec<RefU64>>  :51-56  # 获取待调试或者整个db的参考号集合
```

### src\data_interface\db_model.rs
```
impl AiosDBManager  :68-227
  pub async fn init_form_config() → anyhow::Result<Self>  :70-70  # 从默认配置文件初始化
  pub async fn exec_watcher(mgr: Arc<AiosDBManager>) → anyhow::Result<()>  :79-79  # 看门狗的失败必须向上传播（T903）：过去这里 `
  pub async fn run_e3d_clone_bg_task(mgr: Arc<AiosDBManager>) → anyhow::Result<()>  :86-86
  pub async fn exec_delta_clone_remotes(watcher: &PdmsWatcher, sync_msg: SyncE3dFileMsg,) → anyhow::Result<bool>  :101-104
  pub async fn spawn_exec_watcher(mgr: Arc<AiosDBManager>) → anyhow::Result<()>  :155-155
  pub async fn demo_mqtt_requests()  :171-171
  pub async fn poll_sync_e3d_mqtt_events(watcher: Arc<PdmsWatcher>)  :205-205  # 另外将里面可能有关联的db，也要同步检查后一下？？
```

### src\data_interface\dbnum_state.rs
```
pub struct FileObservation  :30-39  # File observation captured during a (read-only) scan
pub struct DbnumState  :43-55  # Effective DBNUM state resolved from the stored record (+ one
pub enum FileAnomaly  :89-109  # A file-identity anomaly for one `dbnum` (see spec §文件异常)
impl DbnumState  :169-293
  pub async fn list_registered() → anyhow::Result<Vec<DbnumSta...  :172-172  # List registered DB files
  pub async fn read(dbnum: u32) → anyhow::Result<Option<Dbnum...  :229-229  # Read the effective state for one `dbnum` (with one-time migr
  pub async fn applied_sesno(dbnum: u32) → anyhow::Result<i32>  :259-259  # Authoritative applied watermark for one `dbnum` (0 when unin
  pub async fn record_scan(obs: &FileObservation) → anyhow::Result<()>  :275-275  # Persist a scan observation WITHOUT touching the applied wate
pub fn resolve_migrated_applied_sesno(existing_applied: Option<i32>, legacy_watermark_sesno: Option<i32>, info_table_max_sesno: Option<i32>,) → Option<i32>  :116-124  # Resolve the effective applied watermark from the three order
pub fn check_file_against_state(stored_db_type: Option<&str>, stored_path: Option<&str>, applied_sesno: i32, observed_db_type: &str, observed_path: &str, observed_file_latest_sesno: i32,) → Option<FileAnomaly>  :130-159  # Classify one observed file for one `dbnum` against its store
```

### src\data_interface\generation_root.rs
```
pub struct GenerationNode  :43-47
pub struct GenerationRoot  :50-55
pub enum GenerationRootKind  :36-40
pub fn is_coarse_hierarchy_noun(noun: &str) → bool  :57-60
pub fn is_delivery_unit_noun(noun: &str, unit_types: &[String]) → bool  :62-68
pub fn resolve_delivery_unit_types(appended: &[String]) → Vec<String>  :100-102  # Defaults ∪ configured additions
pub fn resolve_delivery_unit_types_from_config(configured: Option<&[String]>, appended: &[String],) → Vec<String>  :111-131  # Resolve the effective delivery-unit type set from project co
pub fn configured_delivery_unit_types() → Vec<String>  :137-150  # Effective delivery-unit types for the running project
pub fn resolve_element_generation_root(refno: RefnoEnum, unit_types: &[String], lookup: impl FnMut(RefnoEnum) → Option<GenerationNode>, ) -...  :207-234  # Resolve a changed element:
pub fn resolve_owner_generation_root(owner: RefnoEnum, unit_types: &[String], lookup: impl FnMut(RefnoEnum) → Option<GenerationNode>, ) -...  :239-257  # Resolve an old/new OWNER reference
pub async fn resolve_live_element_generation_root(refno: RefnoEnum, unit_types: &[String],) → anyhow::Result<Option<Gener...  :289-304
pub async fn resolve_live_owner_generation_root(owner: RefnoEnum, unit_types: &[String],) → anyhow::Result<Option<Gener...  :306-321
```

### src\data_interface\helper.rs
```
pub async fn delete_inst_relate_cascade(refnos: &[RefnoEnum], chunk_size: usize,) → anyhow::Result<()>  :91-118  # 级联删除 inst_relate 及其关联的 geo_relate 和 inst_geo 数据
pub async fn delete_inst_relate_subtree(refnos: &[RefnoEnum], chunk_size: usize,) → anyhow::Result<()>  :131-147  # 收集给定 refno 及其**子树（含已软删节点）**的全部节点，级联删除它们的
```

### src\data_interface\increment_manager.rs
```
pub struct IncrementInfo  :241-252  # 增量更新信息结构体
impl IncrementInfo  :280-310
  pub fn is_modified(&self) → bool  :287-287  # 检查元素是否被修改
  pub fn is_deleted(&self) → bool  :297-297  # 检查元素是否被删除
  pub fn is_added(&self) → bool  :307-307  # 检查元素是否为新增
impl AiosDBManager  :340-548
  pub async fn update_mysql_pdms_elements_simple(&self, range_eles: &BTreeMap<u32, Vec<EleOperationData>>,) → anyhow::Result<()>  :362-365  # 简化的MySQL pdms_element表更新方法
```

### src\data_interface\increment_pipeline.rs
```
pub struct IncrFileSuccess  :29-39  # One file that completed Surreal persist + watermark advance
pub struct IncrFileError  :43-46  # One file that failed before watermark advance
pub struct IncrResult  :50-55  # Result of [`IncrementPipeline::apply`]
pub struct IncrementPipeline  :356-356  # Independent deep module: collect delta → Surreal persist → d
impl IncrResult  :57-81
  pub fn all_changed_refnos(&self) → Vec<RefU64>  :58-58
  pub fn geometry_changed_refnos(&self) → Vec<RefU64>  :66-66  # Refnos from successes that are not SYS meta DBs (eligible fo
  pub fn had_work(&self) → bool  :74-74
  pub fn has_db_type(&self, db_type: &str) → bool  :78-78
impl StageTimings  :330-352
impl IncrementPipeline  :386-547
  pub fn new() → Self  :387-387
  pub fn collect_changes(path: &std::path::Path, sesno_range: RangeInclusive<i32>,) → anyhow::Result<BTreeMap<u32...  :397-400  # Side-effect-free change collection for one file over a sesno
  pub async fn apply(&self, increment_ranges_map: IndexMap<PathBuf, (DbPageBasicInfo, RangeInclusive<i32>, String) → IncrResult  :416-419  # Apply incremental updates for the given sesno ranges
  pub async fn apply_with_precollected(&self, increment_ranges_map: IndexMap<PathBuf, (DbPageBasicInfo, RangeInclusive<i32>, String) → IncrResult  :434-441  # 同 [`Self::apply`]，但允许调用方交出**已经收集好**的增量窗口，避免同一个文件
```

### src\data_interface\increment_record.rs
```
pub struct IncrGeoUpdateLog  :11-22  # 需要修改的模型的增量参考号数据
impl IncrGeoUpdateLog  :24-55
  pub fn count(&self) → usize  :26-26
  pub fn get_all_visible_refnos(&self) → HashSet<RefnoEnum>  :34-34
  pub async fn get_all_geom_refnos_deep(&self) → HashSet<RefnoEnum>  :44-44
```

### src\data_interface\interface.rs
```
pub trait PdmsDataInterface  :16-16
```

### src\data_interface\mesh_manager.rs
```
impl AiosDBManager  :9-86
  pub async fn cache_plant_meshes(&self, geo_hashes: impl IntoIterator<Item = &u64>, overwrite: bool,) → anyhow::Result<bool>  :11-15  # 拉取geo_hashs对应的meshes
  pub async fn get_plant_mesh(&self, geo_hash: u64) → anyhow::Result<Option<Plant...  :38-38  # 获取geo_hash 对应的plant mesh数据
  pub async fn get_transformed_plant_mesh(&self, geo_hash: u64, t: &Transform,) → anyhow::Result<Option<Plant...  :52-56  # 获得变换后的mesh
  pub async fn get_transformed_mesh_elev(&self, geo_hash: u64, t: &Transform,) → anyhow::Result<Option<(f32,...  :63-67  # 获得变换后的mesh的标高
```

### src\data_interface\model_refresh.rs
```
pub struct ModelRefreshPolicy  :30-30  # Deep module: one interface, swappable refresh adapters
impl ModelRefreshPolicy  :32-119
  pub async fn cleanup_deleted_by_pe_state(refnos: &[RefU64]) → anyhow::Result<()>  :93-93  # F1/F3（T304）补偿路径专用：补偿只有 `changed_refnos`（不含操作类型），故按**当前
```

### src\data_interface\model_update_pending.rs
```
pub struct IncrementUpdateAttempt  :27-34  # Short-lived recovery record written before any PE mutation
pub struct PendingModelWork  :47-60
pub async fn enqueue_plan(plan: &ModelUpdatePlan) → anyhow::Result<()>  :72-77  # Persist the exact model work before advancing `applied_sesno
pub async fn enqueue_legacy_changed_refnos(dbnum: u32, end_sesno: i32, db_type: &str, refnos: &[aios_core::RefU64],) → anyhow::Result<()>  :82-114  # Translate legacy changed-refno jobs into stable root work
pub async fn enqueue_legacy_root(dbnum: u32, end_sesno: i32, root_refno: &str, noun: &str,) → anyhow::Result<()>  :116-134
pub async fn load_attempt(dbnum: u32) → anyhow::Result<Option<Incre...  :166-197
pub async fn prepare_attempt(attempt: &IncrementUpdateAttempt) → anyhow::Result<()>  :199-230
pub async fn finalize_attempt(dbnum: u32, end_sesno: i32, plan: &ModelUpdatePlan, window_statements: &[String],) → anyhow::Result<()>  :265-287  # Atomically establish durable model work, advance the authori
pub async fn clear_regen_work(dbnum: u32, root_refno: &str) → anyhow::Result<()>  :355-374
pub async fn mark_regen_failed(dbnum: u32, root_refno: &str, error: &str) → anyhow::Result<()>  :376-389
pub async fn drain(mgr: &AiosDBManager) → anyhow::Result<usize>  :566-578
pub async fn drain_non_regen(mgr: &AiosDBManager) → anyhow::Result<usize>  :580-586
```

### src\data_interface\on_demand_model.rs
```
pub struct OnDemandModelResult  :31-38
pub enum OnDemandModelStatus  :25-28
impl AiosDBManager  :40-101
  pub async fn ensure_model_generated(&self, requested_refno: RefnoEnum,) → anyhow::Result<OnDemandMode...  :45-48  # Ensure that `requested_refno` has a renderable model
```

### src\data_interface\sesno_range.rs
```
pub struct SesnoUpdatePlan  :26-36  # Resolved incremental window for one DB file
pub struct SesnoRangeResolver  :40-40  # Independent module: watermark (dbnum) + nearest sesno → opti
impl SesnoRangeResolver  :42-170
  pub fn new() → Self  :43-43
  pub async fn query_watermark(dbnum: u32) → anyhow::Result<u32>  :60-60  # Authoritative watermark for this dbnum
  pub async fn resolve(&self, path: &Path, project: &str, dbnum: u32, file_latest_sesno: i32, skip_cata: bool, db_type: &str,) → anyhow::Result<Option<Sesno...  :69-77  # Build an update plan when `file_latest_sesno > watermark`, o
  pub async fn resolve_with_header(&self, path: &Path, project: &str, basic_info: DbPageBasicInfo, skip_cata: bool, db_type: &str,) → anyhow::Result<Option<Sesno...  :101-108  # Convenience when caller already has `DbPageBasicInfo` (watch
```

### src\data_interface\side_effect_pending.rs
```
pub struct PendingJob  :39-51
pub struct SideEffectCompensator  :55-55  # Independent module: enqueue / complete / drain side-effect j
pub enum SideEffectKind  :24-27
impl SideEffectKind  :29-36
impl SideEffectCompensator  :57-203
  pub async fn enqueue_from_incr(_mgr: &AiosDBManager, incr: &IncrResult) → anyhow::Result<()>  :64-64  # After PE+watermark success: enqueue only legacy non-model si
  pub async fn mark_done(kind: SideEffectKind, dbnum: u32, end_sesno: i32) → anyhow::Result<()>  :124-124
  pub async fn mark_failed(kind: SideEffectKind, dbnum: u32, end_sesno: i32, err: &str,) → anyhow::Result<()>  :138-143
  pub async fn drain(mgr: &AiosDBManager) → anyhow::Result<usize>  :163-163  # Replay pending/failed jobs (attempts < MAX_ATTEMPTS)
```

### src\data_interface\structs.rs
```
pub struct RefnoHasNegPosInfoTuple  :15-15  # 有负实体的集合信息, 返回tuple
pub struct RefnoHasNegInfoTuple  :25-25
pub struct CateBrepShapeData  :46-49
```

### src\data_interface\sync_publisher.rs
```
pub struct SyncOutcome  :18-22  # Outcome of one publish attempt
pub struct SyncPublisher  :26-28  # Independent module: archive + dedup + MQTT notify
impl SyncPublisher  :30-160
  pub fn new(mqtt_client: Arc<AsyncClient>) → Self  :31-31
  pub async fn ensure_archive(path: &PathBuf) → anyhow::Result<String>  :36-36  # Ensure a single `
  pub async fn publish(&self, incr: &IncrResult) → SyncOutcome  :50-50  # After a successful incremental apply: compress each success 
```

### src\data_interface\tidb_manager.rs
```
pub struct AiosDBManager  :41-60
impl AiosDBManager  :65-69
impl AiosDBManager  :71-333
```

### src\data_state\save_data_state.rs
```
pub async fn save_data_state(data: Vec<RefnoStatusInfo>, pool: &Pool<MySql>) → anyhow::Result<()>  :7-20
```

### src\defines.rs
```
pub struct RString  :29-29
impl RString  :37-37
```

### src\fast_model\aabb_tree.rs
```
pub async fn manual_update_aabbs(replace_exist: bool) → anyhow::Result<()>  :20-66  # 手动更新所有 inst_relate 的 AABB 包围盒
```

### src\fast_model\cal_model\bran_model.rs
```
pub async fn update_cal_bran_component() → anyhow::Result<()>  :5-9
```

### src\fast_model\cal_model\equip_model.rs
```
pub async fn update_cal_equip() → anyhow::Result<()>  :11-15
pub async fn update_cal_equip_wtrans() → anyhow::Result<()>  :20-47
pub async fn cal_equip_nearest_floor() → anyhow::Result<()>  :50-102
```

### src\fast_model\cata_model.rs
```
pub enum NgmrRemovedType  :120-131
pub fn init_chrome_tracing() → anyhow::Result<()>  :66-100  # Initializes Chrome tracing for performance analysis
pub async fn gen_cata_single_geoms(design_refno: RefnoEnum, brep_shape_map: &CateBrepShapeMap, design_axis_map: &DashMap<RefnoEnum, PlantAxisMap>,) → anyhow::Result<bool>  :134-259  # 获取单个元件的模型数据
pub fn cal_sjus_value(sjus: &str, height: f32) → f32  :263-272  # 计算对齐偏移值
pub async fn gen_cata_geos(db_option: Arc<DbOption>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :355-488  # 生成元件库的branch型几何体
pub async fn gen_cata_geos_with_tracing(db_option: Arc<DbOption>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, branch_map: Arc<HashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :1311-1365  # Simplified version of gen_cata_geos for tracing analysis
pub async fn gen_cata_geos_with_tracing(db_option: Arc<DbOption>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :1368-1380  # Simplified version of gen_cata_geos for tracing analysis
pub async fn query_ngmr_owner(refno: RefnoEnum, ngmr_geo_refno: RefnoEnum,) → anyhow::Result<Vec<RefnoEnum>>  :1383-1434
pub async fn gen_cata_geos_parallel_optimized(db_option: Arc<DbOption>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :1444-1604  # P0级优化：并行元件库几何体生成函数
```

### src\fast_model\coverage_audit.rs
```
pub fn audit_enabled() → bool  :38-46  # 观测是否开启。
pub async fn audit_segment(target_refnos: &[RefnoEnum], skip_exist: bool)  :64-123  # 观测一个生成分段：统计差集 noun 在本段子树里的真实命中数。
pub fn report_and_reset()  :126-149  # 打印本次生成的累计观测结果并清空，供下一次生成重新计数。
```

### src\fast_model\gen_model.rs
```
pub struct DbModelInstRefnos  :46-51  # 一个db生成模型里，汇总的参考号集合
impl DbModelInstRefnos  :53-149
  pub async fn execute_gen_inst_meshes(&self, db_option_arc: Option<Arc<DbOption>>,) → anyhow::Result<()>  :54-57
  pub async fn execute_boolean_meshes(&self, db_option_arc: Option<Arc<DbOption>>,) → anyhow::Result<()>  :105-108
pub async fn gen_all_geos_data(manual_refnos: Vec<RefnoEnum>, db_option: &DbOption, incr_updates: Option<IncrGeoUpdateLog>,) → anyhow::Result<bool>  :160-268  # 生成几何体数据
pub async fn process_meshes_by_dbnos(dbnos: &[u32], db_option: &DbOption) → anyhow::Result<()>  :281-303  # 更新模型数据
pub async fn gen_geos_data_by_dbnum(dbno: u32, db_option_arc: Arc<DbOption>, sender: flume::Sender<ShapeInstancesData>,) → anyhow::Result<DbModelInstR...  :317-463  # 生成几何体数据
pub async fn gen_geos_data(dbno: Option<u32>, manual_refnos: Vec<RefnoEnum>, db_option: &DbOption, incr_updates: Option<IncrGeoUpdateLog>, sender: flume::Sender<ShapeInstancesData>,) → anyhow::Result<Vec<RefnoEnum>>  :547-694  # 生成几何体数据
pub async fn query_tubi_size(refno: RefnoEnum, tubi_cat_ref: RefnoEnum, is_hang: bool,) → anyhow::Result<TubiSize>  :887-913  # 查询tubi的大小
```

### src\fast_model\loop_model.rs
```
pub async fn gen_loop_geos(db_option: Arc<DbOption>, loop_owner_refnos: &[RefnoEnum], sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :23-142  # 处理带有loop的元件
```

### src\fast_model\manifold_bool.rs
```
pub async fn apply_cata_neg_boolean_manifold(refnos: &[RefnoEnum], replace_exist: bool, dir: PathBuf,) → anyhow::Result<()>  :61-185  # 处理元件库有负实体的布尔运算
pub async fn apply_insts_boolean_manifold(refnos: &[RefnoEnum], replace_exist: bool, dir: PathBuf,) → anyhow::Result<()>  :225-234  # 对多个实例进行布尔运算
pub async fn apply_insts_boolean_manifold_single(refno: RefnoEnum, replace_exist: bool, dir: PathBuf,) → anyhow::Result<()>  :247-346  # 对实例进行布尔运算
```

### src\fast_model\occ_generate.rs
```
pub struct NegInfo  :754-761
pub struct ParamNegInfo  :775-782
pub struct CataNegGroup  :1011-1015
pub struct GmGeoData  :1018-1025
pub async fn test_gen_geos() → anyhow::Result<()>  :37-43  # 生成小的几何体
pub async fn gen_meshes_in_db(option: Option<Arc<DbOption>>, refnos: &[RefnoEnum],) → anyhow::Result<()>  :127-167  # 生成模型的部分，update aabb
pub async fn booleans_meshes_in_db(option: Option<Arc<DbOption>>, refnos: &[RefnoEnum],) → anyhow::Result<()>  :170-198  # 执行布尔运算的部分
pub async fn process_meshes_update_db(option: Option<Arc<DbOption>>, refnos: &[RefnoEnum],) → anyhow::Result<()>  :208-254  # 处理网格并更新数据库
pub async fn process_meshes_update_db_deep_default(refnos: &[RefnoEnum]) → anyhow::Result<()>  :265-268  # 使用默认数据库选项更新深层模型网格数据
pub async fn process_meshes_update_db_deep(dboption: &DbOption, refnos: &[RefnoEnum],) → anyhow::Result<()>  :280-353  # 使用指定数据库选项更新深层模型网格数据
pub async fn gen_inst_meshes(refnos: &[RefnoEnum], replace_exist: bool, dir: PathBuf,) → anyhow::Result<()>  :378-500  # 生成实例的网格数据
pub async fn update_inst_relate_aabbs_by_refnos(refnos: &[RefnoEnum], replace_exist: bool,) → anyhow::Result<()>  :678-745  # 刷新inst_relate 的 aabb
pub async fn apply_insts_boolean_occ(refnos: &[RefnoEnum], replace_exist: bool, dir: PathBuf,) → anyhow::Result<()>  :805-914
pub async fn apply_cata_neg_boolean_occ(dir: PathBuf) → anyhow::Result<()>  :1029-1146
```

### src\fast_model\pdms_inst.rs
```
pub async fn init_inst_relate_indices() → anyhow::Result<()>  :17-24  # 初始化数据库的 inst_relate 表的索引
pub async fn save_instance_data_single(inst_mgr: &ShapeInstancesData, replace_exist: bool,) → anyhow::Result<()>  :27-177  # 保存instance 数据到数据库
pub async fn save_instance_data(inst_mgr: &ShapeInstancesData, replace_exist: bool,) → anyhow::Result<()>  :397-536  # 保存instance 数据到数据库（并行优化版本）
```

### src\fast_model\prim_model.rs
```
pub async fn gen_prim_geos(db_option: Arc<DbOption>, prim_refnos: &[RefnoEnum], sender: flume::Sender<ShapeInstancesData>,) → anyhow::Result<bool>  :24-150  # 生成基本体的几何数据
```

### src\fast_model\query.rs
```
pub async fn query_gm_params(refno: RefnoEnum) → anyhow::Result<Vec<GmParam>>  :7-36
pub async fn get_generic_type(refno: RefnoEnum) → anyhow::Result<PdmsGenericT...  :39-47
```

### src\fast_model\resolve.rs
```
pub async fn get_or_create_scom_info(cata_refno: RefnoEnum) → anyhow::Result<ScomInfo>  :11-92  # 收集SCOM的信息, 暂时慎用缓存
pub async fn resolve_axis_params(refno: RefnoEnum, context: Option<CataContext>,) → anyhow::Result<BTreeMap<i32...  :95-111  # 求解axis的数值
pub async fn resolve_desi_comp(desi_refno: RefnoEnum, mut tubi_scom: Option<RefnoEnum>,) → anyhow::Result<CateGeomsInfo>  :114-152  # 求解design component
```

### src\fast_model\room_model.rs
```
pub async fn test_cal_rooms() → anyhow::Result<()>  :26-40
pub async fn test_cal_distance() → anyhow::Result<()>  :45-82
pub async fn build_room_relations(db_option: &DbOption) → anyhow::Result<()>  :96-120  # 构建房间关系
pub fn match_room_name_hd(room_name: &str) → bool  :178-181  # hd 正则匹配是否满足房间命名规则
pub fn match_room_name_hh(room_name: &str) → bool  :184-186  # hh 正则匹配是否满足房间命名规则
pub async fn cal_room_refnos(mesh_dir: &PathBuf, panel_refno: RefnoEnum, exclude_refnos: &HashSet<RefnoEnum>, inside_tol: f32,) → anyhow::Result<HashSet<Refn...  :251-386
```

### src\fast_model\shared.rs
```
pub fn negative_noun_names() → &'static [String]  :10-25  # Negative-geometry nouns from the decoded positive-equivalent
pub fn negative_noun_refs() → Vec<&'static str>  :27-29
pub fn is_negative_noun(noun: &str) → bool  :31-36
pub fn aabb_apply_transform(aabb: &Aabb, t: &Transform) → Aabb  :50-57  # 针对aabb，应用transform
```

### src\fast_model\utils.rs
```
pub async fn save_aabb_to_surreal(aabb_map: &DashMap<String, Aabb>)  :8-33
pub async fn save_pts_to_surreal(vec3_map: &DashMap<u64, String>)  :35-53
pub async fn save_transforms_to_surreal(trans_map: &HashMap<u64, String>) → anyhow::Result<()>  :55-69
```

### src\graph_db\mod.rs
```
pub struct ForeignEdges  :7-13
pub struct ParaDocument  :16-19
pub struct DataDocument  :22-27
```

### src\graph_db\structs.rs
```
pub struct PdmsEleDataVersioned  :16-36  # 版本数据库里存储的索引值
pub struct PdmsEleData  :83-96
pub struct PdmsEleGraphEdge  :99-103
pub struct PdmsEleEdge  :106-125
pub struct SSCEleGraphNode  :128-134
pub struct PdmsInstanceGraphEdge  :137-141
pub struct PdmsMdbEdge  :164-181
impl PdmsEleDataVersioned  :52-79
  pub fn get_scheme() → &'static str  :54-54
pub fn ser_refno_as_ref_type(refno: &RefU64, s: S) → Result<S::Ok, S::Error> whe...  :42-50
pub fn ser_refno_as_ele_edge(refno: &RefU64, s: S) → Result<S::Ok, S::Error> whe...  :143-148
pub fn de_refno_as_edge(deserializer: D) → Result<RefU64, D::Error> wh...  :150-159
```

### src\gui\config_panel_story.rs
```
pub struct ConfigPanelStory  :39-72
impl ConfigPanelStory  :74-82
impl ConfigPanelStory  :84-209
  pub fn view(window: &mut Window, cx: &mut App) → Entity<Self>  :85-85
impl ConfigPanelStory  :681-685
impl ConfigPanelStory  :687-798
```

### src\gui\logs.rs
```
pub struct LogUpdateEvent  :14-14
pub struct LogItem  :18-23
pub struct LogListItem  :35-39
pub struct LogListDelegate  :101-104
pub enum LogLevel  :27-31
impl LogListItem  :41-49
  pub fn new(id: impl Into<ElementId>, log_item: LogItem, selected: bool) → Self  :42-42
impl LogListItem  :51-98
impl LogListDelegate  :106-134
impl LogListDelegate  :136-153
  pub fn new() → Self  :137-137
  pub fn add_log(&mut self, message: impl Into<SharedString>, level: LogLevel)  :144-144
pub fn add_global_log(message: S, level: LogLevel)  :161-189
pub fn log_from_thread(message: S, level: LogLevel)  :193-220
```

### src\gui\mod.rs
```
pub fn run_gui()  :28-39
```

### src\gui\theme_engineering_console.rs
```
pub fn engineering_console_dark() → ThemeColor  :37-175  # Dark "Engineering Console" token set
pub fn engineering_console_light() → ThemeColor  :189-309  # Light "Precision Daylight" token set (same semantics, swappe
pub fn apply(cx: &mut App, dark: bool)  :315-335  # Apply the Engineering Console theme to the global [`Theme`]
pub fn apply_dark(cx: &mut App)  :338-340  # Convenience: apply the dark Engineering Console theme
```

### src\lib.rs
```
pub async fn run_cli(db_option: DbOption) → anyhow::Result<()>  :121-322
pub async fn run_app(option: Option<DbOptionExt>) → anyhow::Result<()>  :325-372  # 运行app
```

### src\mqtt_service\mod.rs
```
pub struct SyncE3dFileMsg  :9-28
pub struct MqttInstance  :71-74
impl SyncE3dFileMsg  :31-48
  pub fn new(file_names: Vec<String>, file_hashes: Vec<String>) → Self  :34-34
pub fn new_mqtt_inst(id: &str) → MqttInstance  :79-98
```

### src\noun_layout.rs
```
pub struct LayoutAttr  :25-42  # 一个属性在 `noun_layout
pub struct LayoutNoun  :46-55  # 一个 noun 的导出记录。属性顺序就是 `DbElementType
pub fn word_len(a: &LayoutAttr) → Option<u32>  :81-94  # 该属性在隐含块里占几个字。BOOL 返回 1，但整个 BOOL 游共用这一个字。
pub fn compute_offsets(attrs: &[LayoutAttr], slotted: &HashSet<i32>,) → Result<Vec<(i32, u32)>, Str...  :100-130  # 按 ADR-008 的布局规则算出每个占槽属性的 `(hash, 打包 offset)`。
pub fn implicit_len(attrs: &[LayoutAttr], slotted: &HashSet<i32>) → Result<u32, String>  :134-152  # 一个 noun 的隐含块总字长，即元素记录 word0 应该等于的值。
pub fn to_attr_infos(noun: &LayoutNoun, slotted: &HashSet<i32>) → Result<Vec<AttrInfo>, String>  :186-202  # 把一个 noun 的占槽属性折成 `AttrInfo` 列表，可直接填进 `PdmsDatabaseInfo`。
```

### src\options.rs
```
pub struct DbOptionExt  :8-52  # 扩展DbOption，添加异地部署相关的配置
impl DbOptionExt  :54-60
impl DbOptionExt  :62-66
pub fn get_db_option_ext() → DbOptionExt  :121-134  # 获取扩展的数据库选项
```

### src\tables.rs
```
pub fn gen_create_explicit_tables_sql() → String  :10-24
pub fn gen_create_uda_tables_sql() → String  :27-38
pub fn gen_create_dbno_infos_tables_sql() → String  :41-57
pub fn gen_create_element_tables_sql() → String  :60-89
pub fn gen_create_ssc_element_tables_sql() → String  :92-110
pub fn gen_create_data_state_tables_sql() → String  :113-124  # 创建 数据状态表
pub fn gen_create_project_mdb_sql() → String  :127-137
pub fn gen_insert_project_mdb_sql(att_map: &NamedAttrMap) → String  :139-150
pub fn gen_create_project_mdb_json_sql() → String  :153-165
pub fn gen_create_implicit_tables_sql(type_name: &str, att_map: &BTreeMap<u32, (String, AttrVal) → String  :168-236
pub fn gen_create_room_code_table_sql() → String  :238-245
pub fn gen_create_version_info_table_sql(project_name: &str) → String  :247-254
pub fn gen_create_pdms_version_table_sql() → String  :257-269  # 创建版本管理的数据表
pub fn gen_create_file_version_table_sql() → String  :272-281  # 创建存储每个文件的版本号表
pub fn gen_create_pdms_mesh_table_sql() → String  :283-290
```

### src\team_data.rs
```
pub struct SysDBData  :12-20  # Admin模块DB信息
pub async fn sync_team_data(mgr: &AiosDBMgr) → anyhow::Result<()>  :23-92  # 同步system db的信息
```

### src\test\test_performance.rs
```
pub struct PerformanceStats  :22-41  # 性能测试结果统计
pub struct StageAnalysis  :45-64  # 详细阶段分析
pub struct ParallelCataPerformanceStats  :1523-1550  # 并行元件库处理性能统计
pub struct GenGeosDataPerformanceStats  :1690-1711  # gen_geos_data 函数性能统计
pub struct GenGeosDataMetrics  :1715-1728  # gen_geos_data 性能指标
impl StageAnalysis  :66-80
impl PerformanceStats  :82-112
  pub fn new(dbno: u32) → Self  :83-83
  pub fn instances_per_second(&self) → f64  :98-98  # 计算每秒生成的实例数
  pub fn meshes_per_second(&self) → f64  :106-106  # 计算每秒生成的网格数
impl ParallelCataPerformanceStats  :1552-1686
  pub fn new(input_count: usize) → Self  :1553-1553
  pub fn calculate_metrics(&mut self)  :1577-1577  # 计算性能指标
  pub fn generate_report(&self) → String  :1604-1604  # 生成性能报告
impl GenGeosDataPerformanceStats  :1730-1883
  pub fn new(input_refno_count: usize) → Self  :1731-1731
  pub fn calculate_metrics(&mut self)  :1747-1747  # 计算性能指标
  pub fn generate_report(&self) → String  :1791-1791  # 生成性能报告
pub fn init_performance_tracing() → anyhow::Result<()>  :115-136  # 初始化性能追踪
pub async fn test_model_generation_performance(start_dbno: u32, end_dbno: u32, db_option: &DbOption,) → anyhow::Result<Vec<Performa...  :140-169  # 测试指定数据库号范围内所有模型的生成速度
pub fn analyze_performance_bottlenecks(stats: &[PerformanceStats]) → Vec<String>  :553-647  # 分析性能瓶颈并提供优化建议
pub fn generate_detailed_stage_analysis(stats: &[PerformanceStats]) → String  :786-929  # 生成详细的阶段耗时分析报告
pub fn generate_optimization_recommendations(stats: &[PerformanceStats]) → Vec<String>  :932-1056  # 生成针对性的优化建议
pub async fn test_gen_geos_data_performance(manual_refnos: Vec<RefnoEnum>, db_option: &DbOption,) → anyhow::Result<GenGeosDataP...  :1125-1243  # 测试 gen_geos_data 函数的性能
pub async fn test_gen_geos_data_performance_without_db_init(manual_refnos: Vec<RefnoEnum>, db_option: &DbOption,) → anyhow::Result<GenGeosDataP...  :1249-1332  # 测试 gen_geos_data 函数的性能（不初始化数据库连接）
```

### src\test\test_query\mod.rs
```
pub async fn init_test_surreal()  :16-32
```

### src\version_management\mod.rs
```
pub struct SetStatusData  :7-16
```

### src\version_management\set_status.rs
```
pub async fn check_need_update_version(attr_map: &AttrMap) → bool  :5-7  # 在增量更新中对比最新的属性，当 :CNPEversion 发生变化时,则需要存储该部分数据
pub async fn set_version_from_uda(save_data: SetStatusData) → anyhow::Result<()>  :10-12  # 当 check_need_update_version 为 true 或者 平台设置数据状态时，将对应的数据存到版本管理
```

### src\versioned_db\database.rs
```
pub enum SenderJsonsData  :52-58
pub trait MySqlMethods  :282-288
pub async fn create_project_database(project: &str, url: &str) → anyhow::Result<()>  :292-300  # 初始化project database
pub async fn create_info_database(aios_mgr: &AiosDBMgr) → anyhow::Result<()>  :304-374  # 初始化 info 库和表
pub async fn sync_pdms(db_option: &DbOption) → anyhow::Result<()>  :378-505  # 初始化同步pdms数据到数据
pub async fn define_dbnum_event() → anyhow::Result<()>  :507-546
pub async fn define_dbnum_event_array_id() → anyhow::Result<()>  :549-580  # 定义dbnum_info_table的更新事件, pe 的id 为array的情况
pub async fn execute_sql(conn: &Pool<MySql>, sql: &str) → bool  :583-602
pub async fn check_and_clear_db(db_no: u32) → anyhow::Result<()>  :604-625
pub async fn sync_total_async_threaded(db_option: &DbOption, project: &str, cur_dbno_set: Arc<DashSet<u32>>, db_types: &[&str], proj_progress_chunk: usize,) → anyhow::Result<HashMap<u32,...  :629-753  # 多线程同步数据，包括增量同步
```

### src\versioned_db\pe.rs
```
pub async fn save_pes(db_basic: &DbBasicData, total_attr_map: &DashMap<RefU64, NamedAttrMap>, db_num: i32, file_name: &str, db_type: &str, option: &DbOption, output: flume::Sender<SenderJsonsData>,) → anyhow::Result<()>  :42-74  # 保存element数据到版本管理
pub async fn save_pes_mysql(db_basic: &DbBasicData, project: &str, total_attr_map: &DashMap<RefU64, NamedAttrMap>, project_maps: &HashMap<String, Pool<MySql>>, option: &DbOption, db_num: i32,)  :77-154
pub async fn save_pe_relates(db_basic: &DbBasicData, output: flume::Sender<SenderJsonsData>)  :157-192
```

### src\versioned_db\task.rs
```
pub async fn initialize_global_db_sender()  :12-23
pub async fn get_global_db_sender() → flume::Sender<SenderJsonsData>  :56-63
```

### src\web_service\events.rs
```
pub struct WsEvent  :33-38  # 一条广播事件。`seq`/`ts` 不在此处：信封序号由各 WS 连接自行递增编排。
pub enum Topic  :16-19  # 事件主题（客户端经 WS `subscribe` 消息按主题订阅）。
impl Topic  :21-29
  pub fn parse(s: &str) → Option<Self>  :22-22
pub fn event_sender() → &'static broadcast::Sender<...  :43-45  # 全局事件发送端（懒初始化；容量 1024，慢消费者滞后跳帧由客户端 REST 对齐补偿）。
pub fn publish(topic: Topic, ty: &'static str, task_id: Option<String>, payload: serde_json::Value,)  :48-60  # 广播一条事件；无订阅者时返回错误被忽略（正常情形）。
pub fn notify_incr_applied(incr: &IncrResult)  :66-102  # 自动模式增量应用后的摘要事件（评审决议：仅摘要，不携带逐 refno 明细）。
```

### src\web_service\handlers.rs
```
pub struct ProjectReq  :24-27
pub struct TaskListQuery  :129-136
pub struct EnsureModelReq  :166-168
pub async fn health(State(state) → Json<serde_json::Value>  :36-43  # GET /api/v1/health
pub async fn update_preview(State(state) → Result<Json<serde_json::Val...  :46-59  # POST /api/v1/update/preview — 映射 `preview_manual_update`（spe
pub async fn update_execute(State(state) → Result<impl IntoResponse, A...  :62-126  # POST /api/v1/update/execute — 异步任务，进度经 WS 推送（spec §4
pub async fn tasks_list(State(state) → Json<serde_json::Value>  :139-149  # GET /api/v1/tasks
pub async fn task_get(State(state) → Result<Json<serde_json::Val...  :152-163  # GET /api/v1/tasks/{id}
pub async fn model_ensure(State(state) → Result<Json<serde_json::Val...  :171-195  # POST /api/v1/model/ensure — 映射 `ensure_model_generated`（幂等同步
pub async fn pending_units(State(_state) → Result<Json<serde_json::Val...  :198-205  # GET /api/v1/update/pending-units — 映射 `load_pending_model_un
pub async fn dbnums(State(_state) → Result<Json<serde_json::Val...  :208-213  # GET /api/v1/dbnums — 映射 `DbnumState::list_registered`（spec §
```

### src\web_service\mod.rs
```
pub struct AppState  :27-33  # 各 handler 共享的服务状态。
pub struct ApiError  :37-41  # 统一错误响应：`{ "code":
impl ApiError  :43-93
  pub fn bad_request(message: impl Into<String>) → Self  :44-44
  pub fn not_found(message: impl Into<String>) → Self  :52-52
  pub fn conflict(message: impl Into<String>) → Self  :60-60
  pub fn timeout(message: impl Into<String>) → Self  :68-68
  pub fn from_domain(error: anyhow::Error) → Self  :77-77  # 领域层 `anyhow::Error` 的统一映射：`sync_live` 前置条件拒绝归为 422，其余 500。
impl ApiError  :95-104
pub async fn serve_if_configured(mgr: Arc<AiosDBManager>) → anyhow::Result<()>  :107-113  # 按配置启动 Web 服务；`http_api_addr` 未配置时立即返回（不启动）。
pub async fn serve(mgr: Arc<AiosDBManager>, addr: &str, cors_origins: Option<Vec<String>>,) → anyhow::Result<()>  :116-147  # 在 `addr` 上启动服务并阻塞运行（正常情况下不返回）。
```

### src\web_service\tasks.rs
```
pub struct TaskEntry  :37-50
pub struct TaskRegistry  :54-56  # 插入序即时间序的注册表；超容量时从最老的已完结任务开始剔除。
pub enum TaskState  :18-23  # 任务状态机：`running -> succeeded | partial | failed`（单飞策略下无 queue
impl TaskState  :25-34
  pub fn as_str(&self) → &'static str  :26-26
impl TaskRegistry  :58-136
  pub fn new_task_id(prefix: &str) → String  :59-59
  pub fn running_for_project(&self, project: &str) → Option<String>  :69-69  # 同项目存在 running 任务时返回其 task_id（服务层单飞预检，spec §4
  pub fn insert_running(&self, task_id: &str, kind: &'static str, project: &str)  :77-77
  pub fn bump_events(&self, task_id: &str)  :104-104
  pub fn finish(&self, task_id: &str, state: TaskState, result: serde_json::Value)  :111-111
  pub fn get(&self, task_id: &str) → Option<TaskEntry>  :120-120
  pub fn list(&self, state: Option<&str>, kind: Option<&str>, limit: usize) → Vec<TaskEntry>  :125-125  # 按创建时间倒序（最近优先）过滤列出。
```

### src\web_service\ws.rs
```
pub async fn ws_handler(ws: WebSocketUpgrade, State(state) → Response  :30-32
```
