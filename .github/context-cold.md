<!-- Generated by SigMap gen-context.js v8.24.0 — cold context (MCP only) -->
<!-- NOT auto-injected. Retrieve via MCP: read_context({ module: "..." }) -->
<!-- Generated by SigMap gen-context.js v8.24.0 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| To rank files by topic | `sigmap --query "<topic>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

Always run `sigmap ask` (or `sigmap --query`) before searching for files relevant to a task.

## todos
```
src/api/attr.rs:117  # TODO: 需要进一步查找原因
src/data_interface/db_model.rs:96  # TODO: ，需要配置各个db对应的映射, 不同区域对应不同的db
src/data_interface/db_model.rs:116  # TODO: 如果没有需要新加数据
src/data_interface/interface.rs:71  # TODO: 后面要去掉，不需要使用这个，直接用上memcache + arangodb
src/data_interface/tidb_manager.rs:103  # TODO: 修改为图数据库，尽可能避免使用TIDB
src/data_interface/tidb_manager.rs:264  # TODO: 这里还需要将project的信息利用起来
src/data_to_file/modify/modify.rs:511  # TODO: 修改次数页第二页 变化的参考号和本参考号看起来毫无相关
src/fast_model/gen_model.rs:642  # TODO: 检查两个类型是否有可能在一个层级树里，如果不需要可以跳过
src/fast_model/loop_model.rs:36  # TODO: 暂时不用多线程，有一些问题
src/fast_model/loop_model.rs:140  # TODO: 关于justline，可能需要jusline的信息才能判断中心点
src/fast_model/occ_generate.rs:455  # TODO: 排除已经生成了的模型
src/fast_model/occ_generate.rs:621  # TODO: edge 这里取中点就可以了
src/fast_model/occ_generate.rs:706  # TODO: (与 inst_relate 同款的 D9 顺序问题)：inst_geo.aabb 指针在上面的并发任务里
src/fast_model/query.rs:23  # TODO: visible 不应该在这里执行过滤
src/fast_model/query.rs:29  # TODO: add other types
src/graph_db/structs.rs:162  # TODO: use custom serde
src/plug_in/water_calculation.rs:130  # TODO: ，更改门的角度参数，重新生成men的mesh - 开孔的长方体mesh
src/ssc.rs:1616  # TODO: 不能同时update多次 后期将这些aql优化到一次执行
src/versioned_db/database.rs:321  # TODO: 改成一对多的实现
src/versioned_db/database.rs:795  # TODO: 按照文件大小排序，只有小于多少的能开启多线程，模型一大就不合适了
```

## src

### src/admin.rs
```
pub async fn filter_sys_file(project_dir: &str) → anyhow::Result<()>  :4-41
```

### src/api/admin.rs
```
pub struct AdminData  :23-31  # 管理员信息
pub async fn sync_system_db(mgr: &AiosDBManager) → anyhow::Result<()>  :34-96  # 同步system db的信息
pub async fn query_all_db_refnos(pool: &Pool<MySql>) → anyhow::Result<Vec<RefU64>>  :98-114
pub async fn get_pdms_tree_user(elements: Vec<PdmsElement>, aios_mgr: &AiosDBManager) → Vec<PdmsElementWithUser>  :116-138
```

### src/api/attr.rs
```
impl AiosDBManager  :32-88
  pub async fn get_geo_width(&self, refnos: &[RefU64]) → anyhow::Result<IndexMap<Ref...  :35-35  # 获得集几何体的宽度
pub async fn query_implicit_attrs_by_owner(owner: RefU64, type_name: &str, pool: &Pool<MySql>, column_names: Option<Vec<&str>>) → anyhow::Result<Vec<AttrMap>>  :91-104  # 指定从特定的表查询数据，根据owner查询
pub fn convert_row_to_attmap(row: &MySqlRow, type_hash: i32, column_names: &[&str]) → anyhow::Result<AttrMap>  :107-192
pub async fn query_implicit_attr(refno: RefU64, ref_basic: &CachedRefBasic, pool: &Pool<MySql>, column_names: Option<Vec<&str>>) → anyhow::Result<AttrMap>  :195-235  # 获得隐式属性
pub async fn query_foreign_refnos_from_table(noun: &str, table_name: &str, pool: &Pool<MySql>) → anyhow::Result<Vec<(RefU64,...  :238-256  # 查找整张表的 外键 refno 返回自身 refno + foreign refno
pub async fn query_explicit_attr(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<AttrMap>  :258-263
pub async fn query_uda_attr(att_type: Vec<i32>, pool: &Pool<MySql>) → anyhow::Result<AttrMap>  :266-280  # 查找该类型对应的所有 uda
pub async fn query_refno_uda_value(refno: RefU64, uda_name: &str, pool: &Pool<MySql>) → anyhow::Result<Option<AttrV...  :283-291  # 查询该参考号某个uda的值
pub async fn query_attr(refno: RefU64, aios_mgr: &AiosDBManager, column_names: Option<Vec<&str>>) → anyhow::Result<AttrMap>  :293-321
pub async fn insert_attr_info(pool: Pool<MySql>) → anyhow::Result<()>  :323-335
pub async fn query_position_from_id(refno: RefU64, aios_mgr: &AiosDBManager) → anyhow::Result<Option<Vec3>>  :337-354
pub async fn query_ori_from_id(refno: RefU64, table_name: &str, pool: &Pool<MySql>) → anyhow::Result<Option<Quat>>  :356-369
pub async fn query_foreign_refno(refno: RefU64, foreign_type: &str, pool: &Pool<MySql>) → anyhow::Result<Option<RefU64>>  :371-381
pub async fn query_numbdbs_by_mdb(dbs: RefU64Vec, pool: &Pool<MySql>) → anyhow::Result<Vec<u32>>  :383-394
pub fn gen_query_implicit_attr_sql(refno: RefU64, table_name: &str, columns: &[&str]) → String  :410-419
pub fn gen_query_implicit_attr_sql_by_owner(owner: RefU64, type_name: &str, columns: &Option<Vec<&str>>) → String  :423-431  # 生成通过owner获取的sql语句
pub async fn get_site_major_from_uda(site_refno: RefU64, pool: &Pool<MySql>) → Option<UdaMajorType>  :434-442  # 获取site属于哪个专业
pub async fn query_uda_ukey_udna_all(pool: &Pool<MySql>) → anyhow::Result<HashMap<u32,...  :445-455  # 获取该项目对应的所有的 uda name ，并过滤调 udna 为空的情况
pub async fn query_uda_ukey_udet_all(pool: &Pool<MySql>) → anyhow::Result<HashMap<u32,...  :458-468  # 查找所有的自定义类型的udna以及ukey
pub async fn query_uda_ukey(uda: &str, pool: &Pool<MySql>) → anyhow::Result<i32>  :471-476  # 查找某个uda对应的ukey
pub fn gen_query_explicit_attr_sql(refno: RefU64) → String  :478-482
pub fn gen_query_uda_attr_sql(att_types: Vec<i32>) → String  :484-496
```

### src/api/children.rs
```
pub struct SimpleNodeDataForPlat  :116-120
impl AiosDBManager  :225-316
  pub fn get_ancestor_refno_of_type_data(&self, mut refno: RefU64, att_type: &str) → anyhow::Result<RefU64>  :226-226
  pub fn get_ancestor_refno_till_type(&self, mut refno: RefU64, att_types: &[&str]) → Option<RefU64>  :238-238  # 过滤父节点
  pub fn traverse_ancestor(&self, mut refno: RefU64, func: impl Fn(RefU64) → bool) -> Option<RefU64>  :251-251
  pub async fn traverse_foreign(&self, mut refno: RefU64, foreigns: &[&str], func: impl Fn(RefU64) → bool) -> Option<RefU64>  :264-264
  pub async fn query_children_eles_order(&self, refno: RefU64, filter: &[&str], db_types: &[&str],) → anyhow::Result<Vec<PdmsElem...  :283-288  # 按照顺序返回子节点的PdmsElement数据
pub async fn travel_children_eles(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Vec<RefU64>>  :30-44  # 遍历该节点下的 children (包含自己)
pub async fn travel_children_without_leaf(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Vec<RefU64>>  :48-64  # 遍历该节点的children，不包含叶子节点
pub async fn travel_children_for_elenode(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Vec<EleTreeN...  :67-89  # 遍历该节点下的 children (不包含自己) 返回 EleTreeNode
pub async fn travel_children_for_elenode_without_children_count(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Vec<EleTreeN...  :91-113
pub async fn travel_children_with_type(refno: RefU64, att_type: String, pool: &Pool<MySql>) → anyhow::Result<Vec<EleTreeN...  :123-140  # 遍历该节点的所有子节点为指定type的所有数据 返回 refno name owner
pub async fn travel_children_with_refno(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Vec<RefU64>>  :144-147  # 遍历该节点的所有子节点为指定refno返回 refno
pub async fn query_children_id_name_with_type(pool: &Pool<MySql>, refno: RefU64, att_type: &str) → anyhow::Result<Vec<(RefU64,...  :150-160  # 查询指定type的children 的 （ID，name）的集合
pub async fn fuzzy_query_refnos_by_name(att_type: String, name: String, pool: &Pool<MySql>) → anyhow::Result<Vec<(RefU64,...  :163-174  # 模糊查询 类型为 att_type ，name 中包含指定值 的所有 refno和 name
pub async fn fuzzy_query_refnos_by_name_limit(name: String, numbdbs: &BTreeSet<i32>, pool: &Pool<MySql>) → anyhow::Result<Vec<(RefU64,...  :176-186
pub async fn query_numbdb_from_refnos(refnos: Vec<RefU64>, pool: &Pool<MySql>) → anyhow::Result<Vec<i32>>  :189-206  # 获取参考号集合属于哪些 numbdb
pub async fn query_db_num_by_refno(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<i32>  :209-213  # 获取参考号属于那个dbnum
pub async fn query_owner_type_from_id(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Option<(RefU...  :216-223  # 通过 refno 获取 owner 和 owner 的type
pub async fn query_ancestor_of_type(mut refno: RefU64, att_type: &str, pool: &Pool<MySql>) → anyhow::Result<Option<RefU64>>  :319-327
pub fn query_ancestor_of_type_from_cache(refno: RefU64, att_type: &str) → Option<(RefU64, String)>  :330-342  # 通过 ref_basic 缓存来查找某个节点得指定类型 祖先节点 的参考号
pub async fn query_ancestor_refnos_till_type(mut refno: RefU64, att_type: &str, pool: &Pool<MySql>) → anyhow::Result<Vec<RefU64>>  :344-355
pub async fn query_ancestor_refnos_till_type_aql(database: &ArDatabase, mut refno: RefU64, att_type: &str) → anyhow::Result<Vec<RefU64>>  :357-369
pub async fn query_children_contains_types(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Option<Vec<S...  :372-380  # 获取children有那些tpe
pub async fn query_owner_till_type(mut refno: RefU64, types: Vec<String>, pool: &Pool<MySql>) → anyhow::Result<RefU64>  :383-391  # 查找他的owner直到包含传进来的Vec<att_type>
```

### src/api/data_state.rs
```
pub async fn query_refnos_state(refno: RefU64, pool: &Pool<MySql>, arango_database: &ArDatabase) → anyhow::Result<DataStateVec>  :14-41  # 查找该节点下的所有子节点的data_state数据
pub async fn query_refnos_scope(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<DataScopeVec>  :43-69
pub async fn insert_refnos_state(vals: DataScopeVec, state: String, pool: &Pool<MySql>) → anyhow::Result<()>  :72-83  # 将设定的state值插入到数据库中
```

### src/api/dbno_sql.rs
```
pub async fn query_dbno_count(dbno: i32, pool: &Pool<MySql>, project: &str) → anyhow::Result<i32>  :6-10
```

### src/api/dbno_version.rs
```
pub async fn query_dbno_version(dbno:i32,pool:&Pool<MySql>) → anyhow::Result<Option<i32>>  :5-19
```

### src/api/element.rs
```
pub fn gen_pdms_element_insert_sql(att: &NamedAttrMap, dbno: i32, children_map: &HashMap<RefU64, Vec<RefU64>>,) → String  :9-40  # 生成树结构的sql
pub fn cal_default_name(refno: RefU64, attr: &NamedAttrMap, children_map: &HashMap<RefU64, Vec<RefU64>>,) → String  :43-63  # 如果名称未给定，根据属性列表和children列表获得当前的元素的名称
pub fn get_order(refno: RefU64, attr: &NamedAttrMap, children_map: &HashMap<RefU64, Vec<RefU64>>,) → usize  :67-80  # 获得顺序值
```

### src/api/increment.rs
```
pub async fn query_latest_data(version: u32, pool: &Pool<MySql>) → anyhow::Result<Vec<Incremen...  :12-39
```

### src/api/metadata_manage.rs
```
pub async fn query_metadata_tree_root(pool: &Pool<MySql>) → anyhow::Result<Option<Metad...  :10-33  # 找到元数据管理树结构的根节点
pub async fn query_metadata_tree_children(id: u64, pool: &Pool<MySql>) → anyhow::Result<Vec<Metadata...  :35-62
pub async fn query_metadata_tree_node_data(id: u64, pool: &Pool<MySql>) → anyhow::Result<MetadataMana...  :64-96
pub async fn query_metadata_table_sql(id: u64, pool: &Pool<MySql>) → anyhow::Result<Vec<Metadata...  :98-140
pub async fn query_metadata_table_code_sql(id: u64, pool: &Pool<MySql>) → anyhow::Result<Vec<String>>  :142-153
pub async fn query_tree_node_detail(id: u64, pool: Pool<MySql>) → anyhow::Result<Option<Metad...  :155-164
```

### src/api/pdms_user.rs
```
pub async fn query_b_existence_pdms_user(user_name:&str,pool:&Pool<MySql>) → anyhow::Result<bool>  :9-17  # 传入一个用户名，判断是否存在与 pdms 的用户中
pub async fn query_all_pdms_user(pool:&Pool<MySql>) → anyhow::Result<DashSet<Stri...  :19-30
```

### src/api/profession_form.rs
```
pub async fn create_and_insert_profession_table(map: Vec<HashMap<String, String>>, form_name: &str, pool: &Pool<MySql>) → anyhow::Result<()>  :8-21
pub fn gen_create_profession_table_sql(map: &HashMap<String, String>, form_name: &str) → String  :23-32
pub async fn gen_insert_profession_to_db_sql(map: Vec<HashMap<String, String>>, form_name: &str, pool: &Pool<MySql>) → anyhow::Result<()>  :34-71
pub async fn select_profession_form_data_page(table: &str, page: u32, page_count: u32, pool: &Pool<MySql>) → anyhow::Result<Vec<HashMap<...  :73-94
```

### src/api/project_mdb.rs
```
pub async fn query_db_quick_info(mdb: &str, module: &str, pool: &Pool<MySql>) → anyhow::Result<Vec<DbQuickI...  :25-42
pub async fn query_world_refnos(mdb: &str, module: &str, pool: &Pool<MySql>) → anyhow::Result<Vec<RefU64>>  :45-55
pub async fn query_db_nums_of_mdb(mdb: &str, module: &str, pool: &Pool<MySql>) → anyhow::Result<Vec<i32>>  :58-68  # 查询 mdb 和module 包含了哪些 numdb
pub async fn query_if_contains_mdb(mdb: &str, module: &str, pool: &Pool<MySql>) → anyhow::Result<bool>  :70-75
pub fn gen_insert_project_mdb_sql(map: &MdbQuickInfoMap) → String  :77-94
```

### src/api/refno_info.rs
```
pub async fn get_ref0_projects(pool: &Pool<MySql>) → anyhow::Result<DashMap<u32,...  :24-43  # 更新获得ref0->projects 缓存
pub async fn sync_refno_basic_map(pool: &Pool<MySql>) → anyhow::Result<bool>  :76-101  # 获取生成refno到RefBasic的映射, todo 存储有点慢，需要批量存储
pub async fn query_refno_height_position(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<String>  :105-114  # 通过uda，获取设备的底标高
```

### src/api/room_code.rs
```
pub async fn query_room_code(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Option<String>>  :9-20
pub async fn query_room_code_with_refnos(refnos: Vec<RefU64>, pool: &Pool<MySql>) → anyhow::Result<Vec<(RefU64,...  :23-40
pub async fn query_room_nodes(dbno: &Vec<i32>, pool: &Pool<MySql>) → anyhow::Result<Vec<PdmsElem...  :43-53
```

### src/api/ssc_data.rs
```
pub struct SscEleNode  :38-44
pub async fn get_room_refnos_from_spa_tree_aql(room_refno: RefU64, database: &ArDatabase) → anyhow::Result<Vec<RefU64>>  :47-76  # 通过指定包围盒计算包围盒中的房间的所有节点
pub async fn query_all_room_data(pool: &Pool<MySql>) → anyhow::Result<HashMap<RefU...  :79-107  # 获取所有带有房间号的节点属性
pub async fn query_all_room_data_aql(database: &ArDatabase, pool: &Pool<MySql>, db_option: &DbOption) → anyhow::Result<HashMap<RefU...  :109-148
pub async fn query_ssc_children(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Vec<EleTreeN...  :150-176
pub async fn query_ssc_children_without_children_count(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Vec<EleTreeN...  :178-203
pub async fn query_ssc_world(pool: &Pool<MySql>) → anyhow::Result<Option<EleTr...  :205-227
pub async fn query_ssc_owner(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Option<RefU64>>  :230-243  # 查找ssc的owner
pub async fn query_ssc_type(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Option<String>>  :246-259  # 查找ssc的type
pub async fn query_ssc_children_contains_types(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Option<Vec<S...  :263-271  # 获取children有那些tpe
pub async fn query_ssc_children_count(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<usize>  :273-277
pub async fn travel_ssc_children(refno: RefU64, pool: &Pool<MySql>) → anyhow::Result<Vec<RefU64>>  :280-294  # 遍历该ssc节点的所有子节点
pub async fn get_ancestor_till_type(mut refno: RefU64, att_type: Option<&str>, pool: &Pool<MySql>) → anyhow::Result<Option<RefU64>>  :296-323
pub async fn update_ssc_type(names: Vec<String>, pool: &Pool<MySql>) → anyhow::Result<()>  :325-341
```

### src/api/test_sample.rs
```
pub async fn get_test_sample_pool() → Pool<MySql>  :11-15
pub async fn get_test_info_pool() → Pool<MySql>  :17-21
```

### src/api/three_dimensional_review_data.rs
```
pub async fn save_three_dimensional_review_data_to_arango(database: &ArDatabase, review_data: ThreeDimensionalModelDataCrate,) → anyhow::Result<()>  :9-16
pub async fn save_threed_review_data_to_arango(database: &ArDatabase, review_data: ThreeDimensionalModelDataCrate,) → anyhow::Result<()>  :19-26
pub async fn query_three_dimensional_review_data(database: &ArDatabase, key_value: &str) → anyhow::Result<Option<Vec<T...  :44-49
pub async fn query_threed_review_data(database: &ArDatabase, key_value: &str) → anyhow::Result<Option<Vec<T...  :52-58
pub async fn query_threed_review_data_by_name(database: &ArDatabase, name: &str) → anyhow::Result<Option<Vec<T...  :61-69
```

### src/api/virtual_hole.rs
```
pub async fn query_virtual_hole_data(database: &ArDatabase, key_value: &str) → anyhow::Result<Option<Vec<S...  :7-14
pub async fn query_all_virtual_hole_audit_data(database: &ArDatabase) → anyhow::Result<Option<Vec<S...  :27-34
pub async fn query_hole_detail_data_by_code(database: &ArDatabase, key: &str) → anyhow::Result<Option<Vec<V...  :37-43
pub async fn query_embed_detail_data_by_code(database: &ArDatabase, key: &str) → anyhow::Result<Option<Vec<V...  :45-51
pub async fn query_hole_data_status_by_key(database: &ArDatabase, key: &str) → anyhow::Result<Option<Vec<S...  :54-60  # 返回该孔洞处于哪个校核阶段
pub async fn query_embed_data_status_by_key(database: &ArDatabase, key: &str) → anyhow::Result<Option<Vec<S...  :63-69  # 返回该埋件处于哪个校核阶段
```

### src/data_interface/batch_queue.rs
```
pub struct DataBatch  :19-30  # 一个 dbnum 在一个会话号区间上的一次数据应用——词表里的「数据批次」。
pub enum BatchState  :12-15  # 队列项的两种活着的状态。终态不留在队列里，它们进任务注册表的历史。
pub enum Enqueued  :34-43  # 一次入队的落点。调用方拿它写日志或发事件，不必自己再判一遍。
pub fn enqueue(queue: &mut Vec<DataBatch>, dbnum: u32, db_type: &str, applied_sesno: i32, file_latest_sesno: i32,) → Enqueued  :66-104  # 把一次「dbnum 的文件会话号到了 `file_latest_sesno`」的发现放进队列。
pub fn freeze_next(queue: &mut [DataBatch], paused: bool) → Option<usize>  :113-120  # FIFO 出队并冻结：取最早入队的那条排队项，转 `Running`，返回它的下标。
```

### src/data_interface/batch_scheduler.rs
```
pub struct DiscoveredBatch  :25-36  # 一次发现（文件会话号超过水位）携带的全部入队信息。
pub struct FrozenBatch  :50-59  # 冻结出队后交给 worker 的一份快照。
pub struct EnqueuedBatchInfo  :63-71  # 入队回执的一行（HTTP 202 与日志共用；rollout 第九节第 7 条）。
pub struct EnqueueOutcome  :75-78  # 一次入队的结果：落点 + 对应任务行。
pub struct ManualEnqueueReceipt  :82-106  # 手动触发「扫描 + 入队”的整体回执（POST /update/execute 的 202 响应体）。
pub struct BlockedDbnum  :109-112
pub struct QueueRow  :116-123  # 队列快照的一行（含运行中的），供面板/日志。
pub struct BatchScheduler  :125-134
impl BatchScheduler  :144-280
  pub fn global() → &'static BatchScheduler  :145-145
  pub fn enqueue(&self, registry: &TaskRegistry, found: &DiscoveredBatch) → EnqueueOutcome  :170-170  # 把一次发现放进队列，并让注册表跟上（新行 / 并入 / 无动作）。
  pub fn freeze_next(&self, registry: &TaskRegistry) → Option<FrozenBatch>  :279-279  # FIFO 出队并冻结（暂停时恒 None）。注册表行随之转 running。
```

### src/data_interface/cata_closure.rs
```
pub struct InMemoryCataLocator  :68-71  # 就地内存定位器：`ref0→dbnum` + `dbnum→(db_type, project, path)`。
pub struct ParsedCataEle  :461-472  # 部分解析得到的单个 CATA 元素（闭包扩展所需的最小信息）。
pub struct CataClosureConfig  :584-601  # 闭包行为配置。
pub struct CataClosureManifest  :655-666  # 闭包结果：每个 CATA dbnum 需解析的 refno 集合 + 统计。
pub struct CataClosureResolver  :671-682  # refno 级 CATA 引用闭包引擎（BFS）。
pub struct LazyFallbackOutcome  :1002-1005  # 惰性兜底结果统计。
pub struct GeoSmokeReport  :1188-1198  # 单根几何冒烟报告：对一组设计参考号逐个 `resolve_desi_comp` 算确定性摘要。
pub trait CataDbLocator  :46-53  # 把一个引用（`RefU64`）定位到它所属的 db —— 闭包引擎唯一的外部依赖。
impl InMemoryCataLocator  :76-243
  pub fn from_parts(ref0_to_dbnum: HashMap<u32, u32>, dbnum_files: HashMap<u32, (String, String, PathBuf) → Self  :78-81  # 直接注入（单测 / 自定义来源）。`dbnum_files`：dbnum -> (db_type, project, p
  pub fn ref0_count(&self) → usize  :102-102  # 已登记的 `ref0` 数（诊断用）。
  pub fn dbnum_count(&self) → usize  :107-107  # 已登记的 dbnum 数（诊断用）。
  pub async fn build_for_project(project: &str) → anyhow::Result<Self>  :114-114  # 端到端构建：读 `dbnum_watermark` 得各库文件身份 → 扫描各库 `ref0` 集（带磁盘指纹缓存）。
  pub fn build_from_dir(project: &str, root_dir: &Path) → Self  :214-214  # 目录扫描构建（无需 SurrealDB）：遍历工程目录下全部 db 文件，建
impl InMemoryCataLocator  :272-286
impl Ref0IndexCache  :425-453
impl CataClosureConfig  :603-616
impl CataClosureConfig  :618-651
  pub fn precise() → Self  :624-624  # 精确模式（refno 级按需 / 惰性小闭包）：children 仅对几何与点集容器展开
  pub fn excluding_dbnum(mut self, dbnum: u32) → Self  :642-642
  pub fn excluding_dbnums(mut self, dbnums: impl IntoIterator<Item = u32>) → Self  :647-647
impl CataClosureResolver  :684-821
  pub fn new(locator: &'a L, cfg: CataClosureConfig) → Self  :685-685
  pub fn with_retain_attmaps(mut self, retain: bool) → Self  :698-698  # 开启属性表保留（小闭包惰性兜底场景；大闭包慎用，内存随 visited 线性增长）。
  pub fn take_attmaps(&mut self) → HashMap<RefU64, (NamedAttrM...  :704-704  # 取走保留的属性表（`retain_attmaps` 开启时在 `resolve()` 后调用）。
```

### src/data_interface/db_manager.rs
```
impl AiosDBManager  :35-59
  pub async fn get_gen_model_root_refnos(&self, db_nos: &[i32]) → anyhow::Result<Vec<RefU64>>  :37-37  # 获得db number 对应的site参考号
  pub async fn get_gen_model_target_refnos(&self, geo_type: GeoEnum, db_nos: &[i32], is_parent: bool,) → anyhow::Result<Vec<RefU64>>  :51-56  # 获取待调试或者整个db的参考号集合
```

### src/data_interface/db_model.rs
```
impl AiosDBManager  :67-227
  pub async fn init_form_config() → anyhow::Result<Self>  :69-69  # 从默认配置文件初始化
  pub async fn exec_watcher(mgr: Arc<AiosDBManager>) → anyhow::Result<()>  :81-81  # 看门狗的失败必须向上传播（T903）：过去这里 `
  pub async fn run_e3d_clone_bg_task(mgr: Arc<AiosDBManager>) → anyhow::Result<()>  :89-89
  pub async fn exec_delta_clone_remotes(watcher: &PdmsWatcher, sync_msg: SyncE3dFileMsg,) → anyhow::Result<bool>  :104-107
  pub async fn spawn_exec_watcher(mgr: Arc<AiosDBManager>) → anyhow::Result<()>  :158-158
  pub async fn demo_mqtt_requests()  :175-175
  pub async fn poll_sync_e3d_mqtt_events(watcher: Arc<PdmsWatcher>)  :209-209  # 另外将里面可能有关联的db，也要同步检查后一下？？
```

### src/data_interface/generation_root.rs
```
pub struct GenerationNode  :43-47
pub struct GenerationRoot  :50-55
pub enum GenerationRootKind  :36-40
pub fn is_coarse_hierarchy_noun(noun: &str) → bool  :57-60
pub fn is_delivery_unit_noun(noun: &str, unit_types: &[String]) → bool  :62-68
pub fn resolve_delivery_unit_types(appended: &[String]) → Vec<String>  :100-102  # Defaults ∪ configured additions
pub fn resolve_delivery_unit_types_from_config(configured: Option<&[String]>, appended: &[String],) → Vec<String>  :111-131  # Resolve the effective delivery-unit type set from project co
pub fn configured_delivery_unit_types() → Vec<String>  :137-150  # Effective delivery-unit types for the running project
pub fn resolve_element_generation_root(refno: RefnoEnum, unit_types: &[String], lookup: impl FnMut(RefnoEnum) → Option<GenerationNode>, ) -...  :207-234  # Resolve a changed element:
pub fn resolve_owner_generation_root(owner: RefnoEnum, unit_types: &[String], lookup: impl FnMut(RefnoEnum) → Option<GenerationNode>, ) -...  :239-257  # Resolve an old/new OWNER reference
pub async fn resolve_live_element_generation_root(refno: RefnoEnum, unit_types: &[String],) → anyhow::Result<Option<Gener...  :289-304
pub async fn resolve_live_owner_generation_root(owner: RefnoEnum, unit_types: &[String],) → anyhow::Result<Option<Gener...  :306-321
```

### src/data_interface/helper.rs
```
pub async fn delete_inst_relate_cascade(refnos: &[RefnoEnum], chunk_size: usize,) → anyhow::Result<()>  :99-121  # 级联删除 inst_relate 及其关联的 geo_relate 和 inst_geo 数据
pub async fn delete_inst_relate_subtree(refnos: &[RefnoEnum], chunk_size: usize,) → anyhow::Result<()>  :134-151  # 收集给定 refno 及其**子树（含已软删节点）**的全部节点，级联删除它们的
```

### src/data_interface/increment_pipeline.rs
```
pub struct IncrFileSuccess  :29-47  # One file that completed Surreal persist + watermark advance
pub struct IncrFileError  :51-54  # One file that failed before watermark advance
pub struct IncrResult  :58-63  # Result of [`IncrementPipeline::apply`]
pub struct IncrementPipeline  :360-360  # Independent deep module: collect delta → Surreal persist → d
impl IncrResult  :65-85
  pub fn all_changed_refnos(&self) → Vec<RefU64>  :66-66
  pub fn geometry_changed_refnos(&self) → Vec<RefU64>  :74-74  # Refnos from successes that are not SYS meta DBs (eligible fo
  pub fn had_work(&self) → bool  :82-82
impl StageTimings  :334-356
impl IncrementPipeline  :422-583
  pub fn new() → Self  :423-423
  pub fn collect_changes(path: &std::path::Path, sesno_range: RangeInclusive<i32>,) → anyhow::Result<BTreeMap<u32...  :433-436  # Side-effect-free change collection for one file over a sesno
  pub async fn apply(&self, increment_ranges_map: IndexMap<PathBuf, (DbPageBasicInfo, RangeInclusive<i32>, String) → IncrResult  :452-455  # Apply incremental updates for the given sesno ranges
  pub async fn apply_with_precollected(&self, increment_ranges_map: IndexMap<PathBuf, (DbPageBasicInfo, RangeInclusive<i32>, String) → IncrResult  :470-477  # 同 [`Self::apply`]，但允许调用方交出**已经收集好**的增量窗口，避免同一个文件
pub async fn selfcheck_surreal_functions()  :403-420  # 启动自检：收口事务依赖的 SurrealDB 自定义函数在不在。
```

### src/data_interface/interface.rs
```
pub trait PdmsDataInterface  :23-23  # 只读数据访问面。
```

### src/data_interface/mesh_manager.rs
```
impl AiosDBManager  :9-86
  pub async fn cache_plant_meshes(&self, geo_hashes: impl IntoIterator<Item = &u64>, overwrite: bool,) → anyhow::Result<bool>  :11-15  # 拉取geo_hashs对应的meshes
  pub async fn get_plant_mesh(&self, geo_hash: u64) → anyhow::Result<Option<Plant...  :38-38  # 获取geo_hash 对应的plant mesh数据
  pub async fn get_transformed_plant_mesh(&self, geo_hash: u64, t: &Transform,) → anyhow::Result<Option<Plant...  :52-56  # 获得变换后的mesh
  pub async fn get_transformed_mesh_elev(&self, geo_hash: u64, t: &Transform,) → anyhow::Result<Option<(f32,...  :63-67  # 获得变换后的mesh的标高
```

### src/data_interface/model_impact.rs
```
pub struct OperationEffectSummary  :73-88  # 一次操作的「效果汇总」：动作标志 + 观察到的效果集合 + 保留的原始 DCHC code + 变更属性名。
pub enum AttributeModelImpact  :27-31  # 属性对模型生成输入的影响（三态，保留作向后兼容 / 粗判）。
pub enum AttributeEffect  :37-50  # 更细的「效果分类」——把 bool/三态升级为 effect（对齐 core
pub enum OperationImpact  :62-69  # 一次操作对模型的处理动作。
pub enum ChangeBucket  :489-503  # core
impl AttributeEffect  :52-58
  pub fn affects_model(self) → bool  :55-55  # 是否影响模型（需重生成或变换更新）。仅 `DataOnly` 为否。
impl OperationEffectSummary  :90-101
  pub fn impact(&self) → OperationImpact  :92-92  # 归约为调度用的三态动作。
impl ChangeBucket  :505-515
pub fn normalize_attribute_name(raw_name: &str) → String  :192-201  # 归一化属性名：去掉 `att
pub fn classify_attribute_effect(raw_name: &str) → AttributeEffect  :204-214  # 细粒度效果分类：查 [`ATTRIBUTE_EFFECT_TABLES`] 合并出的映射，未命中则 `Unknown`。
pub fn classify_attribute_effect_with_meta(raw_name: &str, is_reference: bool) → AttributeEffect  :225-232  # 带 att_meta 提示的效果分类（A2 · ELEMENT 自动级联）。
pub fn attribute_is_reference(raw_name: &str) → bool  :254-256  # 某属性名是否为**引用类型**（schema `att_type == ELEMENT`）——A2 ELEMENT 自动
pub fn classify_attribute_model_impact(raw_name: &str) → AttributeModelImpact  :259-265  # 三态分类（向后兼容）：由细粒度效果归约——DataOnly→KnownNeutral、Unknown→Unknown、其
pub fn raw_dchc_code(raw_name: &str) → Option<i32>  :273-279  # 保留的 Core3D 原始设计变化码（DCHC，字段 596407）。
pub fn attribute_affects_model(raw_name: &str) → bool  :296-301  # 属性是否命中「已知会影响模型」的分类——除 `DataOnly` 与 `Unknown` 之外的一切。
pub fn classify_operation_effects(op: &EleOperationData) → OperationEffectSummary  :336-399  # 汇总一次操作的效果（宁多勿漏），并保留原始 DCHC code。
pub fn classify_operation_impact(op: &EleOperationData) → OperationImpact  :402-404  # 判定一次操作应如何处理模型（宁多勿漏）：由 [`classify_operation_effects`] 归约为三态动作
pub fn is_loop_container_noun(noun: &str) → bool  :408-420  # 是否为 loop/坐标辅助类型（LOOP/PLOO/VERT/PAVE、*DATU 等）：这些自身不是
pub fn named_attr_refno(value: &NamedAttrValue) → Option<RefnoEnum>  :422-429
pub fn owner_change(op: &EleOperationData) → (Option<RefnoEnum>, Option<...  :437-472  # Extract the OLD and NEW OWNER references from a modify op (e
pub fn changed_owner_refnos(op: &EleOperationData) → Vec<RefnoEnum>  :475-478  # 提取一次修改里 OWNER 属性的新旧引用（元素被搬迁时，新旧 owner 两侧都需重生成）。
pub fn added_owner(op: &EleOperationData) → Option<RefnoEnum>  :522-532  # 一个 `Add` 操作里新元素的 owner（G2：新建元素时其 owner 必须记 MemberChanged，
pub fn user_change_buckets(op: &EleOperationData) → Vec<(ChangeBucket, RefnoEnum)>  :555-598  # 一次元素操作产生的 `(桶, refno)` 分配，忠实于 core
```

### src/data_interface/model_refresh.rs
```
pub struct ModelRefreshPolicy  :30-30  # Deep module: one interface, swappable refresh adapters
impl ModelRefreshPolicy  :32-119
  pub async fn cleanup_deleted_by_pe_state(refnos: &[RefU64]) → anyhow::Result<()>  :93-93  # F1/F3（T304）补偿路径专用：补偿只有 `changed_refnos`（不含操作类型），故按**当前
```

### src/data_interface/on_demand_model.rs
```
pub struct OnDemandModelResult  :33-44
pub struct ModelGenerationInProgress  :98-100
pub enum OnDemandModelStatus  :23-30
pub enum UnresolvableRoot  :222-229  # 解析不出生成根的两种缘由。调用方（HTTP 层）据此分型：客户端对着容器该做的事
impl AiosDBManager  :46-95
  pub async fn ensure_model_generated(&self, requested_refno: RefnoEnum, force: bool,) → anyhow::Result<OnDemandMode...  :55-59  # Ensure that `requested_refno` has a renderable model
impl ModelGenerationInProgress  :102-106
impl ModelGenerationInProgress  :108-108
impl UnresolvableRoot  :231-241
impl UnresolvableRoot  :243-243
```

### src/data_interface/project_paths.rs
```
pub struct ProjectWatchDirs  :151-157  # 单个项目的解析结论。
pub struct WatchDirPlan  :161-163  # 全部项目的解析结论。空列表本身就是一种故障，所以连「为什么空」一起带出来。
pub struct MountState  :315-328  # 已经挂到文件监控器上的目录集合。
impl WatchDirPlan  :165-219
  pub fn dirs(&self) → Vec<PathBuf>  :166-166
  pub fn is_empty(&self) → bool  :173-173
  pub fn problems(&self) → Vec<String>  :178-178  # 逐项目的失败原因，一行一条。
  pub fn describe(&self) → String  :194-194  # 启动时打给人看的一段说明：每个项目用了哪个根、找到哪些库目录、失败在哪。
impl MountState  :330-419
  pub fn new() → Self  :331-331
  pub fn len(&self) → usize  :335-335
  pub fn is_empty(&self) → bool  :339-339
  pub fn contains(&self, dir: &Path) → bool  :343-343
  pub fn mount(&mut self, watcher: &mut W, dirs: &[PathBuf]) → Vec<String>  :348-348  # 挂载还没挂上的目录，返回逐目录的失败原因（已挂上的静默跳过）。
  pub fn missing(&self, dirs: impl IntoIterator<Item = PathBuf>) → Vec<PathBuf>  :371-371  # `dirs` 里还没挂上的那些。
  pub fn refresh_health(&mut self, dirs: &[PathBuf]) → Vec<PathBuf>  :382-382  # 复查已挂目录还在不在，把不可达的降级为「失联」并从已挂集合里摘掉。
  pub fn unwatch_lost(&mut self, watcher: &mut W) → usize  :402-402  # 失联目录恢复之前必须先 `unwatch`，再走正常的 [`Self::mount`]。
pub fn normalize_path_input(raw: &str) → PathBuf  :34-41  # 把配置里写的路径归一成本平台能用的形式。
pub fn is_absolute_input(raw: &str) → bool  :45-52  # 这个配置值是不是一个「自带完整位置」的路径（绝对路径 / UNC），而不是要拼到
pub fn resolve_project_root(db_option: &DbOption, project: &str) → Option<PathBuf>  :60-81  # 解析某个项目的根目录，与 [`DbOption::get_project_path`] 同语义，但多认两件事：
pub fn collect_db_dirs_in(root: &Path) → io::Result<Vec<PathBuf>>  :119-147  # 列出这个项目根下所有的 `*000` 库目录。
pub fn plan_watch_dirs(db_option: &DbOption) → WatchDirPlan  :224-276  # 按配置解析出全部监控目录。
pub fn path_identity(path: &Path) → String  :288-291  # 去重用的 key：先尽量落到真实路径上（吃掉 `
pub fn record_watch_dir_owners(plan: &WatchDirPlan)  :438-453  # 把一次解析结果里的「目录 → 项目」登记下来。
pub fn project_of_path(file: &Path) → Option<String>  :456-462  # 这个文件（或目录）落在哪个项目下。
```

### src/data_interface/sesno_range.rs
```
pub struct SesnoUpdatePlan  :26-36  # Resolved incremental window for one DB file
pub struct SesnoRangeResolver  :40-40  # Independent module: watermark (dbnum) + nearest sesno → opti
impl SesnoRangeResolver  :42-170
  pub fn new() → Self  :43-43
  pub async fn query_watermark(dbnum: u32) → anyhow::Result<u32>  :60-60  # Authoritative watermark for this dbnum
  pub async fn resolve(&self, path: &Path, project: &str, dbnum: u32, file_latest_sesno: i32, skip_cata: bool, db_type: &str,) → anyhow::Result<Option<Sesno...  :69-77  # Build an update plan when `file_latest_sesno > watermark`, o
  pub async fn resolve_with_header(&self, path: &Path, project: &str, basic_info: DbPageBasicInfo, skip_cata: bool, db_type: &str,) → anyhow::Result<Option<Sesno...  :101-108  # Convenience when caller already has `DbPageBasicInfo` (watch
```

### src/data_interface/side_effect_pending.rs
```
pub struct PendingJob  :37-50
pub struct SideEffectCompensator  :54-54  # Independent module: enqueue / complete / drain side-effect j
pub enum SideEffectKind  :21-25
impl SideEffectKind  :27-34
impl SideEffectCompensator  :56-232
  pub async fn enqueue_from_incr(_mgr: &AiosDBManager, incr: &IncrResult) → anyhow::Result<()>  :68-68  # After PE+watermark success: enqueue only legacy non-model si
  pub async fn enqueue_syst(dbnum: u32, end_sesno: i32, db_type: &str) → anyhow::Result<()>  :92-92  # 单个 SYST 批次落库后登记一条派生同步任务（数据批次 worker 用）。
  pub async fn enqueue_ref_rev(dbnum: u32, end_sesno: i32, db_type: &str, referrers: &[RefU64],) → anyhow::Result<()>  :105-110  # 反向引用索引没维护上 → 记一条定点重建任务。
  pub async fn mark_done(kind: SideEffectKind, dbnum: u32, end_sesno: i32) → anyhow::Result<()>  :158-158
  pub async fn mark_failed(kind: SideEffectKind, dbnum: u32, end_sesno: i32, err: &str,) → anyhow::Result<()>  :172-177
  pub async fn drain(mgr: &AiosDBManager) → anyhow::Result<usize>  :214-214  # Replay pending/failed jobs (attempts < MAX_ATTEMPTS)
```

### src/data_interface/spatial_model.rs
```
impl AiosDBManager  :12-42
  pub async fn get_along_end_key_points(&self, refno: RefU64) → Option<(Vec3, Vec3)>  :15-15  # 获得当前参考号下的模型前后方向关键点
```

### src/data_interface/structs.rs
```
pub struct RefnoHasNegPosInfoTuple  :15-15  # 有负实体的集合信息, 返回tuple
pub struct RefnoHasNegInfoTuple  :25-25
pub struct CateBrepShapeData  :46-49
```

### src/data_interface/sync_publisher.rs
```
pub struct SyncOutcome  :18-22  # Outcome of one publish attempt
pub struct SyncPublisher  :26-28  # Independent module: archive + dedup + MQTT notify
impl SyncPublisher  :30-158
  pub fn new(mqtt_client: Arc<AsyncClient>) → Self  :31-31
  pub async fn ensure_archive(path: &PathBuf) → anyhow::Result<String>  :36-36  # Ensure a single `
  pub async fn publish(&self, incr: &IncrResult) → SyncOutcome  :50-50  # After a successful incremental apply: compress each success 
```

### src/data_interface/task_registry.rs
```
pub struct TaskEntry  :60-98
pub struct TaskRegistry  :116-118  # 插入序即时间序的注册表。
pub enum TaskState  :34-40  # 任务状态机：`queued -> running -> succeeded | partial | failed`；冻结
impl TaskState  :42-57
  pub fn as_str(&self) → &'static str  :43-43
  pub fn is_terminal(&self) → bool  :54-54  # 终态才可被容量剔除；queued / running 永不剔除（ADR-011 §11）。
impl TaskRegistry  :131-311
  pub fn global() → &'static TaskRegistry  :134-134  # 进程级单例：worker（feature 无关）与 web_service（`http_api` 门内）
  pub fn new_task_id(prefix: &str) → String  :151-151
  pub fn insert_queued_batch(&self, task_id: &str, project: &str, dbnum: u32, db_type: &str, start_sesno: i32, end_sesno: i32,)  :161-169  # 新排一条数据批次（state = queued）。返回该行 task_id。
  pub fn insert_running_room_round(&self, task_id: &str, project: &str, total: u32, detail: serde_json::Value,)  :193-199  # 新排一条房间收敛轮（房间轮不排队，创建即 running；ADR-011 §10）。
  pub fn update_queued_range(&self, task_id: &str, end_sesno: i32)  :274-274  # 排队中的行被后来的触发并入会话：只推高右端（ADR-011 §5）。
  pub fn set_queued_start(&self, task_id: &str, start_sesno: i32)  :283-283
  pub fn set_frozen_range(&self, task_id: &str, end_sesno: i32)  :297-297  # 冻结点重扫定下真实上界后回写（ADR-011 §5）。
  pub fn mark_started(&self, task_id: &str)  :307-307  # 出队冻结：queued → running，记录开跑时刻。
pub fn process_started_at() → &'static str  :105-107
```

### src/data_interface/tidb_manager.rs
```
pub struct AiosDBManager  :41-60
impl AiosDBManager  :65-69
impl AiosDBManager  :71-333
```

### src/data_interface/update_scope.rs
```
pub struct UpdateScope  :29-37  # 一次扫描的执行范围。
impl UpdateScope  :39-154
  pub async fn resolve(mdb: &str) → anyhow::Result<Self>  :50-50  # 解出 `mdb` 声明的 DESI 库号。
  pub fn unrestricted() → Self  :101-101  # 不设限。按 dbnum 点名的入口用它——回归工具与按需初始化的调用方已经自己
  pub fn warning(&self) → Option<&str>  :111-111  # 范围没解出设计库名单时的那句话，调用方要原样放进自己的 warnings。
  pub fn admits(&self, db_type: &str, dbnum: u32) → bool  :130-130  # 这个库进不进本期执行范围。
  pub fn mdb(&self) → &str  :141-141  # MDB 名（已带前导 `/`）。不设限时是空串。
  pub fn declared_desi(&self) → impl Iterator<Item = u32> + '_  :146-146  # MDB 声明的 DESI 库号，升序。
  pub fn declares(&self, dbnum: u32) → bool  :151-151  # 是否按 dbnum 逐个点名（`unrestricted` 时恒为假：那种模式下没有"声明"可言）。
```

### src/data_state/save_data_state.rs
```
pub async fn save_data_state(data: Vec<RefnoStatusInfo>, pool: &Pool<MySql>) → anyhow::Result<()>  :7-20
```

### src/data_to_excel/export_csv.rs
```
pub fn create_csv_file(header: Vec<String>, values: Vec<Vec<String>>) → Vec<u8>  :5-16  # 生成csv文件
```

### src/data_to_file/create_att.rs
```
pub struct CreateAttrMap  :16-20
pub fn gen_implicit_attr_data(attr: AttrMap) → Vec<u8>  :24-90
pub fn gen_children_data(refno: RefU64, children: Option<Vec<RefU64>>) → Vec<u8>  :115-132  # 生成children
```

### src/data_to_file/increment/data_page.rs
```
pub struct DataPageIncrement  :33-45
impl DataPageIncrement  :47-73
  pub fn convert_new_increment_data_file(self) → Option<Vec<u8>>  :48-48
pub fn convert_new_node_data_explicit(refno: RefU64, default_map: AttrMap, b_f64: bool) → Vec<u8>  :126-257  # 将显式属性转换为 bytes
pub fn convert_new_node_data_implicit(attr_map: BTreeMap<u32, (NounHash, AttrVal) → Vec<u8>  :272-364  # 将隐式属性转换为 bytes
```

### src/data_to_file/mod.rs
```
pub struct NewPage  :31-44  # 修改属性后，该文件的所有数据页
pub struct OldDataPage  :56-63
pub enum PageType  :20-27
impl NewPage  :46-53
  pub fn convert_into_one_page(self) → Vec<u8>  :47-47
impl OldDataPage  :65-76
  pub fn turn_self_into_vec(self) → Vec<u8>  :66-66
  pub fn convert_new_data_page(self) → Vec<u8>  :70-70
pub fn get_last_page_no(input: &[u8]) → u32  :79-81  # 从pdms文件中，获得 pdms 最新的 page_no
pub fn get_page_no(input: &[u8], page_type: PageType) → Option<u32>  :84-98  # 指定page的类型，通过该类型最后出现的位置,返回该类型的page_num
pub fn get_latest_page(input: &[u8], refno: RefU64, page_no: [u8; 12]) → Option<(Vec<u8>, usize)>  :101-113  # 获得最后一个存在该参考号的 page,并返回他开始的position
pub fn get_refno_position_in_page(input: &[u8], refno: RefU64) → Option<usize>  :116-122  # 返回参考号在该数据页中的位置
```

### src/data_to_file/modify/claim_page.rs
```
pub struct ClaimPageModify  :12-21
impl ClaimPageModify  :23-57
  pub fn convert_new_claim_page(self, input: &[u8]) → Option<Vec<u8>>  :25-25  # 生成新的claim_page，需要传入pdms数据文件和修改的参考号
```

### src/data_to_file/modify/data_page.rs
```
pub struct DataPageModify  :10-16
impl DataPageModify  :18-32
  pub fn convert_new_data_page_modify(self, input: &[u8]) → Option<Vec<u8>>  :19-19
pub fn get_latest_data_page(input: &[u8], refno: RefU64, att_type: &str) → Option<OldDataPage>  :35-40  # 根据 refno + type 找到在文件中最新的数据
```

### src/data_to_file/modify/index_page.rs
```
pub struct IndexPage  :13-18
impl IndexPage  :20-66
  pub fn convert_new_index_page(self, input: &[u8]) → Option<Vec<u8>>  :21-21
```

### src/data_to_file/modify/modify.rs
```
pub struct ModifyNewData  :74-79
pub struct ModifyData  :557-569
pub enum GlobalPage  :573-579  # 存放全局的 name_page type_page等
impl ModifyNewData  :81-225
  pub fn get_refno_bytes(&self) → Vec<u8>  :82-82
  pub fn get_refno_and_type_bytes(&self) → Vec<u8>  :86-86
  pub fn get_type_hash_u32(&self) → u32  :93-93
  pub fn get_noun_hash_u32(&self) → u32  :97-97
  pub fn get_noun_hash_vec(&self) → Vec<u8>  :101-101
  pub fn convert_implicit_data_to_vec(&self, b_f64: bool) → Vec<u8>  :135-135
  pub fn convert_explicit_data_to_vec(&self, b_f64: bool) → Vec<u8>  :177-177
impl ModifyData  :581-687
  pub fn convert_new_modify_data(self) → Option<Vec<u8>>  :582-582
pub fn change_origin_file(path: &str) → (u32, Vec<u8>)  :317-326  # 读取原文件，返回新的版本号和原数据
pub fn find_data_in_origin_file(input: &[u8], buf: &[u8]) → Option<OldDataPage>  :329-347  # 传入 refno + type 返回该数据在pdms文件中的位置
pub fn convert_new_data_page(mut page: OldDataPage, data: ModifyNewData, pdms_database_info: &PdmsDatabaseInfo, latest_page_no: u32,) → Option<Vec<u8>>  :350-425  # 将修改的值写入到 DataPage中
pub fn get_origin_children_and_explicit_data(input: &[u8], mut pos: usize) → (Vec<u8>, Vec<u8>)  :456-471  # 获取该节点的 children 或者 显示属性
pub fn convert_first_version_page(input: &[u8], refno: &[u8], version: u32) → Option<Vec<u8>>  :474-489  # 修改第一个refno + version page 的版本号
pub async fn convert_change_times_page(input: &[u8], refno: RefU64, pool: &Pool<MySql>,) → anyhow::Result<Option<(Vec<...  :492-520  # 修改次数页
pub fn convert_conversation_page(input: &[u8], version: u32, change_times: &[u8],) → Option<Vec<u8>>  :523-555  # 会话页
```

### src/data_to_file/modify/name_page.rs
```
pub struct NamePageModify  :12-17
impl NamePageModify  :19-52
  pub fn convert_new_name_page(self, input: &[u8]) → Option<Vec<u8>>  :21-21  # 生成新的name_page，暂时只支持已存在的 name ,且不支持中文
```

### src/data_to_file/modify/session_page.rs
```
pub struct SessionPageModify  :17-32  # 生成 session_page 需要的数据
impl SessionPageModify  :34-91
  pub fn convert_session_page(self, input: &[u8]) → Vec<u8>  :38-38  # 通过旧的session_page数据 ，生成新的session_page
pub fn get_latest_session_page(input: &[u8], last_page_no: u32) → Vec<u8>  :94-97  # 获得修改之前，最新的session_page的0x800的数据
```

### src/defines.rs
```
pub struct RString  :29-29
impl RString  :37-37
```

### src/fast_model/aabb_tree.rs
```
pub fn mark_aabb_tree_dirty()  :20-22  # 增量路径动过内存树之后调用：标记「有变更待落盘」。
pub fn stage_project_aabb_tree_file()  :49-67  # 启动加载空间树**之前**调用：把本项目的树文件放到 rs-core 硬编码的裸路径上。
pub async fn persist_aabb_tree_if_dirty() → anyhow::Result<bool>  :84-97  # 脏则写回 `accel_tree
pub async fn persist_aabb_tree() → anyhow::Result<()>  :102-107  # 无条件写回并清脏标记（全量生成收尾、对账重建后走这里）。
pub async fn sync_aabb_tree_with_db() → anyhow::Result<()>  :132-169  # 用库里的包围盒数量与内存空间树对账，少了就重建。
pub async fn manual_update_aabbs(replace_exist: bool) → anyhow::Result<()>  :184-230  # 手动更新所有 inst_relate 的 AABB 包围盒
```

### src/fast_model/cal_model/bran_model.rs
```
pub async fn update_cal_bran_component() → anyhow::Result<()>  :5-9
```

### src/fast_model/cal_model/equip_model.rs
```
pub async fn update_cal_equip() → anyhow::Result<()>  :9-13
pub async fn update_cal_equip_wtrans() → anyhow::Result<()>  :18-44
pub async fn cal_equip_nearest_floor() → anyhow::Result<()>  :47-91
```

### src/fast_model/cata_model.rs
```
pub enum NgmrRemovedType  :120-131
pub fn init_chrome_tracing() → anyhow::Result<()>  :66-100  # Initializes Chrome tracing for performance analysis
pub async fn gen_cata_single_geoms(design_refno: RefnoEnum, brep_shape_map: &CateBrepShapeMap, design_axis_map: &DashMap<RefnoEnum, PlantAxisMap>,) → anyhow::Result<bool>  :134-259  # 获取单个元件的模型数据
pub fn cal_sjus_value(sjus: &str, height: f32) → f32  :263-272  # 计算对齐偏移值
pub async fn gen_cata_geos(db_option: Arc<DbOption>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :278-411  # 生成元件库的branch型几何体
pub async fn gen_cata_geos_with_tracing(db_option: Arc<DbOption>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, branch_map: Arc<HashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :1236-1290  # Simplified version of gen_cata_geos for tracing analysis
pub async fn gen_cata_geos_with_tracing(db_option: Arc<DbOption>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :1293-1305  # Simplified version of gen_cata_geos for tracing analysis
pub async fn query_ngmr_owner(refno: RefnoEnum, ngmr_geo_refno: RefnoEnum,) → anyhow::Result<Vec<RefnoEnum>>  :1308-1359
pub async fn gen_cata_geos_parallel_optimized(db_option: Arc<DbOption>, target_cata_map: Arc<DashMap<String, CataHashRefnoKV>>, branch_map: Arc<DashMap<RefnoEnum, Vec<SPdmsElement>>>, sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :1369-1500  # P0级优化：并行元件库几何体生成函数
```

### src/fast_model/coverage_audit.rs
```
pub fn audit_enabled() → bool  :38-46  # 观测是否开启。
pub async fn audit_segment(target_refnos: &[RefnoEnum], skip_exist: bool)  :64-123  # 观测一个生成分段：统计差集 noun 在本段子树里的真实命中数。
pub fn report_and_reset()  :126-149  # 打印本次生成的累计观测结果并清空，供下一次生成重新计数。
```

### src/fast_model/gen_model.rs
```
pub struct DbModelInstRefnos  :45-50  # 一个db生成模型里，汇总的参考号集合
impl DbModelInstRefnos  :52-148
  pub async fn execute_gen_inst_meshes(&self, db_option_arc: Option<Arc<DbOption>>,) → anyhow::Result<()>  :53-56
  pub async fn execute_boolean_meshes(&self, db_option_arc: Option<Arc<DbOption>>,) → anyhow::Result<()>  :104-107
pub async fn gen_all_geos_data(db_option: &DbOption) → anyhow::Result<bool>  :160-270  # 生成几何体数据。
pub async fn process_meshes_by_dbnos(dbnos: &[u32], db_option: &DbOption) → anyhow::Result<()>  :322-344  # 更新模型数据
pub async fn gen_geos_data_by_dbnum(dbno: u32, db_option_arc: Arc<DbOption>, sender: flume::Sender<ShapeInstancesData>,) → anyhow::Result<DbModelInstR...  :358-505  # 生成几何体数据
pub async fn gen_geos_data(dbno: Option<u32>, manual_refnos: Vec<RefnoEnum>, db_option: &DbOption, sender: flume::Sender<ShapeInstancesData>,) → anyhow::Result<Vec<RefnoEnum>>  :583-714  # 生成几何体数据
pub async fn query_tubi_size(refno: RefnoEnum, tubi_cat_ref: RefnoEnum, is_hang: bool,) → anyhow::Result<TubiSize>  :858-884  # 查询tubi的大小
```

### src/fast_model/loop_model.rs
```
pub async fn gen_loop_geos(db_option: Arc<DbOption>, loop_owner_refnos: &[RefnoEnum], sjus_map_arc: Arc<DashMap<RefnoEnum, (Vec3, f32) → anyhow::Result<bool>  :23-142  # 处理带有loop的元件
```

### src/fast_model/manifold_bool.rs
```
pub async fn apply_cata_neg_boolean_manifold(refnos: &[RefnoEnum], replace_exist: bool, dir: PathBuf,) → anyhow::Result<()>  :61-185  # 处理元件库有负实体的布尔运算
pub async fn apply_insts_boolean_manifold(refnos: &[RefnoEnum], replace_exist: bool, dir: PathBuf,) → anyhow::Result<()>  :225-234  # 对多个实例进行布尔运算
pub async fn apply_insts_boolean_manifold_single(refno: RefnoEnum, replace_exist: bool, dir: PathBuf,) → anyhow::Result<()>  :247-346  # 对实例进行布尔运算
```

### src/fast_model/occ_generate.rs
```
pub struct AabbChange  :739-742  # 一个元素的包围盒确实变了。
pub struct NegInfo  :908-915
pub struct ParamNegInfo  :929-936
pub struct CataNegGroup  :1165-1169
pub struct GmGeoData  :1172-1179
pub async fn test_gen_geos() → anyhow::Result<()>  :38-44  # 生成小的几何体
pub async fn gen_meshes_in_db(option: Option<Arc<DbOption>>, refnos: &[RefnoEnum],) → anyhow::Result<()>  :128-173  # 生成模型的部分，update aabb
pub async fn booleans_meshes_in_db(option: Option<Arc<DbOption>>, refnos: &[RefnoEnum],) → anyhow::Result<()>  :176-204  # 执行布尔运算的部分
pub async fn process_meshes_update_db(option: Option<Arc<DbOption>>, refnos: &[RefnoEnum],) → anyhow::Result<()>  :214-265  # 处理网格并更新数据库
pub async fn process_meshes_update_db_deep_default(refnos: &[RefnoEnum]) → anyhow::Result<()>  :276-279  # 使用默认数据库选项更新深层模型网格数据
pub async fn process_meshes_update_db_deep(dboption: &DbOption, refnos: &[RefnoEnum],) → anyhow::Result<()>  :291-399  # 使用指定数据库选项更新深层模型网格数据
pub async fn gen_inst_meshes(refnos: &[RefnoEnum], replace_exist: bool, dir: PathBuf,) → anyhow::Result<()>  :424-548  # 生成实例的网格数据
pub async fn update_inst_relate_aabbs_by_refnos(refnos: &[RefnoEnum], replace_exist: bool,) → anyhow::Result<Vec<AabbChan...  :769-774  # 刷新inst_relate 的 aabb
pub async fn apply_insts_boolean_occ(refnos: &[RefnoEnum], replace_exist: bool, dir: PathBuf,) → anyhow::Result<()>  :959-1068
pub async fn apply_cata_neg_boolean_occ(dir: PathBuf) → anyhow::Result<()>  :1183-1300
```

### src/fast_model/pdms_inst.rs
```
pub async fn init_inst_relate_indices() → anyhow::Result<()>  :41-48  # 初始化数据库的 inst_relate 表的索引
pub async fn save_instance_data_single(inst_mgr: &ShapeInstancesData, replace_exist: bool,) → anyhow::Result<()>  :51-201  # 保存instance 数据到数据库
pub async fn save_instance_data(inst_mgr: &ShapeInstancesData, replace_exist: bool,) → anyhow::Result<()>  :421-563  # 保存instance 数据到数据库（并行优化版本）
```

### src/fast_model/prim_model.rs
```
pub async fn gen_prim_geos(db_option: Arc<DbOption>, prim_refnos: &[RefnoEnum], sender: flume::Sender<ShapeInstancesData>,) → anyhow::Result<bool>  :24-150  # 生成基本体的几何数据
```

### src/fast_model/query.rs
```
pub async fn query_gm_params(refno: RefnoEnum) → anyhow::Result<Vec<GmParam>>  :7-36
pub async fn get_generic_type(refno: RefnoEnum) → anyhow::Result<PdmsGenericT...  :40-48
```

### src/fast_model/room_predicate.rs
```
pub enum AabbVerdict  :17-27  # 只看包围盒能得出的结论。
pub fn aabb_is_usable(aabb: &Aabb) → bool  :30-33  # 包围盒本身是否可用。NaN / Inf 的盒子在库里真实存在，必须先挡掉。
pub fn count_vertices_inside(panel: &TriMesh, aabb: &Aabb) → u8  :40-45  # 八个顶点里有几个落在面板内（0–8）。
pub fn verdict_of(inside_count: u8) → AabbVerdict  :49-55  # 顶点计数到结论的映射。调用方若已经拿到计数（要用它排序），可以直接用这个，
pub fn classify_by_aabb(panel: &TriMesh, aabb: &Aabb) → AabbVerdict  :58-60  # 第一轮：拿元素的世界包围盒对面板做八顶点测试。
pub fn center_distance(panel_aabb: &Aabb, element_aabb: &Aabb) → f32  :64-66  # 归属强度的次键：元素包围盒中心到面板包围盒中心的距离，越小越像「主房间」。
pub fn any_point_inside(panel: &TriMesh, world_pts: impl IntoIterator<Item = Point<f32>>) → bool  :69-73  # 第二轮：任一实际几何点落在面板内即算成员。点必须已经变换到世界坐标系。
pub fn membership_by_aabb(panel: &TriMesh, aabb: &Aabb) → Option<(AabbVerdict, u8)>  :80-86  # 第一轮的完整结果：包围盒结论 + 顶点计数，盒子不可用时为 `None`。
pub fn element_in_panel(panel: &TriMesh, aabb: &Aabb, world_pts: F) → bool where F: FnOnce() -> I...  :94-105  # 两轮合一，供反向（元素 → 面板）使用。
```

### src/fast_model/shared.rs
```
pub fn negative_noun_names() → &'static [String]  :10-25  # Negative-geometry nouns from the decoded positive-equivalent
pub fn negative_noun_refs() → Vec<&'static str>  :27-29
pub fn is_negative_noun(noun: &str) → bool  :31-36
pub fn aabb_apply_transform(aabb: &Aabb, t: &Transform) → Aabb  :50-57  # 针对aabb，应用transform
```

### src/fast_model/utils.rs
```
pub async fn save_aabb_to_surreal(aabb_map: &DashMap<String, Aabb>) → anyhow::Result<()>  :16-38  # 把一批 `aabb:⟨hash⟩` 记录写进库。
pub async fn save_pts_to_surreal(vec3_map: &DashMap<u64, String>)  :40-58
pub async fn save_transforms_to_surreal(trans_map: &HashMap<u64, String>) → anyhow::Result<()>  :60-76
```

### src/graph_db/mod.rs
```
pub struct ForeignEdges  :7-13
pub struct ParaDocument  :16-19
pub struct DataDocument  :22-27
```

### src/graph_db/ssc_arango.rs
```
pub async fn set_arangodb_all_ssc_nodes(pool: &Pool<MySql>, database: &ArDatabase) → anyhow::Result<()>  :9-61  # 将 ssc固定节点保存到图数据库（zone下面的层级除外）
pub async fn query_ssc_instance_with_refno_in_arangodb(refno: RefU64, database: &ArDatabase) → anyhow::Result<Option<Vec<E...  :64-88  # 传入ssc参考号，返回该参考号下面的模型数据
```

### src/graph_db/structs.rs
```
pub struct PdmsEleDataVersioned  :16-36  # 版本数据库里存储的索引值
pub struct PdmsEleData  :83-96
pub struct PdmsEleGraphEdge  :99-103
pub struct PdmsEleEdge  :106-125
pub struct SSCEleGraphNode  :128-134
pub struct PdmsInstanceGraphEdge  :137-141
pub struct PdmsMdbEdge  :164-181
impl PdmsEleDataVersioned  :52-79
  pub fn get_scheme() → &'static str  :54-54
pub fn ser_refno_as_ref_type(refno: &RefU64, s: S) → Result<S::Ok, S::Error> whe...  :42-50
pub fn ser_refno_as_ele_edge(refno: &RefU64, s: S) → Result<S::Ok, S::Error> whe...  :143-148
pub fn de_refno_as_edge(deserializer: D) → Result<RefU64, D::Error> wh...  :150-159
```

### src/gui/assets.rs
```
pub struct Assets  :11-11
impl Assets  :13-31
```

### src/gui/config_panel_story.rs
```
pub struct ConfigPanelStory  :39-72
impl ConfigPanelStory  :74-82
impl ConfigPanelStory  :84-209
  pub fn view(window: &mut Window, cx: &mut App) → Entity<Self>  :85-85
impl ConfigPanelStory  :681-685
impl ConfigPanelStory  :687-798
```

### src/gui/logs.rs
```
pub struct LogUpdateEvent  :14-14
pub struct LogItem  :18-23
pub struct LogListItem  :35-39
pub struct LogListDelegate  :101-104
pub enum LogLevel  :27-31
impl LogListItem  :41-49
  pub fn new(id: impl Into<ElementId>, log_item: LogItem, selected: bool) → Self  :42-42
impl LogListItem  :51-98
impl LogListDelegate  :106-134
impl LogListDelegate  :136-153
  pub fn new() → Self  :137-137
  pub fn add_log(&mut self, message: impl Into<SharedString>, level: LogLevel)  :144-144
pub fn add_global_log(message: S, level: LogLevel)  :161-189
pub fn log_from_thread(message: S, level: LogLevel)  :193-220
```

### src/gui/mod.rs
```
pub fn run_gui()  :28-39
```

### src/gui/theme_engineering_console.rs
```
pub fn engineering_console_dark() → ThemeColor  :37-175  # Dark "Engineering Console" token set
pub fn engineering_console_light() → ThemeColor  :189-309  # Light "Precision Daylight" token set (same semantics, swappe
pub fn apply(cx: &mut App, dark: bool)  :315-335  # Apply the Engineering Console theme to the global [`Theme`]
pub fn apply_dark(cx: &mut App)  :338-340  # Convenience: apply the dark Engineering Console theme
```

### src/mdb.rs
```
pub async fn get_project_mdb(project_pool: &Pool<MySql>) → anyhow::Result<DashMap<Stri...  :9-23  # 获取设计库所有的 mdb 和 mdb 下面的 db_num
```

### src/metadata.rs
```
pub struct MetadataManagerExcelTreeData  :40-44
pub struct MetadataManagerExcelTableData  :60-68
impl MetadataManagerExcelTreeData  :46-57
impl MetadataManagerExcelTableData  :70-85
pub async fn create_metadata_tree_table_sql(pool: &Pool<MySql>) → anyhow::Result<()>  :87-113
pub async fn create_metadata_data_table_sql(pool: &Pool<MySql>) → anyhow::Result<()>  :115-143
pub async fn save_metadata_data(data: DashMap<u64, MetadataManagerTreeNode>, pool: &Pool<MySql>) → anyhow::Result<()>  :145-175
pub async fn save_metadata_table_data(data: Vec<MetadataManagerTableData>, pool: &Pool<MySql>) → anyhow::Result<()>  :177-214
pub fn read_metadata_excel_bytes(data: Vec<u8>, sheet_idx: usize) → Vec<Vec<String>>  :290-311
pub fn convert_metadata_tree_value_from_excel_bytes(mut tree_data: Vec<Vec<String>>, major: &str) → DashMap<u64, MetadataManage...  :314-383  # 读取 excel 表格生成元数据管理树结构的数据
pub fn convert_metadata_table_value_from_excel_bytes(table_data: Vec<Vec<String>>, major: &str) → Vec<MetadataManagerTableData>  :385-454
pub async fn replace_metadata_table_data(data: Vec<ShowMetadataManagerTableData>, pool: &Pool<MySql>) → anyhow::Result<bool>  :456-466
pub fn convert_str_to_hash(input: &str) → u64  :468-472
pub fn get_characters_in_str(input: &str) → String  :475-481  # 获取 字符串中的字符部分 ,遇到非字符就停止
pub fn correct_to_mysql_str(input: &str) → String  :496-510  # 将不符合 mysql 语法的字符进行转义
pub fn get_metadata_filename_major(file_name: &str) → Option<String>  :513-519  # 根据文件名确定传入的excel文件属于哪个专业
```

### src/mqtt_service/mod.rs
```
pub struct SyncE3dFileMsg  :9-28
pub struct MqttInstance  :71-74
impl SyncE3dFileMsg  :31-48
  pub fn new(file_names: Vec<String>, file_hashes: Vec<String>) → Self  :34-34
pub fn new_mqtt_inst(id: &str) → MqttInstance  :79-98
```

### src/noun_layout.rs
```
pub struct LayoutAttr  :25-42  # 一个属性在 `noun_layout
pub struct LayoutNoun  :46-55  # 一个 noun 的导出记录。属性顺序就是 `DbElementType
pub fn word_len(a: &LayoutAttr) → Option<u32>  :81-94  # 该属性在隐含块里占几个字。BOOL 返回 1，但整个 BOOL 游共用这一个字。
pub fn compute_offsets(attrs: &[LayoutAttr], slotted: &HashSet<i32>,) → Result<Vec<(i32, u32)>, Str...  :100-130  # 按 ADR-008 的布局规则算出每个占槽属性的 `(hash, 打包 offset)`。
pub fn implicit_len(attrs: &[LayoutAttr], slotted: &HashSet<i32>) → Result<u32, String>  :134-152  # 一个 noun 的隐含块总字长，即元素记录 word0 应该等于的值。
pub fn to_attr_infos(noun: &LayoutNoun, slotted: &HashSet<i32>) → Result<Vec<AttrInfo>, String>  :186-202  # 把一个 noun 的占槽属性折成 `AttrInfo` 列表，可直接填进 `PdmsDatabaseInfo`。
```

### src/options.rs
```
pub struct DbOptionExt  :10-54  # 扩展DbOption，添加异地部署相关的配置
impl DbOptionExt  :56-62
impl DbOptionExt  :64-68
pub fn get_db_option_ext() → DbOptionExt  :137-150  # 获取扩展的数据库选项
```

### src/other_plat/plat_user.rs
```
pub async fn save_plat_user(users: Vec<PuHuaPlatUser>, pool: &Pool<MySql>) → anyhow::Result<()>  :8-17  # 将平台的人员（非pdms人员）保存到数据库
pub async fn b_exit_user(aios_mgr: &AiosDBManager, user: &str) → anyhow::Result<bool>  :20-27  # 登录功能，判断是否存在该用户
pub async fn query_all_plat_user(pool: &Pool<MySql>) → anyhow::Result<Vec<String>>  :32-42  # 查询所有的普华的用户
pub fn gen_query_all_personnel_info_sql() → String  :58-62
```

### src/pcf/atta.rs
```
pub async fn gen_atta_data(aios_mgr: &AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>, materials: &mut Vec<(RefU64, String) → Vec<u8>  :9-22
```

### src/pcf/bend.rs
```
pub async fn gen_bend_data(aios_mgr: &AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>, materials: &mut Vec<(RefU64, String) → Vec<u8>  :10-25
```

### src/pcf/bran.rs
```
pub async fn get_bran_name_and_children(refno: RefU64, aios_mgr: &AiosDBManager, thickness_map: &DashMap<String, DashMap<String, String>>) → anyhow::Result<Vec<u8>>  :38-109
pub async fn gen_bran_pipeline_reference_data(attr: &AttrMap, start_position: Vec3, pool: &Pool<MySql>, pipe_thickness_data: &Vec<u8>,pipe_temp:f64) → Vec<u8>  :111-124
pub async fn gen_bran_connection_data(refno: Option<RefU64>, leave_position: Vec3, aios_mgr: &AiosDBManager, pool: &Pool<MySql>) → Vec<u8>  :126-149
pub fn gen_type_name_data(type_name: &str) → Vec<u8>  :199-202  # 生成 pcf type 名
pub fn gen_endpoint_data(point: Vec3, bore: f32) → Vec<u8>  :205-207  # 生成 end_point 得 pcf 数据
pub fn gen_center_point_data(center_point: Vec3) → Vec<u8>  :210-212  # 生成 center_point 得 pcf 数据
pub fn gen_cords_point_data(cords_point: Vec3) → Vec<u8>  :214-216
pub async fn gen_item_code_data_attr_val(spre_refno: Option<&AttrVal>, aios_mgr: &AiosDBManager, materials: &mut Vec<(RefU64, String) → Vec<u8>  :218-237
pub async fn gen_item_code_data_refno(spre_refno: RefU64, pool: &Pool<MySql>) → Vec<u8>  :239-246
pub fn gen_item_code_data_name(spre_name: &str) → Vec<u8>  :248-250
pub fn gen_refno_data(refno: RefU64) → Vec<u8>  :253-255  # 生成 ID-REF-NO 的数据
pub fn gen_refno_data_pipe(refno: RefU64) → Vec<u8>  :257-259
pub fn gen_co_ords_data(position: Vec3) → Vec<u8>  :278-280
```

### src/pcf/cap.rs
```
pub async fn gen_cap_data(aios_mgr:&AiosDBManager, attr: &AttrMap, pool:&Pool<MySql>) → Vec<u8>  :8-15
```

### src/pcf/coup.rs
```
pub async fn gen_coup_data(aios_mgr: &AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>, materials: &mut Vec<(RefU64, String) → Vec<u8>  :8-18
```

### src/pcf/elbo.rs
```
pub async fn gen_elbo_data(aios_mgr: &AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>, materials: &mut Vec<(RefU64, String) → Vec<u8>  :11-25  # 生成 elbo 特有的 pcf 数据
pub async fn get_catr_para_data_from_spre(spre: Option<&AttrVal>, aios_mgr: &AiosDBManager, pool: &Pool<MySql>) → Vec<u8>  :27-50
pub fn create_radius_data(attr: &AttrMap) → Vec<u8>  :54-59  # 生成 radius 数据 ( 直接从desi的attr里面取的radius )
```

### src/pcf/excel_api.rs
```
impl PipeThicknessTable  :17-22
pub fn get_pipe_thickness_table() → anyhow::Result<DashMap<Stri...  :25-40  # 获取管道外径壁厚表
```

### src/pcf/flan.rs
```
pub async fn gen_flan_data(aios_mgr:&AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>,materials:&mut Vec<(RefU64,String) → Vec<u8>  :8-16
```

### src/pcf/gask.rs
```
pub async fn gen_gask_data(aios_mgr:&AiosDBManager,attr: &AttrMap,pool:&Pool<MySql>,materials:&mut Vec<(RefU64,String) → Vec<u8>  :8-17
```

### src/pcf/inst.rs
```
pub async fn gen_inst_data(aios_mgr: &AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>, materials: &mut Vec<(RefU64, String) → Vec<u8>  :8-20
```

### src/pcf/nozz.rs
```
pub async fn gen_nozz_data(aios_mgr: &AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>) → Vec<u8>  :8-18
```

### src/pcf/olet.rs
```
pub async fn gen_olet_data(aios_mgr: &AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>, materials: &mut Vec<(RefU64, String) → Vec<u8>  :9-21
```

### src/pcf/pcf_api.rs
```
pub async fn gen_node_basic_data(refno: RefU64, mut data: &mut Vec<u8>, mut materials: &mut Vec<(RefU64, String) → bool  :49-91  # 生成每个节点都存在的 pcf 数据 ，返回值为是否是cap 是cap就代表bran结束，后面的节点就不用执行了
pub async fn create_center_point_data(refno: RefU64, aios_mgr: &AiosDBManager) → Vec<u8>  :94-100  # 生成 center_point 数据
pub async fn create_cords_point_data(refno: RefU64, aios_mgr: &AiosDBManager) → Vec<u8>  :103-109  # 生成 cords_point 数据
pub async fn create_s_key_data(attr: &AttrMap, aios_mgr: &AiosDBManager) → Vec<u8>  :112-141  # 生成 SKEY 数据
pub async fn get_s_key_value(attr: &AttrMap, aios_mgr: &AiosDBManager, pool: &Pool<MySql>) → Option<String>  :143-166
pub fn create_angl_data(attr: &AttrMap) → Vec<u8>  :169-175  # 生成 ANGL 数据
pub fn create_refno_data(attr: &AttrMap) → Vec<u8>  :177-182
pub fn create_temperature_data(temp: f64) → Vec<u8>  :184-186
pub fn create_s_text_data(attr: &AttrMap) → Vec<u8>  :188-193
pub async fn create_pipeline_spec_data(attr: &AttrMap, pool: &Pool<MySql>) → Vec<u8>  :195-203
pub async fn create_tee_item_code_bran_data(attr: &AttrMap, pool: &Pool<MySql>) → Vec<u8>  :206-214
pub fn create_pipeline_href_data(attr: &AttrMap) → Vec<u8>  :216-221
pub fn create_pipeline_tref_data(attr: &AttrMap) → Vec<u8>  :223-228
pub async fn create_cref_name_data(attr: &AttrMap, pool: &Pool<MySql>) → Vec<u8>  :230-238
pub fn create_end_position_null_data(position: Vec3) → Vec<u8>  :240-245
pub async fn create_weld_spec_data(attr: &AttrMap, aios_mgr: &AiosDBManager) → Vec<u8>  :247-288
pub fn create_thickness_data(name: &str, thickness_map: &DashMap<String, DashMap<String, String>>, b_pipe: bool) → Vec<u8>  :291-319  # b_pipe 分为 bran_thickness 和 pipe_thickness 两种 取数据方式一样，最后输出的文字
pub fn gen_s_key_data_str(s_key: &str) → Vec<u8>  :322-324  # 生成 SKEY pcf 的数据
```

### src/pcf/redu.rs
```
pub async fn gen_redu_data(aios_mgr: &AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>, materials: &mut Vec<(RefU64, String) → Vec<u8>  :8-19
```

### src/pcf/tee.rs
```
pub async fn gen_tee_data(aios_mgr: &AiosDBManager, attr: &AttrMap, bran_attr: &AttrMap, pool: &Pool<MySql>, materials: &mut Vec<(RefU64, String) → Vec<u8>  :16-55
pub async fn create_tee_branch_point_data(aios_mgr: &AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>) → Vec<u8>  :57-89
```

### src/pcf/tubi.rs
```
pub async fn gen_tubi_data(start_point: Vec3, end_point: Vec3, tubi_size: TubiSize, bran_attr: &AttrMap, from_refno: Option<RefU64>, materials:&mut Vec<(RefU64,String) → Vec<u8>  :10-34
```

### src/pcf/valv.rs
```
pub async fn gen_valv_data(aios_mgr: &AiosDBManager, attr: &AttrMap, pool: &Pool<MySql>, materials: &mut Vec<(RefU64, String) → Vec<u8>  :8-22
```

### src/plug_in/penetration_piece.rs
```
pub async fn get_penetration_detail_by_refno(aios_mgr: &AiosDBManager, refno_vec: &mut Vec<(RefU64, RefU64) → anyhow::Result<PenetrationVec>  :9-55
pub fn get_x_deviation_angle(mut data: &mut PenetrationData)  :59-72  # 得到贯穿件X轴偏移角度
pub fn test_x_deviation_angle()  :76-87
```

### src/plug_in/plugging_material.rs
```
pub async fn get_plugging_setting_data(pool: &Pool<MySql>) → anyhow::Result<PluggingMate...  :13-56
pub async fn update_plugging_setting_data(plugging: UpdatePluggingSettingEvent, pool: &Pool<MySql>) → anyhow::Result<()>  :59-71
```

### src/plug_in/radiating.rs
```
pub struct HeatDissipationData  :29-36
pub struct GetPipeHeatDissipationRequest  :39-42
pub struct GetPipeHeatDissipationResponse  :45-51
pub async fn get_pipe_heat_dissipation(requests: Vec<GetPipeHeatDissipationRequest>, aios_mgr: &AiosDBManager) → anyhow::Result<Vec<GetPipeH...  :54-127  # 获取 pipe下的bran中，每个bran的散热量
pub async fn get_heat_dissipation_data(bran_refno: RefU64, database: &ArDatabase, aios_mgr: &AiosDBManager) → anyhow::Result<f32>  :130-229  # 返回整个bran的散热面积
```

### src/plug_in/room_setting.rs
```
pub async fn update_selected_room_ssc_site(sites: (Vec<SiteData>, Vec<SiteData>) → anyhow::Result<()>  :5-22
pub async fn query_room_site_table(pool: &Pool<MySql>) → anyhow::Result<&'static str>  :51-62
pub async fn query_selected_room_site(pool: &Pool<MySql>) → anyhow::Result<Vec<(String,...  :70-81
```

### src/plug_in/ssc_setting.rs
```
pub async fn update_selected_ssc_site(sites: (Vec<SiteData>, Vec<SiteData>) → anyhow::Result<()>  :8-25
pub async fn query_selected_ssc_site(pool: &Pool<MySql>) → anyhow::Result<Vec<(String,...  :47-58
pub async fn query_table_ssc(pool: &Pool<MySql>) → anyhow::Result<&'static str>  :60-71
```

### src/plug_in/vague_search.rs
```
pub async fn save_vague_search_condition(condition: SearchConditionSave, pool: &Pool<MySql>) → anyhow::Result<()>  :9-17  # 保存查询条件到数据库
pub fn gen_query_all_vague_search_info_sql() → String  :45-49  # 查询数据库中所有记录
```

### src/plug_in/virtual_hole.rs
```
pub async fn get_audit_data(aios_mgr: &AiosDBManager, data: &mut SendHoleData)  :18-113
pub async fn update_virtual_hole_data_version_aql(database: &ArDatabase, key: String, version: char,) → anyhow::Result<Option<Vec<V...  :117-125  # 更新虚拟孔洞版本
pub async fn update_virtual_embed_data_version_aql(database: &ArDatabase, key: String, version: char,) → anyhow::Result<Option<Vec<V...  :128-137  # 更新虚拟埋件版本
pub async fn update_virtual_hole_status(document_key: &str, status: &str, database: &ArDatabase) → anyhow::Result<String>  :177-198  # 根据提资表单，更新虚拟孔洞埋件中的校审状态
```

### src/plug_in/water_calculation.rs
```
pub async fn save_stp_data_to_arangodb(aios_mgr: &AiosDBManager, mut stp: ExportFloodingStpEvent,) → String  :30-63  # 将数据保存至图数据库
pub async fn export_stp(mgr: &AiosDBManager, stp_packet: ExportFloodingStpEvent,) → anyhow::Result<bool>  :67-79  # 导出水淹计算stp
pub async fn export_stp(mgr: &AiosDBManager, stp_packet: ExportFloodingStpEvent,) → anyhow::Result<bool>  :84-196  # 导出水淹计算stp
pub async fn delete_water_calculation_data(database: &ArDatabase, key: String,) → anyhow::Result<Option<Vec<F...  :200-207  # 删除水淹计算的指定key的记录
pub async fn truncate_water_calculation_data(database: &ArDatabase,) → anyhow::Result<Option<Vec<F...  :210-221  # 清空水淹计算的指定key的记录
pub async fn query_water_calculation_data(database: &ArDatabase, key_value: &str,) → anyhow::Result<Option<Vec<F...  :225-236  # 查询数据库中是否已有当前名称的文件
pub async fn query_water_calculation_data_total_aql(database: &ArDatabase) → anyhow::Result<Vec<Flooding...  :239-246  # 查询数据库中所有记录
```

### src/rvm/cata_element.rs
```
pub async fn create_cata_element_data(refno: RefU64, desi_instance: RvmGeoInfo, database: &ArDatabase) → anyhow::Result<Vec<u8>>  :13-22
```

### src/rvm/data_api.rs
```
pub enum ShapeModule  :30-33
pub enum RvmShapeTypeData  :37-60  # rvm 格式类型
impl RvmShapeTypeData  :62-115
  pub fn get_shape_number(&self) → u8  :64-64  # 获得 ShapeType在 Prim种代表的数字
  pub fn convert_shape_type_to_bytes(&self) → Vec<u8>  :79-79
pub async fn create_refnos_rvm_data(select_refno: Vec<RefU64>, db_option: &DbOption, database: &ArDatabase) → anyhow::Result<Vec<u8>>  :136-246  # 生成 rvm 文件
pub async fn query_rvm_geo_instance_aql(database: &ArDatabase, refnos: Vec<RefU64>) → anyhow::Result<Vec<RvmGeoIn...  :249-285  # 从inst中查询rvm需要的数据
pub async fn query_single_rvm_geo_instance_aql(refnos: Vec<RefU64>, database: &ArDatabase) → anyhow::Result<Vec<RvmGeoIn...  :287-324
pub async fn query_rvm_tubi_instances_aql(refnos: Vec<RefU64>, database: &ArDatabase) → anyhow::Result<Vec<RvmGeoIn...  :327-364
pub fn gen_prim_data_test(refno: RefU64, geo_instance: &RvmInstGeo, desi_transform: Transform, b_desi_cyli: bool, mesh_data: &PlantMeshesData) → Option<Vec<u8>>  :366-410
pub fn gen_mesh_data(transform: Transform, mesh: PlantMesh) → Vec<u8>  :413-437
pub fn gen_data_from_tree(tree: Tree<(RefU64, Vec<u8>) → Vec<u8>  :439-447
pub fn gen_name_position_data(name: &str, position: Vec3) → Vec<u8>  :473-475
pub fn keep_2_decimals_from_f32(input: f32) → f32  :542-544
pub fn get_num_from_str(input: &str) → Option<i32>  :547-555  # 正则匹配字符串中的数字
```

### src/rvm/elements.rs
```
pub async fn create_rvm_file(refno: RefU64, aios_mgr: &AiosDBManager) → anyhow::Result<Vec<u8>>  :31-54
pub async fn create_owner_data(refno: RefU64, aios_mgr: &AiosDBManager, database: &ArDatabase) → anyhow::Result<Vec<u8>>  :56-70
pub async fn convert_shape_type_data(refno: RefU64, aios_mgr: &AiosDBManager) → anyhow::Result<Option<RvmSh...  :256-273
```

### src/rvm/head.rs
```
pub fn create_head_data(db_option:&DbOption) → Vec<u8>  :8-17  # 生成 rvm 头部信息
pub fn create_tail_data(cntb_count:usize) → Vec<u8>  :20-27
```

### src/rvm_baseline/att.rs
```
pub struct AttIndex  :37-39
impl AttIndex  :41-119
  pub fn load(paths: &[impl AsRef<Path>]) → Result<Self>  :42-42
  pub fn is_empty(&self) → bool  :53-53
  pub fn len(&self) → usize  :57-57
  pub fn get(&self, section: &str) → Option<&Section>  :61-61
  pub fn root_element(&self) → Option<&str>  :66-66  # 导出根元素名，取自 Header Information 的 `Element` 字段。
pub fn refno_from_att_name(value: &str) → Option<String>  :124-134  # 把 `=24384/22405` 解析成 `24384/22405`；不是这个形态则返回 None。
```

### src/rvm_baseline/identity.rs
```
pub struct DefaultName  :13-20  # E3D 默认命名的解析结果。
pub fn parse_default_name(name: &str) → Option<DefaultName>  :23-35  # 解析 `<NOUN> <n> of <OWNER_DESC>`；不是默认命名时返回 None。
pub fn named_owner_from_desc(desc: &str) → Option<&str>  :40-48  # 从 owner 描述里取出命名 owner：`BRANCH /x/B1` → `/x/B1`。
pub fn full_noun_to_short(full: &str) → String  :56-79  # 名词全称 → PDMS 短名词（与站点库 `pe
pub fn noun_from_name(name: &str) → Option<String>  :83-85  # 从组名推断 PDMS 名词。命名元素推不出来（名字里没有类型信息），返回 None，
pub fn stable_id(dbnum: u32, path: &str) → u64  :91-105  # 由路径派生稳定 id。身份解析未接入时，两侧无法按真实 refno join，
```

### src/ssc_doc.md
```
h2 SSC 树结构的数据保存 <br>
h3 一. 说明
h3 二. 层级说明
h3 三. 树结构设计
```

### src/ssc.rs
```
pub struct SiteExcelData  :41-50
pub struct PdmsSscMajorCode  :54-61  # pdms site 和 zone name 对应的专业代码
pub struct RoomExcelData  :72-100  # 房间信息 excel 字段
pub struct SscMajorCodeExcel  :258-265  # ssc专业配置excel表 返回的对应数据
pub struct PdmsNodeMajor  :929-938
impl SiteExcelData  :63-68
  pub fn is_valid(&self) → bool  :65-65
impl SscLevelExcel  :1632-1639
  pub fn is_valid(&self) → bool  :1633-1633
pub async fn async_total_ssc_data(project_pool: &Pool<MySql>, mgr: Arc<AiosDBManager>,) → anyhow::Result<()>  :102-154
pub async fn async_total_ssc_data_refactor(mgr: &AiosDBManager) → anyhow::Result<()>  :156-162
pub async fn replace_ssc_room_refno(room_info: HashMap<String, RefU64>, pool: &Pool<MySql>,) → anyhow::Result<()>  :165-185  # 将ssc房间对应的参考号修改为pdms房间的参考号
pub fn get_room_level_from_excel_refactor() → anyhow::Result<SscMajorCode...  :268-337  # 读取 专业分类 excel表 ，返回需要的值
pub fn parse_room_info_from_excel() → anyhow::Result<HashMap<Stri...  :339-366  # 解析 excel 表单 ，找到每一层下面所有的房间号 返回所有的安装厂房下对应的层位，层位下对应的房间
pub async fn get_room_info_from_excel_refactor(database: &ArDatabase) → anyhow::Result<()>  :369-503  # 解析 excel 表单 ，找到每一层下面所有的房间号,并按树结构保存到图数据库中
pub fn get_rooms_from_excel() → anyhow::Result<Vec<String>>  :538-554
pub async fn insert_set_ssc_node_sql(room_info: HashMap<String, RefU64>, pool: &Pool<MySql>,) → anyhow::Result<(DashMap<Str...  :557-573  # 创建ssc固定节点
pub async fn insert_ssc_room_node(mut room_data: HashMap<RefU64, SscEleNode>, zone_level_map: DashMap<String, RefU64>, zone_name_map: DashMap<String, String>, mut next_refno: RefU64, pool: &Pool<MySql>, mgr: Arc<AiosDBManager>,) → anyhow::Result<Vec<String>>  :576-680  # 保存房间下的元件
pub async fn query_refnos_belong_zones(refnos: Vec<RefU64>, database: &Database,) → anyhow::Result<Vec<PdmsNode...  :941-970  # 返回参考号集合所属的zone以及专业代码
pub async fn insert_ssc_room_node_refactor(database: &ArDatabase) → anyhow::Result<()>  :973-1102  # 保存房间下的元件
pub fn set_ssc_node() → anyhow::Result<( String, Da...  :1154-1256  # 设置 ssc 的固定节点
pub fn gen_insert_ssc_node_sql(refno: RefU64, type_name: &str, owner: RefU64, name: &str, real_pdms_refno: RefU64, order_num: usize,) → (RefU64, String)  :1266-1281  # ssc 假节点
pub fn gen_insert_ssc_node_sql_with_pdms_refno(refno: RefU64, type_name: &str, owner: RefU64, name: &str, pdms_real_refno: RefU64, order_num: usize,) → (RefU64, String)  :1284-1300  # ssc 节点引用 pdms refno
pub fn change_children_vec_to_map(refno: RefU64, children: Vec<EleTreeNode>,) → HashMap<RefU64, Vec<RefU64>>  :1303-1312  # 将refno有那些children存放在hashmap中
pub fn deal_room_info(room_data: HashMap<RefU64, SscEleNode>) → HashMap<String, RefU64>  :1469-1475  # 处理从数据库获取的和 room 相关的信息转换成 房间名 对应 参考号
```

### src/surreal_retry.rs
```
pub fn is_retryable_surreal_write_error(error: &str) → bool  :9-11
pub async fn execute_surreal_checked(sql: &str, context: &str) → anyhow::Result<()>  :34-59  # 执行一段 SQL 并逐条检查结果，遇到写冲突按指数退避重试。
```

### src/tables.rs
```
pub fn gen_create_explicit_tables_sql() → String  :10-24
pub fn gen_create_uda_tables_sql() → String  :27-38
pub fn gen_create_dbno_infos_tables_sql() → String  :41-57
pub fn gen_create_element_tables_sql() → String  :60-89
pub fn gen_create_ssc_element_tables_sql() → String  :92-110
pub fn gen_create_data_state_tables_sql() → String  :113-124  # 创建 数据状态表
pub fn gen_create_project_mdb_sql() → String  :127-137
pub fn gen_insert_project_mdb_sql(att_map: &NamedAttrMap) → String  :139-150
pub fn gen_create_project_mdb_json_sql() → String  :153-165
pub fn gen_create_implicit_tables_sql(type_name: &str, att_map: &BTreeMap<u32, (String, AttrVal) → String  :168-236
pub fn gen_create_room_code_table_sql() → String  :238-245
pub fn gen_create_version_info_table_sql(project_name: &str) → String  :247-254
pub fn gen_create_pdms_version_table_sql() → String  :257-269  # 创建版本管理的数据表
pub fn gen_create_file_version_table_sql() → String  :272-281  # 创建存储每个文件的版本号表
pub fn gen_create_pdms_mesh_table_sql() → String  :283-290
```

### src/team_data.rs
```
pub struct SysDBData  :12-20  # Admin模块DB信息
pub async fn sync_team_data(mgr: &AiosDBMgr) → anyhow::Result<()>  :23-92  # 同步system db的信息
```

### src/test/common.rs
```
pub async fn get_arangodb_conn_from_db_option_for_test(o: &DbOption) → anyhow::Result<ArDatabase>  :5-9
```

### src/test/test_performance.rs
```
pub struct PerformanceStats  :22-41  # 性能测试结果统计
pub struct StageAnalysis  :45-64  # 详细阶段分析
pub struct ParallelCataPerformanceStats  :1521-1548  # 并行元件库处理性能统计
pub struct GenGeosDataPerformanceStats  :1688-1709  # gen_geos_data 函数性能统计
pub struct GenGeosDataMetrics  :1713-1726  # gen_geos_data 性能指标
impl StageAnalysis  :66-80
impl PerformanceStats  :82-112
  pub fn new(dbno: u32) → Self  :83-83
  pub fn instances_per_second(&self) → f64  :98-98  # 计算每秒生成的实例数
  pub fn meshes_per_second(&self) → f64  :106-106  # 计算每秒生成的网格数
impl ParallelCataPerformanceStats  :1550-1684
  pub fn new(input_count: usize) → Self  :1551-1551
  pub fn calculate_metrics(&mut self)  :1575-1575  # 计算性能指标
  pub fn generate_report(&self) → String  :1602-1602  # 生成性能报告
impl GenGeosDataPerformanceStats  :1728-1881
  pub fn new(input_refno_count: usize) → Self  :1729-1729
  pub fn calculate_metrics(&mut self)  :1745-1745  # 计算性能指标
  pub fn generate_report(&self) → String  :1789-1789  # 生成性能报告
pub fn init_performance_tracing() → anyhow::Result<()>  :115-136  # 初始化性能追踪
pub async fn test_model_generation_performance(start_dbno: u32, end_dbno: u32, db_option: &DbOption,) → anyhow::Result<Vec<Performa...  :140-169  # 测试指定数据库号范围内所有模型的生成速度
pub fn analyze_performance_bottlenecks(stats: &[PerformanceStats]) → Vec<String>  :553-647  # 分析性能瓶颈并提供优化建议
pub fn generate_detailed_stage_analysis(stats: &[PerformanceStats]) → String  :786-929  # 生成详细的阶段耗时分析报告
pub fn generate_optimization_recommendations(stats: &[PerformanceStats]) → Vec<String>  :932-1056  # 生成针对性的优化建议
pub async fn test_gen_geos_data_performance(manual_refnos: Vec<RefnoEnum>, db_option: &DbOption,) → anyhow::Result<GenGeosDataP...  :1125-1242  # 测试 gen_geos_data 函数的性能
pub async fn test_gen_geos_data_performance_without_db_init(manual_refnos: Vec<RefnoEnum>, db_option: &DbOption,) → anyhow::Result<GenGeosDataP...  :1248-1330  # 测试 gen_geos_data 函数的性能（不初始化数据库连接）
```

### src/test/test_query/mod.rs
```
pub async fn init_test_surreal()  :16-32
```

### src/test/test_spatial/test_penetration_wall_number.rs
```
pub async fn query_refno_belong_rooms(refno: RefU64,) → (String, String)  :6-22
```

### src/version_management/mod.rs
```
pub struct SetStatusData  :7-16
```

### src/version_management/set_status.rs
```
pub async fn check_need_update_version(attr_map: &AttrMap) → bool  :5-7  # 在增量更新中对比最新的属性，当 :CNPEversion 发生变化时,则需要存储该部分数据
pub async fn set_version_from_uda(save_data: SetStatusData) → anyhow::Result<()>  :10-12  # 当 check_need_update_version 为 true 或者 平台设置数据状态时，将对应的数据存到版本管理
```

### src/versioned_db/database.rs
```
pub enum SenderJsonsData  :52-58
pub trait MySqlMethods  :287-293
pub async fn rebuild_dbnum_info_from_pe(dbnum: u32, file_name: &str, db_type: &str,) → anyhow::Result<usize>  :227-284  # 从 pe 全量重算一个 dbnum 的 `dbnum_info_table` 统计（DELETE + 按 ref0 重建
pub async fn create_project_database(project: &str, url: &str) → anyhow::Result<()>  :297-305  # 初始化project database
pub async fn create_info_database(aios_mgr: &AiosDBMgr) → anyhow::Result<()>  :309-379  # 初始化 info 库和表
pub async fn sync_pdms(db_option: &DbOption) → anyhow::Result<()>  :383-510  # 初始化同步pdms数据到数据
pub async fn define_dbnum_event() → anyhow::Result<()>  :512-568
pub async fn verify_dbnum_event_definition() → anyhow::Result<bool>  :572-589  # 读回 pe 表上 `update_dbnum_event` 的实际定义，校验是好版（string::split 解析
pub async fn execute_sql(conn: &Pool<MySql>, sql: &str) → bool  :592-611
pub async fn check_and_clear_db(db_no: u32) → anyhow::Result<()>  :613-634
pub async fn sync_total_async_threaded(db_option: &DbOption, project: &str, cur_dbno_set: Arc<DashSet<u32>>, db_types: &[&str], proj_progress_chunk: usize,) → anyhow::Result<HashMap<u32,...  :638-764  # 多线程同步数据，包括增量同步
```

### src/versioned_db/pe.rs
```
pub async fn save_pes(db_basic: &DbBasicData, total_attr_map: &DashMap<RefU64, NamedAttrMap>, db_num: i32, file_name: &str, db_type: &str, option: &DbOption, output: flume::Sender<SenderJsonsData>,) → anyhow::Result<()>  :42-74  # 保存element数据到版本管理
pub async fn save_pes_mysql(db_basic: &DbBasicData, project: &str, total_attr_map: &DashMap<RefU64, NamedAttrMap>, project_maps: &HashMap<String, Pool<MySql>>, option: &DbOption, db_num: i32,)  :77-154
pub async fn save_pe_relates(db_basic: &DbBasicData, output: flume::Sender<SenderJsonsData>)  :157-192
```

### src/versioned_db/task.rs
```
pub async fn initialize_global_db_sender()  :12-23
pub async fn get_global_db_sender() → flume::Sender<SenderJsonsData>  :56-63
```

### src/web_service/events.rs
```
pub struct WsEvent  :33-38  # 一条广播事件。`seq`/`ts` 不在此处：信封序号由各 WS 连接自行递增编排。
pub enum Topic  :18-20  # 事件主题（客户端经 WS `subscribe` 消息按主题订阅）。
impl Topic  :22-29
  pub fn parse(s: &str) → Option<Self>  :23-23
pub fn event_sender() → &'static broadcast::Sender<...  :43-45  # 全局事件发送端（懒初始化；容量 1024，慢消费者滞后跳帧由客户端 REST 对齐补偿）。
pub fn publish(topic: Topic, ty: &'static str, task_id: Option<String>, payload: serde_json::Value,)  :48-60  # 广播一条事件；无订阅者时返回错误被忽略（正常情形）。
```

### src/web_service/mod.rs
```
pub struct ServiceIdentity  :28-32  # The immutable project identity served by this process
pub struct AppState  :79-87  # 各 handler 共享的服务状态。
pub struct ApiError  :91-95  # 统一错误响应：`{ "code":
impl ServiceIdentity  :34-72
  pub fn new(project: impl Into<String>, mdb: &str, namespace: impl Into<String>) → Self  :35-35
  pub fn validate(&self, project: Option<&str>, mdb: Option<&str>, namespace: Option<&str>,) → Result<(), ApiError>  :45-50  # Missing values preserve the legacy “use server defaults” con
impl ApiError  :97-175
  pub fn bad_request(message: impl Into<String>) → Self  :98-98
  pub fn not_found(message: impl Into<String>) → Self  :106-106
  pub fn conflict(message: impl Into<String>) → Self  :114-114
  pub fn timeout(message: impl Into<String>) → Self  :122-122
  pub fn container(message: impl Into<String>) → Self  :132-132  # 容器（WORL / SITE / ZONE）不能做生成根。单列一个 code 是给客户端用的：
  pub fn precondition(message: impl Into<String>) → Self  :142-142  # 请求本身没错，是数据的前置条件不满足。与 `from_domain` 里 `sync_live`
  pub fn identity_mismatch(message: impl Into<String>) → Self  :150-150
  pub fn from_domain(error: anyhow::Error) → Self  :159-159  # 领域层 `anyhow::Error` 的统一映射：`sync_live` 前置条件拒绝归为 422，其余 500。
impl ApiError  :177-186
pub async fn serve_if_configured(mgr: Arc<AiosDBManager>) → anyhow::Result<()>  :189-195  # 按配置启动 Web 服务；`http_api_addr` 未配置时立即返回（不启动）。
pub async fn serve(mgr: Arc<AiosDBManager>, addr: &str, cors_origins: Option<Vec<String>>,) → anyhow::Result<()>  :198-264  # 在 `addr` 上启动服务并阻塞运行（正常情况下不返回）。
```

### src/web_service/ws.rs
```
pub async fn ws_handler(ws: WebSocketUpgrade, State(state) → Response  :30-32
```
