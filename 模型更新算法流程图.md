# 模型更新算法流程图

## 🎯 总体架构流程

```mermaid
graph TB
    A[文件监控器启动] --> B[扫描PDMS文件]
    B --> C{检测到文件变化?}
    C -->|是| D[比较会话号]
    C -->|否| E[继续监控]
    D --> F{需要增量更新?}
    F -->|是| G[收集增量数据]
    F -->|否| E
    G --> H[解析操作类型]
    H --> I[分类处理任务]
    I --> J[执行模型更新]
    J --> K[更新完成]
    K --> E
```

## 🔄 增量更新详细流程

```mermaid
graph TB
    A[开始增量更新] --> B[读取文件最新会话号]
    B --> C[查询数据库最新会话号]
    C --> D{文件会话号 > 数据库会话号?}
    D -->|否| E[无需更新]
    D -->|是| F[确定更新范围]
    F --> G[收集增量元素数据]
    G --> H[按会话号分组]
    H --> I[解析操作详情]
    I --> J[更新数据库]
    J --> K[处理模型更新]
    K --> L[更新完成]
    
    subgraph "操作类型解析"
        I --> I1[新增操作]
        I --> I2[修改操作]
        I --> I3[删除操作]
    end
    
    subgraph "模型更新处理"
        K --> K1[几何体更新]
        K --> K2[变换更新]
        K --> K3[AABB更新]
    end
```

## 🎨 模型更新分类处理

```mermaid
graph TB
    A[模型更新开始] --> B[遍历操作数据]
    B --> C{操作类型?}
    
    C -->|新增| D[检查几何属性]
    C -->|修改| E[分析变化属性]
    C -->|删除| F[标记删除]
    
    D --> G{需要生成几何体?}
    G -->|是| H[添加到生成队列]
    G -->|否| I[跳过]
    
    E --> J{几何属性变化?}
    E --> K{变换属性变化?}
    
    J -->|是| L[添加到删除队列]
    J -->|是| M[添加到生成队列]
    K -->|是| N[添加到变换更新队列]
    
    F --> O[PE已处理删除标记]
    
    H --> P[执行分类处理]
    M --> P
    N --> P
    I --> P
    L --> P
    O --> P
    
    P --> Q[批量删除旧数据]
    Q --> R[批量生成新模型]
    R --> S[批量更新变换]
    S --> T[更新完成]
```

## 🏗️ 深度模型生成流程

```mermaid
graph TB
    A[深度模型生成开始] --> B[遍历根节点]
    B --> C[查询可见实例节点]
    C --> D[查询负实例节点]
    D --> E[合并节点列表]
    E --> F{有节点需要处理?}
    F -->|否| G[跳过当前根节点]
    F -->|是| H[生成网格数据]
    H --> I[更新AABB包围盒]
    I --> J[下一个根节点]
    J --> K{还有根节点?}
    K -->|是| B
    K -->|否| L[所有节点处理完成]
    
    subgraph "网格生成详情"
        H --> H1[分页查询几何数据]
        H1 --> H2[并行处理几何体]
        H2 --> H3[应用变换]
        H3 --> H4[处理负几何体]
        H4 --> H5[保存网格文件]
        H5 --> H6[更新数据库状态]
    end
    
    subgraph "AABB更新详情"
        I --> I1[计算变换后AABB]
        I1 --> I2[生成AABB哈希]
        I2 --> I3[更新空间索引]
        I3 --> I4[批量更新数据库]
    end
```

## 🌍 世界变换更新流程

```mermaid
graph TB
    A[世界变换更新开始] --> B[智能筛选节点]
    B --> C[获取子树中有inst_relate的节点]
    C --> D{有节点需要更新?}
    D -->|否| E[无需更新]
    D -->|是| F[分批处理]
    F --> G[批量计算世界变换]
    G --> H[生成更新SQL]
    H --> I[批量执行数据库更新]
    I --> J[下一批次]
    J --> K{还有批次?}
    K -->|是| F
    K -->|否| L[更新完成]
    
    subgraph "世界变换计算"
        G --> G1[获取祖先路径]
        G1 --> G2[从根节点开始]
        G2 --> G3[累积变换矩阵]
        G3 --> G4[应用局部变换]
        G4 --> G5{还有祖先?}
        G5 -->|是| G4
        G5 -->|否| G6[返回世界变换]
    end
```

## 📊 几何体类型处理流程

```mermaid
graph TB
    A[几何体处理开始] --> B{几何体类型?}
    
    B -->|PRIM基础几何体| C[基础几何体处理]
    B -->|LOOP管道| D[管道几何体处理]
    B -->|CATA元件库| E[元件库几何体处理]
    B -->|其他类型| F[通用处理]
    
    C --> C1[立方体/圆柱/球体]
    C1 --> C2[生成基础网格]
    C2 --> C3[应用参数]
    C3 --> G[合并到结果]
    
    D --> D1[解析管道路径]
    D1 --> D2[生成管道截面]
    D2 --> D3[沿路径拉伸]
    D3 --> D4[处理连接点]
    D4 --> G
    
    E --> E1[查询元件库数据]
    E1 --> E2[加载预定义网格]
    E2 --> E3[应用实例参数]
    E3 --> E4[处理BRAN/HANG类型]
    E4 --> G
    
    F --> F1[解析几何参数]
    F1 --> F2[调用对应生成器]
    F2 --> G
    
    G --> H[应用世界变换]
    H --> I[计算AABB]
    I --> J[保存网格文件]
    J --> K[更新数据库]
```

## 🚀 性能优化流程

```mermaid
graph TB
    A[性能优化开始] --> B[批量处理优化]
    A --> C[并行处理优化]
    A --> D[缓存优化]
    A --> E[内存优化]
    
    B --> B1[数据库操作批量化]
    B1 --> B2[网格生成批量化]
    B2 --> B3[SQL语句合并]
    
    C --> C1[异步任务并行化]
    C1 --> C2[按类型分组处理]
    C2 --> C3[多线程网格生成]
    
    D --> D1[变换矩阵缓存]
    D1 --> D2[几何数据缓存]
    D2 --> D3[LRU缓存策略]
    
    E --> E1[内存池管理]
    E1 --> E2[对象复用]
    E2 --> E3[流式处理]
    
    B3 --> F[性能监控]
    C3 --> F
    D3 --> F
    E3 --> F
    
    F --> G[自适应调整]
    G --> H[负载均衡]
    H --> I[优化完成]
```

## 🔍 空间索引更新流程

```mermaid
graph TB
    A[空间索引更新开始] --> B[收集AABB数据]
    B --> C[计算变换后AABB]
    C --> D[生成AABB哈希]
    D --> E[更新R*树索引]
    E --> F[批量更新数据库]
    F --> G[保存AABB数据]
    G --> H[更新完成]
    
    subgraph "R*树操作"
        E --> E1[移除旧对象]
        E1 --> E2[插入新对象]
        E2 --> E3{需要重建索引?}
        E3 -->|是| E4[重建R*树]
        E3 -->|否| E5[继续]
        E4 --> E5
    end
    
    subgraph "AABB计算"
        C --> C1[获取原始AABB]
        C1 --> C2[应用缩放变换]
        C2 --> C3[应用旋转变换]
        C3 --> C4[应用平移变换]
        C4 --> C5[返回变换后AABB]
    end
```

## 📈 监控与诊断流程

```mermaid
graph TB
    A[监控开始] --> B[收集性能指标]
    B --> C[分析处理时间]
    C --> D[检查内存使用]
    D --> E[监控数据库性能]
    E --> F[评估缓存命中率]
    F --> G{发现性能问题?}
    
    G -->|否| H[继续监控]
    G -->|是| I[诊断问题原因]
    
    I --> J{问题类型?}
    J -->|CPU密集| K[增加并行度]
    J -->|内存不足| L[优化内存使用]
    J -->|数据库慢| M[优化SQL查询]
    J -->|缓存失效| N[调整缓存策略]
    
    K --> O[应用优化]
    L --> O
    M --> O
    N --> O
    
    O --> P[验证优化效果]
    P --> Q{效果满意?}
    Q -->|是| H
    Q -->|否| I
    
    H --> R[生成性能报告]
    R --> S[更新优化建议]
    S --> A
```

## 🎯 错误处理与恢复流程

```mermaid
graph TB
    A[操作开始] --> B[执行更新操作]
    B --> C{操作成功?}
    
    C -->|是| D[记录成功日志]
    C -->|否| E[捕获错误]
    
    E --> F{错误类型?}
    F -->|数据库错误| G[重试数据库操作]
    F -->|网络错误| H[重试网络请求]
    F -->|内存错误| I[清理内存并重试]
    F -->|文件错误| J[检查文件权限]
    F -->|其他错误| K[记录错误日志]
    
    G --> L{重试成功?}
    H --> L
    I --> L
    J --> L
    
    L -->|是| D
    L -->|否| M[标记为失败]
    
    K --> M
    M --> N[添加到重试队列]
    N --> O{达到最大重试次数?}
    
    O -->|否| P[延迟后重试]
    O -->|是| Q[标记为永久失败]
    
    P --> B
    Q --> R[发送告警通知]
    
    D --> S[更新进度状态]
    R --> S
    S --> T[操作完成]
```

## 📋 使用说明

### 流程图阅读指南

1. **菱形节点**: 表示判断条件
2. **矩形节点**: 表示处理步骤
3. **圆角矩形**: 表示开始/结束
4. **子图**: 表示详细的子流程

### 关键路径分析

1. **热路径**: 增量更新 → 模型分类 → 深度生成
2. **性能瓶颈**: 元件库几何体生成、世界变换计算
3. **优化重点**: 批量处理、并行化、缓存机制

### 监控要点

1. **处理时间**: 每个阶段的耗时统计
2. **内存使用**: 峰值内存和平均内存
3. **数据库性能**: 查询时间和连接数
4. **错误率**: 各类错误的发生频率

通过这些流程图，可以清晰地理解模型更新算法的各个环节，便于系统维护和性能优化。
