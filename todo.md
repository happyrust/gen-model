### 模型数据存入图数据库
  标记有几何模型的参考号, 或者存入变换信息数据, 关联的Instance data, 不能每次都存在文件里

### 表达式求解bug
1、=15194/22895
( ( ATTRIB DESP[2 ] + 20 ) * MAT ( TRIM ( STR ( ATTRIB DESP[15 ] GT 50 * 1 ) ) , 'true' ) )


### 生成模型的效率优化
#### (1) 重用相同spref的模型，如果design里的几个参数是一致的，可以跳过重复的生成
#### (2）针对直段的处理，需要根据lstu快速的过滤


### 保存到版本管理数据库
#### （1）初始化数据schema，比如可以先生成elbow的schema，pdms element的参数
#### （2）保存数据，构建外键关系


### 增量更新实现
#### （1）每次增量更新，保存属性的更新记录，记录提交时间
#### （2）模型也有一个更新记录表，记录模型的更新时间，更新的日志
#### （3）对比两边更新记录的时间戳


### 宏的实现，使用宏来帮助快速实现查询的接口，避免重复的写，先暂时手写

### 实现增量更新
（1）新增的模型，需要在数据库里记录，方便后续的更新
（2）删除的模型，需要更新它们的owner，如果是负实体的话
（3）
