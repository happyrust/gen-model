### 模型数据存入图数据库
  标记有几何模型的参考号, 或者存入变换信息数据, 关联的Instance data, 不能每次都存在文件里

### 表达式求解bug
1、=15194/22895
( ( ATTRIB DESP[2 ] + 20 ) * MAT ( TRIM ( STR ( ATTRIB DESP[15 ] GT 50 * 1 ) ) , 'true' ) )


### 生成模型的效率优化
#### (1) 重用相同spref的模型，如果design里的几个参数是一致的，可以跳过重复的生成
#### (2）针对直段的处理，需要根据lstu快速的过滤