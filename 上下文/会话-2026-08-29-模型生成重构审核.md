# 会话上下文 — 2026-08-29 · 模型生成走 pdms-io 数据接口的重构方案审核

> 会话：BajieAsk-agent-1-9b3a88c2。同日另有 `会话-2026-08-29.md`（ida-bridge 安装，另一会话），勿混淆。
> **2026-08-30 00:36 会话接力**：本任务经 handoff 转交新会话 BajieAsk-agent-1-1e0bfaa9 继续。接力时已核实 Cargo.toml patch 状态、方案文档、specs/034 均与本档案记载一致，无漂移。

## 任务
审核当前正在重构的方案：模型生成改用 pdms-io 提供的数据接口。

## 任务状态：审核完成 → 用户拍板「回写方案文档」→ **回写已完成（2026-08-30 00:5x）**

### 回写清单（用户 2026-08-30 00:43 选 opt1）

| # | 文件 | 改动 | 消掉的发现 |
|---|---|---|---|
| 1 | `docs/plans/direct-dbelement-read-api.md` | 头部加审核回写记录；L1 段废除「补不了就长期二选一」兜底，T402 定为 D1 硬前置；D1 加三条硬前置（T402 透传 / patch 入口 / CATA 规则定稿）；D2 加 T501 前置门（判据转调 db4，T503 验收口径写进交付物）；文件清单 paged.rs 条目标注 T402 | P1、P3 |
| 2 | `docs/plans/pdms-io-v2-core3d-alignment.md` | 头部加审核回写记录；§2 末新增「CATA 读数的时点规则」：Latest 不 pin（照抄现状 `OnDemandDbSession::open`→`PagedDbSession::open`）、运行内复用同一会话、指纹守卫 + fail-closed sesno 断言、对拍同会话否则 fail loud、将来进水位升级 Pinned(applied_sesno) 走 open_at 同一实现 | W1 |
| 3 | 根 `Cargo.toml` | LOCAL-DEPS PATCH END 之后加「第四路（手动）」注释模板：`[patch."…/pdms-io.git"] pdmsdb_engine_v2 = { path = "../../pdms-io-fork-engine-v2/crates/pdmsdb_engine_v2" }`（core3d_model 经 path 依赖传递，不必单列）；写明手动开关、验收前钉回、不进 Toggle 标记区的理由 | P2 |
| 4 | `specs/034-core3d-semantics/tasks.md` | **无需改**：审核后 engine 侧已自行对齐——T101–T104 全勾并带完成注记（T103 发现两个写侧既有 bug 已立案；T104 未登记 noun 计数已暴露），T006 如实登记「欠现场新鲜导出 + 记 live 台账」 | P4（簿记部分已消） |

### 回写期间新核实的事实

- Toggle-LocalDeps.ps1 翻转 BEGIN/END 之间**所有行**（-On 去一层注释 / -Off 加一层），故第四路模板放标记外，避免被 -On 连带激活（engine-v2 工作副本上有他人 27 个在飞文件）。
- `pdmsdb_engine_v2` 依赖声明（vendor/old-parse-pdms-db/Cargo.toml L57）：`git = "https://github.com/happyrust/pdms-io.git", rev = "13a17e1f3a8e…"`；engine-v2 内 `core3d_model = { path = "../core3d_model" }`。
- CATA 现行时点语义：`on_demand_db.rs` 走 `PagedDbSession::open(path)`（最新会话）+ fail-closed 断言 `paged.snapshot().sesno == authoritative.token().target_sesno()`；`cata_closure.rs` per-dbnum session map + `locator_fingerprint`（leaf|parent size+mtime）+ `dependency_manifest_fingerprint`。

### 仍挂账（非文档动作，待后续会话）

- T006 live：现场新鲜导出跑快照 vs FFI 1931 noun 全等，记 `docs/2026-08-12_live-test-ledger.md`。
- engine-v2 工作副本提交归属（core3d 新文件 untracked 叠在他人 occ-retire 在飞改动上）→ 分支所有者裁决。
- W2 反向引用假设维持挂账（P2 fail-loud 兜底），出现反向查询需求时提前动 D3 反向索引。

---

## 方案文档地图（审核对象）

| 文档 | 状态 | 内容 |
|---|---|---|
| `docs/adr/ADR-053-direct-mode-generation-reads.md` | 已接受 | direct 模式：生成期读数据绕开 SurrealDB 直走 pdms-io 解析；Q1-Q6 全拍板 |
| `docs/plans/direct-mode-model-generation.md` | P0 完成 | direct 模式实施计划 P0-P4；P0 探针验收通过（8000/7333 共 200 样本 0 真值冲突） |
| `docs/plans/direct-dbelement-read-api.md` | 草案 | `DirectMdb`/`DirectDb`/`DbElement` 门面 API，D0-D6 阶段；D6（免入库）需 ADR-054 先裁决 |
| `docs/adr/ADR-055-pdms-io-v2-core3d-semantics.md` | 已接受 | 新版 pdms-io（pdmsdb_engine_v2 db1~db5）db4+ 语义以 Core3D.dll 为准；Q1-Q8 全拍板 |
| `docs/plans/pdms-io-v2-core3d-alignment.md` | 已拍板 | 引擎侧 P0-P5 计划 |
| `specs/034-core3d-semantics/`（spec/plan/tasks） | P0 done | 任务清单 T001-T505 |

## 依赖链事实（已核实）

- gen-model `Cargo.toml`：`parse_pdms_db`(old-parse-pdms-db rev 005936a) + `pdms_io`(old-pdms-io rev 14e2836) + `aios_core`(old-aios-core rev d97586c)。
- **LOCAL-DEPS patch 当前为 ON**（三个 old-* 仓重定向到 `../vendor/`）。
- old-parse-pdms-db → `pdmsdb_engine_v2 = { git = "…/pdms-io.git", rev = "13a17e1f" }`（新引擎经此间接进入 gen-model）。
- **Toggle-LocalDeps.ps1 只覆盖三个 old-* 仓，没有 pdms-io.git 的 patch 入口**→ engine-v2 本地工作 gen-model 看不到（审核发现 P2）。
- `src/bin/direct_attmap_probe.rs` 用 old `pdms_io::io::PdmsIO`（sesno pin 唯一入口）。
- vendor paged.rs 尚无 `open_at`（T402 未消费到）。
- gen-model 尚无 `src/data_interface/direct/`（D1+ 未开工）。

## engine-v2 工作副本现状（d:\work\plant-code\pdms-io-fork-engine-v2）

- 分支 codex/occ-retire-paged-session，HEAD 348d187（领先 gen-model 钉的 13a17e1f 两个提交：`348d187` 头 0x34 按字解释、`cb7dd95` 拒绝页大小假匹配 = P4 两条根因已落地）。
- 27 个他人在飞已修改文件（occ-retire 工作）。
- 未跟踪新文件：`crates/core3d_model/`、`compare/core3d_oracle.rs`、**`db4/core3d.rs`（564 行，`Core3dSemantics` trait 已在实现，含 members_negative）**、`tests/core3d_noun_bits.rs`、`tests/core3d_semantics.rs`。
- tasks.md 簿记：P0（T001-T005,T007）已勾；T006 live FFI 对拍欠现场跑；**T101/T102 未勾但 db4/core3d.rs 已存在 → 簿记漂移**（审核发现 P4）。

## 审核结论摘要

**方向正确、架构分层合理**（gen-model 门面薄封装 ← db4 Core3dSemantics ← core3d_model 共享 crate），主要问题在两条轨道衔接顺序与工具链盲区：

- **P1（高）轨道顺序未强制**：D2 DbElement 若先于 T501（升 rev）开工，gen-model 会本地二次实现 members/climb 判据，违反宪法 II，T503 又得拆。→ D2 遍历/分类 gate 在 T501 之后；D1 可先行。
- **P2（高）工具链盲区**：无 pdms-io.git 的本地 patch 入口，P5 验收开发期做不了。→ Cargo.toml 加 `[patch."https://github.com/happyrust/pdms-io.git"]` 或扩展 Toggle 脚本。
- **P3（中）时点双实现**：D1 计划 Pinned→old PdmsIO / Latest→PagedDbSession 两条路；"补不了就长期二选一"的兜底危险。→ T402 `open_at` 定为 D1 硬前置（P4 根因已落地，T402 很小）。
- **P4（中）簿记漂移**：T101/T102 文件已存在未勾选；T006 live 欠跑；27 个他人在飞文件叠加，提交归属待裁决。
- **W1（挂账）CATA 时点语义未定义**：CATA 不入 dbnum_watermark，direct 下 CATA 用什么 sesno pin 两份计划都没写；生成重度依赖 CATA，P1 动工前要定义。
- **W2（挂账）反向引用假设**：一期假设生成期查询全正向，back-ref 标"部分"；P2 fail-loud 兜底可接受。

## 最终接口面（方案定稿口径，gen-model 消费）

1. `parse_pdms_db::paged::PagedDbSession::open_at(path, sesno)` — 唯一会话入口（pin/最新共用一条实现）
2. `pdmsdb_engine_v2::db4::Core3dSemantics` — 分类/遍历/攀爬（R 编号回引）
3. `core3d_model` 共享 crate — NounBitSource（快照+FFI 双实现）+ 可执行参考模型
4. `dict::NounClassifier` / `refno_index` — 既有复用
5. EleData→NamedAttrMap 同源转换器（Q4）— 与写库侧同一份代码

## 建议推进顺序

1. engine 侧收口 P1-P4（P1 已大半）→ 上游提交
2. 补 pdms-io.git 本地 patch 入口（开发期）
3. old-parse-pdms-db 升 engine rev + 暴露 open_at → gen-model 升 rev（T501）
4. gen-model D1+D2（薄门面）→ D4 读上下文 → D5 对拍
5. CATA 时点规则在 P1 动工前定义（新发现，两份计划均缺）

## 工作日志

- 23:26 收到任务；通读 ADR-053/055、direct 两计划、specs/034 三件套
- 23:3x 核实 Cargo.toml 依赖链与 patch 状态；vendor paged.rs 无 open_at；Toggle 脚本无 pdms-io 入口
- 23:3x 核实 engine-v2 工作副本：P0 done、db4/core3d.rs 已 564 行（tasks.md 未勾 → 簿记漂移）
- 23:4x 建本档案，出审核报告（发现 P1-P4 + 挂账 W1/W2），发决策卡等用户裁决下一步
- 00:36 会话接力至 BajieAsk-agent-1-1e0bfaa9，核实关键事实无漂移，重发决策卡
- 00:43 用户拍板 opt1「回写方案文档」
- 00:5x 回写完成：dbelement 计划 5 处（T402 硬前置/D2 前置门）、alignment 计划补 CATA 时点小节、Cargo.toml 第四路注释模板；tasks.md 经核实已由 engine 侧自行对齐无需改
