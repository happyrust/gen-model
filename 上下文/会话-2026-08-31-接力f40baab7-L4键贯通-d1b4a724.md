# 会话上下文 — 2026-08-31 · 接力 f40baab7 · 增量线 L4／键贯通

- 本会话 BajieAsk sessionId：`BajieAsk-agent-1-d1b4a724`（Cursor 窗口工作区是空壳 `old\plant-8`）
- 实际工作目录：`D:\work\plant-code\old\gen-model`
- 被改 crate：`D:\work\plant-code\old\vendor\e3d-model`（另涉 `vendor\e3d-io`）
- 交接来源：`BajieAsk-agent-1-f40baab7`（标题 DDZO），存档 112 千字符，已全文读完
- 约束：本档案实时续写，不等收尾再补

## 0. 交接接手（23:0x）

### 0.1 上一手停在哪

f40baab7 四篇做完：① 增量实现审核 ② 入库收口（发现两笔已被别的线提交，改做复验）
③ 回填 + 本线文档入库 `328b9f58` ④ **L4 粗粒度键设计 → 推翻题目，改提「P0 键贯通」**。

最后一句话是**一个没被回答的问题**：

> 「是按 P0 键贯通开工，还是先关 P2 的账目守恒？」

用户没回答，直接把上下文交接到本会话。→ **本轮第一件事是把这个拍板补上。**

### 0.2 前手档案

`上下文\会话-2026-08-31-e3d-model增量实现审核与计划进度-f40baab7.md`（360 行，四篇 + 工作日志）。
关键结论见其 §11（七条地基事实）与 §12（P0/P1/P2 方案）。

## 1. 接手复核：磁盘现状 vs 存档（本轮实测，23:0x）

### 1.1 三仓状态（存档导出后又变了）

| 仓 | 存档里的 HEAD | **现在的 HEAD** | 脏 | 变化 |
|---|---|---|---|---|
| `gen-model` | `328b9f58` 21:33 | `328b9f58` 21:33 | 116 | 无 |
| `vendor/e3d-model` | `491d0e3` 20:57 | **`f7cd684` 22:28** | 15（真实 6） | **多一笔** |
| `vendor/e3d-io` | `a66cc83` 20:54 | `a66cc83` 20:54 | 3 | 无 |

`f7cd684`「fix(probes): stop two tallies from reading as failures they are not」是**探针线**的：
`tube_axis_check` 把 db_not_open 按 dbno 拆开报，开了 acp7320 等四个目录库后可解析槽位
2827→3153（共 3172），独立一致 2493→2590；`ppoint_probe` 的分母纠正（Ok(None) 不算失败）。
**只动 examples/，src/ 未碰。**

### 1.2 「15 个脏」里有 9 个是假的（本轮新发现）

23:00:28 有一次**批量 mtime touch**（gen-model 的 `上下文/` 全部文件、e3d-model 的整棵源码树
时间戳都是这一秒）。它弄脏了 git 的 stat 缓存，于是 `git status --porcelain` 报 15 个 ` M`，
而 `git diff --numstat` 逐文件验下来**只有 6 个有真实内容差异**：

| 文件 | 增删 | 属于哪条线 |
|---|---|---|
| `docs/noun-coverage-matrix.md` | +194 | noun 覆盖（T88W） |
| `examples/tube_axis_check.rs` | +105 | 探针 |
| `src/category.rs` | +101 | **PrimitiveContainer 线** |
| `src/pipeline.rs` | +42 | **PrimitiveContainer 线** |
| `src/elmodl.rs` | +13 | **PrimitiveContainer 线**（纯文档注释） |
| `src/bin/dump_categories.rs` | +1 | **PrimitiveContainer 线** |

以下 9 个**内容与 index 完全一致**，不要被 `git status` 误导：
`examples/ppoint_probe.rs`、`examples/route_probe.rs`、`scripts/noun_family_probe.py`、
`src/bin/gen_ams.rs`、`src/bin/update_ams.rs`、`src/catalogue_point.rs`、`src/route.rs`、
`tests/increment_real.rs`、`tests/noun_coverage.rs`。

### 1.3 在飞的 PrimitiveContainer 线在做什么（读 diff 实测）

新增 `Category::PrimitiveContainer`（目前只有 `TMPL`）+ `Report::primitive_containers`
+ `Report::host_negatives` 两本账。核心发现写在 `elmodl.rs` `collect_negatives` 的新注释里：

> `TMPL` 底下负体与正体并排挂着，看着像该由正体兄弟扣掉；**实测证否**——`aps250110` 上
> 5 个 `TMPL "Fire Hose Wheel and Housing"` 的 31 件零件全被扣空（那件 `NXTR` 是
> 1000×450×700 的**墙上凹龛**），端板只剩两条 8.4mm 窄条（`NBOX` 133.2×203.2 是给
> 203×133 工字梁让位的梁廓）。→ 这些负体扣的是**容器之外的宿主**，E3D 那条「构件在别处、
> 洞开在宿主上」的路我们整条都没有，先记 `host_negatives` 挂账、一刀不扣。

`aps250110` 上 `TMPL` 底下 1559 件（`NCYL` 1282 / `NBOX` 232 / `NXTR` 45）。

**与 P0 的关系**：这条线**不碰** `increment.rs` / `update_ams.rs` / `tests/increment_real.rs`
/ `route.rs`，但它占着 `category.rs`（P0-3 要在这里加 `is_derived_unit`）、
`elmodl.rs`（P0-1 要改 `GeometryId::ImpliedTube`）、`pipeline.rs`（P0-1 要改 :434 建键处）。
**同文件不同区段，编辑冲突风险低；真正的风险是共享工作树——我一提交就会把它的活扫进去。**

### 1.4 P0 方案的四条地基事实：**全部复核通过**

| # | 存档的说法 | 本轮实测 | 判定 |
|---|---|---|---|
| 11.1 | `GeometryId::ImpliedTube{container, preceding, ordinal}` | `elmodl.rs:123` 一字不差 | ✅ |
| 11.2 | 上行 `GeometryId`、下行 `removals: Vec<RefNo>` | `increment.rs:307` 是 `Vec<RefNo>`；:648 内部 `BTreeSet<(u32,u32)>`；:697 `.map(refno_of)` | ✅ 不对称属实 |
| 11.5 | refno 键贯穿到裁判 | `increment_real.rs:274` `type Snapshot = BTreeMap<(u32,u32), Invariants>`；:302 `snapshot.insert(key(generated.refno), …)` 丢掉 geometry_id | ✅ |
| 11.6 | `GeometryId` 只有三个消费点、不涉 RVM 对拍 | 消费点 = `gen_ams.rs`(127/372/397-398)、`update_ams.rs`(112/185/204-205)、`increment.rs:694` 排序；`rvm_compare` 零命中 | ✅ |

### 1.5 `built = 0` 仍然成立（决定「现在是便宜窗口」的那条）

扫全部 `out/*/report.json`，含 21:48–21:49 最新三次跑：

| 语料 | count | measured | built |
|---|---|---|---|
| `ams8000-tmpl`(21:49) | 3172 | 0 | **0** |
| `aps250110-tmpl-final`(21:48) | 48 | 0 | **0** |
| `ams8000-p1wire`(17:08) | 3172 | 133 | **0** |

→ **至今没有任何一根隐式管身真被建出来。** 改键不带数据迁移这个前提今天仍然成立。
注意 `f7cd684` 把可解析槽位推到 3153/3172，**那是探针在量 P 点，不是管线在建管身**，
两者不要混为一谈——`built` 要等目录几何求值（BRAN/HANG 主线 P1）落地才会离零。

## 2. 本轮待拍板

见给用户的决策卡（`d1b4a724-start`）：
① 先做 P0 键贯通 / 先关 P2 账目守恒 / 先清三条技术债；
② 共享工作树上 PrimitiveContainer 线的在飞改动怎么共处。

## 工作日志

| 时间 | 事项 |
|---|---|
| 23:0x | 接 f40baab7 交接；全文读完 112 千字符存档（14 条对话 + 20 回合执行记录） |
| 23:0x | 认清 Cursor 工作区 `old\plant-8` 是空壳，实际项目在 `gen-model` + `vendor/*` |
| 23:0x | 三仓 git 复核：e3d-model 多出 `f7cd684`（探针线，只动 examples/） |
| 23:0x | 查出「15 脏」里 9 个是 23:00:28 批量 touch 造成的 stat 假阳性，真实脏 6 个 |
| 23:0x | 读在飞 diff：是 PrimitiveContainer / host_negatives 线，占着 P0 要动的三个文件但不同区段 |
| 23:0x | 复核 P0 四条地基事实（11.1/11.2/11.5/11.6）全部属实；`built=0` 仍成立 |
| 23:0x | 建本档案；向用户补上一手没被回答的拍板问题 |
