# 会话上下文 — 2026-08-31 · 恢复 9DOL 断线会话（ZHAG）

> 本会话：BajieAsk-agent-1-3e9f2aab（ZHAG）。00:08 收到用户指令
> 「恢复 cursor 会话 9f9b5010-804c-4fb3-9024-6d98f98cfca8」。
> 该 composer 即 9DOL（BajieAsk-agent-1-b46585a5），00:07 因 `[internal] HTTP 400`
> turn_ended 断线（transcript jsonl 仅剩这一行错误）。
> 接力链：MD6I → HYOY → AJXG → DR6W → 9DOL → **本会话**。
>
> 恢复方式：从 `%APPDATA%\Cursor\User\globalStorage\state.vscdb` 的 cursorDiskKV
> 提取 395 条 bubble（脚本与导出件在 `.session-restore/`，用后可删），
> 通读全程 + 9DOL 档案《会话-2026-08-30-接力DR6W-9DOL.md》（216 行，读到末尾）。

## 接手时盘面（00:1x 实测核实）

| 项 | 状态 |
|---|---|
| 7333 全量对拍（199094 样本） | **还在跑**：pid 26520，23:44:30 起，836 MB，日志 `.probe_7333_full_9dol.log`；上轮 53 min，预计 ~00:37 出汇总 |
| 对拍中间指标 | `[diff]`（影响模型的真值冲突）**0**；`[neutral-diff]` 30 条打印预算全是 CRFA `db=3_0`，桶按预期工作 |
| 9DOL 未提交的 5 个文件 | 全在盘：`direct_attmap.rs`(M)、`direct_store.rs`(M)、`direct_index_contract.rs`(M)、`direct_attmap_probe.rs`(M)、`issues/ISSUE-027-…`(??)。ISSUE-025(M)/026(??) 是他人文件不碰 |
| 已落库提交 | e3d-io `6bea669`（S1 门面 5 文件 1621 行）、gen-model `dab2944d`（MD6I 索引缺口文档） |
| SurrealDB 8009 | 探针在正常读库 → 活着 |

## 9DOL 已完成（本会话继承，不重做）

1. **修法 A**：`direct_attmap.rs` 加 `(RefU64Type|ElementType, RefNoArray)` 投影臂
   （单非空槽收缩为标量；多非空记 `view_divergence`）+ 4 单测，共 11 绿。
2. **CRFA 根因坐实（字节级）**：`old-parse-pdms-db/src/parse.rs::parse_raw_explicit_attrs`
   标量臂（1932-1936）不跳数组长度前缀，把 `[u32 len][u64 ref]×n` 的头 8 字节读成
   `(word0=len, word1=空槽0高位)` → `pe:3_0`/`pe:4_0`。全库仅 3924 个非空值、只有这两种取值、
   都是幽灵引用（`pe` 表无此行）。已写 `issues/ISSUE-027-write-side-stores-array-length-prefix-as-a-refno.md`。
3. **Phase 2 ①**：`DirectStore.members()/members_in()/children_named_attmaps()`
   （原序、不排序不去重）+ 合同门测试（语料 2332 个带成员元素、6 个反序，测试锁原序）。
4. **探针 `neutral_mismatches` 桶**：模型中立键值分歧不计 fatal、逐键点名打印。
   已知隐患（记在 ISSUE-027 待办）：`attribute_affects_model` 对 `Unknown` 也返回 false，
   CRFA 只是未登记而非正面判中立；meta 版分类器会把未登记引用属性升级为影响模型。

## 待办（接手 9DOL 的 todo）

- [ ] **等 7333 汇总**（预计 ~00:37）：验证等价门转绿（`diverged=0`、`only_db` 模型键=0），出来即报
- [ ] 用户拍板后：9DOL 的 5 个文件逐文件 `git add` 提交一笔（修法 A + Phase 2 ① + 探针桶 + ISSUE-027）
- [ ] Phase 2 ②：`get_cat_refno` 直读版（CATR/SPRE/PRTREF 链，跨库走门面）
- [ ] 回填 task_plan Phase 1/2 与对标矩阵 `docs/plans/2026-08-30-core-dll-api-alignment.md`
- [ ] TYPEX 单例归因（小尾巴）

## 方向重大调整（00:19 用户拍板）

> 「核对模型数据，不要使用 DB 路，而是和 e3d tty 导出属性的方式去对比。
> 目前我们不要使用 surreal 了。而是全部采用 direct read 的方式。」

**含义**：等价门的权威从「direct vs SurrealDB」换成「direct vs E3D TTY ATTLIST 导出」；
生成链数据源全面转 direct read，Surreal 退役。在跑的 7333 对拍让它跑完，
作为 ISSUE-027/修法 A 的收官证据归档，之后不再以 Surreal 为基准。

### 既有机制盘点（00:2x 实测）

| 件 | 位置 | 状态 |
|---|---|---|
| TTY 解析+对账器 | `vendor/e3d-io/src/tty.rs`（53KB） | **已有**：`parse_attlist_capture` 解析 `NOUN-ATTLIST-BEGIN/END` 块；`reconcile_attlist` 逐行四态对账（Equivalent / NeedsReferenceResolution / Mismatch / MissingBinaryValue），来源分桶（BinaryImplicit/Explicit/CatalogDefault/RuntimeDerived/Structural/TtyAuthority） |
| 真语料对账测试 | `tests/complete_attlist_real.rs` | **已有**：BRAN 24384/23257 全 121 行零遗漏对账 |
| TTY 捕获 fixture | `tests/fixtures/bran*_attlist_tty.log` | 仅 2 个 BRAN（手工捕获）；格式：`ZZA 属性名` + `属性名 渲染值` 逐对 |
| 分层抽样器 | `examples/tty_sample_live_tree.rs` | **已有**：每 (noun×layout) 桶取 1 见证，从活树下降（429 库语料实测过） |
| E3D 注入工具 | `tools/e3d/`（RuntimeUdaExport.cs/dll + runtime_uda_probe.mac） | **已有**：ALPHA LOG + import C# DLL 模式，用户在 GUI 跑 |
| 批量捕获宏 | —— | **缺**：没有批量生成 ATTLIST 块的 .mac，需新写 |
| E3D 本机安装 | `C:\Program Files (x86)\AVEVA\Everything3D3.1`（真装，含补丁痕迹）；`D:\AVEVA\Everything3D3.1` 是散件；DESIGN headless 宿主未找到（仅 Administration1.8\mon.exe） | GUI 跑宏可行；全自动待用户告知启动命令 |

### 新等价门方案（待用户拍板范围/方式，卡 zhag-tty-parity）

1. 新写批量捕获宏 `.mac`：循环元素集合，逐元素打 `NOUN-ALL-ATTRIBUTES-BEGIN` 头
   （名字/Ref/Type）+ `NOUN-ATTLIST-BEGIN` 块，ALPHA LOG 落盘。
2. Rust 侧新对账探针（gen-model 或 e3d-io examples）：切块 → 逐元素
   `DirectStore`/`DbElement` 直读抽取 → `reconcile_attlist` → 四态汇总，
   门 = Mismatch 0（已知类别单独记账）。
3. 范围三选（分层抽样+吊架加密 / 7333 全量 / 涉事 noun 全量）——发卡待拍板。

## Surreal 收官对拍结果（00:37 出，exit 1，差一个已知单例即绿）

`.probe_7333_full_9dol.log`，199094 样本（23:44–00:37，53 分钟）：

| 桶 | 数 |
|---|---:|
| 完全一致 / 归一后一致 | 0 / 15408 |
| direct 超集（DB 行缺键） | 183686 |
| **真值冲突** | **0** ← 修法 A 完全生效 |
| direct 缺失 / DB 缺失 | 0 / 0 |
| CRFA 中立账（ISSUE-027） | 2984 值分歧 + 10 unset + 2 view_div(RefNoArray[4]) |
| **形状冲突（唯一红点）** | `TYPEX(RawWords[2] vs WordType)` **恰 1 个**（9DOL 遗留「TYPEX 单例归因」，元素未在日志点名，归因待做：哪个 noun 的 schema 把 TYPEX 声明成 WordType——嫌疑在单元素 noun（SITE/APPLDW/无名 560 个）） |
| **耗时** | **direct 77µs/元素 vs DB 15681µs/元素 = 快 204 倍**（全直读决策的硬证据） |

**定性**：修法 A 被 19.9 万样本验证；等价门差一个已知单例；此轮即 Surreal 基准的收官档案，之后基准换 TTY。

**提交**：gen-model `82fbf01b`（feat(direct): project single-slot ref arrays; hand members out in stored order）——9DOL 的 5 文件 645 行：修法 A + Phase 2 ① 成员导航 + 合同门 + 探针中立桶 + ISSUE-027。分支 `codex/libgm-primitive-caliber`。

## 工作日志

- 00:08 收恢复指令；BajieAsk 服务端查无该 id（0 条）→ 判定为 Cursor composer id
- 00:09 定位 transcript：`~\.cursor\projects\d-work-plant-code-old-gen-model\agent-transcripts\9f9b5010…`
  仅 1 行 `turn_ended HTTP 400` → 转从 state.vscdb 提取 395 bubble 成功
- 00:1x 通读 394 条有效记录 + 9DOL 档案全文；实测核实探针进程/日志/git 状态全部在盘
- 00:1x 建本档案；向用户报恢复完成，等拍板/等探针汇总
