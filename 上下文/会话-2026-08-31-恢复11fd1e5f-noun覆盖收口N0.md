# 会话恢复：cursor composer `11fd1e5f-50c8-4788-9318-556a1f114a8b`

> 恢复时间：2026-08-31 16:55（会话 ZKNS / `BajieAsk-agent-1-2ceb4d6c`）
> 原会话：2026-08-31 15:52:25 → 16:45:51，末尾被 `User aborted request` 掐断
> 原会话对应的侧栏会话：先 `BajieAsk-agent-1-7eb26c84`，16:14 之后改报 `BajieAsk-agent-1-42024d02`

## 一、原始记录在哪

| 用途 | 路径 |
|---|---|
| 落盘原件（406 事件 / 1.1 MB） | `C:\Users\dpc\.cursor\BajieAsk-messages\turns\11fd1e5f-.../4c2ca26fb5456477.json` |
| Cursor 侧 transcript（只剩中止标记） | `.cursor\projects\d-work-plant-code-old-gen-model\agent-transcripts\11fd1e5f-...\*.jsonl` |
| 抽取脚本 | `.scratch/extract-11fd1e5f.ps1`、`.scratch/extract-11fd1e5f-dialog.ps1` |
| 事件流水线（280 行） | `.scratch/11fd1e5f-outline.md` |
| 事件详情（170 KB） | `.scratch/11fd1e5f-full.md` |
| 用户原话 + 完整回复（21 KB） | `.scratch/11fd1e5f-dialog.md` |

## 二、原会话在做什么

用户原始需求（15:55:10）：

> 分析 e3d-model，结合 ida-bridge 分析当前的模型生成算法是否已经覆盖完毕了，针对不同 NOUN 的模型生成处理，是否都考虑到了。帮我总结，使用 plannator

结论是**没有覆盖完**。按 core.dll 自己的字典口径算出的底账：

```
core.dll GTGEOM 级联判定「该出几何」的 noun      277
├─ e3d-model 会建几何                            22   7.9%
├─ 记账不建（Catalog/RouteMember… 有名目）       126  45.5%
└─ 分类表欠账（落 Unknown，无名目）              129  46.6%
```

分母第一次算得出来，是因为 `core.dll.i64` 挂上了 ida-bridge 活桥（`idalib-41236`），把 22 个 `I*COM` 谓词的字段号解了出来——此前既有计划里那条「exact I\*COM 谓词拿不到」的残留就此结掉。

底账三步（都可复算）：活桥解字段号 → 用字段号读 `attlib.dat`（E3D 2.10，core.dll 读的就是这份）出 1384 个 noun 的家族位矩阵 → 与 `category.rs` 的 `classify()` 逐条对账。

## 三、查出来的六个缺陷（尚未修）

| # | 缺陷 | 要点 |
|---|---|---|
| 1 | `POGON` 这个 noun 根本不存在 | 面 noun 是 `POLFAC`(44236870)，旧式 POHE 的面是 `POGO`(832210)；几何照出但账是错的，`accounts_for` 等式判据永远逮不住 |
| 2 | `NSLC` 整族漏，`NSCY`/`NSBO`/`NLCY` 认错家族 | NSLC = primitive+INCOMP（设计负图元）；后三个是 geomset+INGCOM（几何集负体） |
| 3 | `A*` 设备图元族 11 个整族漏 | IECOMP 家族的 `ABOX ACONE ACTOR…ASNOU` 全落 Unknown，而 `AEXTR` 已认领；其中 5 个与已实现图元同形 |
| 4 | 几何集家族 42 个连类别都没有 | IGMCOM 21 + INGCOM 16 + IG2COM 5，core.dll 里是独立一条路（`CGTCT2`+`sub_10714FC0`）——属建模概念缺失 |
| 5 | `NOZZ`/`ELCONN` 判 NonGraphic 与 GTGEOM 硬编码冲突 | GTGEOM 的 IPCOMP\|IHCOMP 分支硬编码 `1062572=NOZZ`、`209154074=ELCONN`、`195113669=EQUCOM` 走复合几何 |
| 6 | `SPINE` 带 IPFCOM 家族位却当剖面数据 | `out/ams1112/report.json` 里 `orphans: {CURVE:14, SPINE:14}` 已经在报警 |

范围边界：**船体 85 个 noun（IHLCOM）不走 GTGEOM**，它们出现在 unknown 里不算本线欠账。
交叉验证：`route-nouns.json` 的 104 个路由成员里 55 带 IPCOMP + 30 带 IHCOMP = 85 个落在级联内，与按属性结构筛出来的清单对上号，路由线可按已定论处理。

## 四、用户已拍板的两件事（16:06 决策卡）

1. **下一步先做 N0 换判据** —— 已完成。
2. **既有 approved 计划的三处修正现在就回填** —— 已完成。

## 五、N0 已交付（16:14 验证通过，5 条测试全绿）

判据换根：审计不再用手抄进 Python 的影子表，改由 `dump_categories` 直接链接 `e3d_model::category` 调真的 `classify()`。复算链路：

```powershell
cd vendor/e3d-model
cargo run --bin dump_categories -- data/noun-family-matrix.json out/noun-categories.json
python scripts/noun_coverage_report.py
```

产物（都在 `vendor/e3d-model/`，已核对存在）：

| 文件 | 大小 | 作用 |
|---|---|---|
| `src/bin/dump_categories.rs` | 7361 | 逐 noun 判词 + 汇总导出 |
| `tests/noun_coverage.rs` | 9102 | 5 条回归护栏 |
| `data/noun-family-matrix.json` | 198332 | 1384 noun 的家族位/能力位/正体等价全表 |
| `scripts/noun_family_probe.py` | 6240 | 家族位探针（换字典版本时重跑） |
| `scripts/icom_field_ids.py` | 2812 | 从 core.dll 解 22 个 `I*COM` 字段号 |
| `scripts/noun_coverage_report.py` | 4565 | 缺口报告 |

五条护栏及其触发条件：

- `cascade_coverage_matches_the_recorded_baseline` — 钉 277/22/126/129
- `the_unknown_backlog_is_still_spread_across_the_same_families` — 防「只挪账不减账」
- `every_noun_named_in_the_classifier_actually_exists_in_the_dictionary` — 正则扫 `category.rs` 源码取 noun 字面量逐个查字典，当前 `KNOWN_BOGUS == ["POGON"]`
- `negative_primitives_and_their_positive_twins_agree` — 当前全表零不一致
- `known_classification_gaps_are_still_open` — 特征化测试，修好缺陷它就红

已删除：`.scratch/noun_coverage_audit.py`（影子表）、`.scratch/coverage_crosscheck.py`、`.scratch/noun_family_probe.py`（已核对确实不存在）。

计划文档状态：`.planning/2026-08-31-noun-coverage-closure/task_plan.md` 里 N0 = **done**，N1–N4 = proposed；§7.1 三处回填标为**已回填**。

## 六、下一步与风险

计划里的下一步是 **N1**：关缺陷 1、2、3 + 缺陷 4 的记账部分。修完后 `known_classification_gaps_are_still_open` 与 `KNOWN_BOGUS` 断言会变红，需要同步更新基线四数。

**并发写风险（恢复时实测，16:55）**：`vendor/e3d-model` 正被另一个会话实时改写——

```
16:54:52  out/ams8000-p1wire/{elements,report,primitive-cache}.json
16:53:53  src/bin/gen_ams.rs
16:53:16  src/pipeline.rs
16:51:55  src/lib.rs
16:51:36  src/catalogue_point.rs
16:48:50  examples/ppoint_probe.rs
16:44:35  tests/noun_coverage.rs   ← N0 的产物，被外部动过（5 条断言内容仍完整）
```

对方大概率是 `BajieAsk-agent-1-42024d02`（在线，正在做 BRAN-HANG 收口 / p1wire，见 `上下文/会话-2026-08-31-进展核对-BRAN-HANG收口-42024d02.md`、`.planning/2026-08-31-bran-hang-model-generation/task_plan.md` 16:44:20）。

`src/category.rs` 的 mtime 仍是 15:41:42，**对方没碰 N1 的主战场**，但共用 cargo build lock，且 `tests/noun_coverage.rs` 已被外部动过一次。动手前建议先向对方声明占用 `category.rs`。

## 七、N1 已交付（四缺陷关闭，覆盖账 129 → 78）

用户决策：**开 N1，不与 42024d02 协调、直接改**。实际改动落在 `category.rs`（对方全程没碰）、`pipeline.rs`、`elmodl.rs`、`dump_categories.rs`、`tests/noun_coverage.rs`。

四缺陷的处置：

| 缺陷 | 做法 | 关掉的欠账 |
|---|---|---|
| 1 `POGON` 不存在 | 改 `POLFAC`(44236870)，补 `POGO`(832210，旧式 `POHE` 的面) | — |
| 2 负图元缺兄弟 | 补 `NSLC` → `NegUnimplemented`；`NSCY`/`NSBO`/`NLCY` 移入 `NegGeometrySet` | 1 |
| 3 `A*` 设备图元族 | 5 个同形直接建，6 个记 `Unsupported` | 11 |
| 4 几何集（记账部分） | 新增 `GeometrySet`/`NegGeometrySet`，认领 `IGMCOM`/`IG2COM`/`INGCOM` 42 个 | 39 |

**缺陷 3 的取证换了路子**：计划原写「读 ams 库里任一 `A*` 元素的属性表」，但级联库里 `A*` 元素一件都没有，读不到。改为比对 `all_attr_info.json`（PDMS/E3D 属性 schema 全量导出）——`ABOX/ACYLI/APYRA/ARTOR/ACTOR` 的量纲属性与 `BOX/CYLI/PYRA/RTOR/CTOR` 逐个同名同义，差集只有 `LEVE/OBST/POSI` 这类非几何簿记属性。脚本 `vendor/e3d-model/scripts/a_family_attr_probe.py` 一键复算。`APOLYH/AREVO` 没接——它俩几何来自成员结构（B-rep 顶点面表 / `PLOO` 环），属性表比对说不了成员的事。

中途还从 IDA bridge 查过 core.dll：`bin_search` 按小端 hash 找 noun 码，`BOX/CYLI/NSLC` 只在 `CONHOL` 里露面，说明 `A*` 是靠字典家族位分派、并非硬编码 noun 码 —— 与「同形孪生」的结论方向一致。（另记：`pseudocode` 表必须带 `WHERE func_ea=` 或 `ea=`；`bin_search` 的列名是 `address` 不是 `ea`。）

验收数字：

- 四数 277 / **27** 建 / **172** 记账 / **78** 欠账，与计划 N1 的预期逐笔对上（51 = 1 + 11 + 39）。
- `cargo test --lib --test noun_coverage` 全绿；`cargo build --all-targets` 通过。
- ams1112 重跑到 **`out/ams1112-n1`**（没覆盖 02:48 的旧 `out/ams1112`）：`visited=6115 generated=4476 unknown=0 orphans=28`，账平 `visited + consumed = 30940`。`unknown_nouns` 空；`orphans` 只剩 `CURVE` 14 + `SPINE` 14 = 缺陷 6，归 N2。

复跑命令：

```powershell
cd vendor/e3d-model
cargo run --quiet --bin gen_ams -- `
  --attlib "E:\reverse\e3d\shadow_e3d31_aps_all\attlib.dat" `
  --db "D:\AVEVA\Projects\E3D3.1\AvevaMarineSample\ams000\ams1112_0001" `
  --template "E:\reverse\e3d\shadow_e3d31_aps_all" --db-type DESI --out out/ams1112-n1
```

顺带收敛的一处隐患：`builds` 判据原先在 `dump_categories.rs` 和 `noun_coverage.rs` 各写一份 `matches!`，新增类别漏改一处就会「审计说建、测试说不建」。现统一为 `Category::builds_geometry()`。

`pipeline.rs` 与对方并发：对方把 `NegGeometrySet` 并进了 `orphans`，那样账面只剩一行「NSBO: 12」，看不出是哪些属主少挖了刀；已改为单开 `geometry_set_negatives` 一本账。每次动 `pipeline.rs` 前都重读了文件再改。

## 八、下一步

N2：复核 14 个 `NonGraphic` 与 3 个 `ProfileData`（缺陷 5、6）。`the_n2_defects_are_still_open` 这条特征化测试已经钉住现状，修的时候它会红。ams1112 的 `CURVE`/`SPINE` 各 14 件就是缺陷 6 的现成样本。

## 九、续接记录

- 16:55 恢复上下文完成，本文件建立。
- N1 全部交付并验收，计划文档 N1 标 done（§一 的四数快照保留不回改，另注 N1 后的当前值）。
