# 会话上下文 — 2026-08-31 · 审核 vendor/e3d-model 实现（c5121ac3）

> 本会话：BajieAsk-agent-1-c5121ac3。01:06 收到任务「审核当前 e3d-model 的实现」。
> 前序档案：《会话-2026-08-31-e3dio接口直读生成模型-core-dll方案-ams1112.md》（建 crate 的那条线，会话 e9ff0ef9）。

## 审核对象与快照时点

- 路径 `d:\work\plant-code\old\vendor\e3d-model`（**不在** gen-model/vendor 下）。
- 2007 行 Rust / 11 个文件 / 23 个单测；**crate 在被另一个会话实时改动**：
  `pipeline.rs` 01:10:12、`category.rs` 01:09:47、`tree_census.rs` 01:07:39 各被写过一次。
  本审核基于 **01:10 快照**，`pipeline.rs` 当时处于半成品状态。
- 唯一一次实跑产物 `out/ams1112/`（01:06:27）：`report.json` = visited 160 / generated 0，
  `elements.json` = `[]`，`model.obj` = 0 字节。

## 一号问题（已被在飞会话定位并在改）：DFS 整片截断

`report.json` 的数能自洽解释：160 = WORL 1 + SITE 1 + ZONE 3 + STRU 6 + FRMW 12
+ CWALL 121 + CFLOOR 15 + MNUM 1。库里 ~30940 个元素，只走到 160 个。

根因：旧 `classify` 把 CWALL 判成 `Catalog`、CFLOOR 判成 `Unsupported`，
而旧 `pipeline` 的这两个类别**不下钻**，DFS 于是停在 FRMW 下一层。
4275 个 PANE / 121 GWALL / 210 NXTR 全在它们底下，一个都没进管线，
总账上却只剩「catalog: CWALL 121」一行人畜无害的计数——正是 crate 自己 doc
里写死禁止的「静默丢子树」。

在飞会话已实测坐实拓扑（CWALL → PANE 1995 / GWALL 122 / STWALL 76 / WALL 14；
CFLOOR → PANE 2280 / FLOOR 81，两者自己不带 PLOO），已把 CWALL/CFLOOR 改判 `List`
并补了回归测试 `hull_containers_are_traversable_lists`。

## 快照时点的半成品状态（`pipeline.rs` 01:10:12）

doc 已改写成「下钻无条件 + `consumed`/`orphans` 两本新账 + `accounts_for` 硬判据」，
但 match 体还没跟上：

- `Category::List | Category::NonGraphic => {}` —— **List 也不 push_members 了**，改到一半。
- `Catalog` / `Unsupported` 仍不下钻。
- 新字段 `consumed` / `orphans` 无人写入；doc 引用的 `Report::accounts_for` 尚不存在。
- `Category::NonGraphic` 在两条 arm 里各出现一次 → `unreachable_pattern` 警告。

## 会 panic 的一条（跨文件不同步）

`category.rs` 新增了 `Category::NegUnimplemented(&'static str)` 并让 `is_negative()`
对它返回 true；`elmodl.rs`（01:00 未再改）的 `negative_world_solid` 还是老 match：
`NegExtrusion / NegPrimitive / NegRevolution / Unsupported(_)` 四臂 + `other => unreachable!()`。

→ `collect_negatives` 按 `is_negative()` 会把 NPYR/NRTO/NCTO/NSBO… 收进来，
落到 `other` 臂直接 `unreachable!()` **panic 整个进程**。
ams1112 里 NPYR 15 + NRTO 8 + NCTO 4 = 27 个，CWALL/CFLOOR 一旦下钻就必然走到。

## 站得住的部分（几何与语义层）

- **倒圆折线化** `discretise.rs`：`t = r/tan(α)`、圆心沿角平分线；无符号夹角对凸角与
  凹角（reflex）都给出正确的切圆，越界不裁只记账（对齐 `mthArcFillet`）。
  段数走 `d2_numberOfSegmentsForCircle`，取到 4 的倍数，与 gen-model `libgm_discretise`
  用同一张对照表钉住（R=1/25/100/250/3000/23400 → 8/16/32/52/176/484）。
- **圆柱段数封顶 1000**（不是 256）、体积断言钉的是内接多边形棱柱而非 πr²h——
  这两条都是踩过坑才写得出来的。
- **`restol = 0.051mm` 让刀**：逐轴 inflate 后再 batch_difference，带近共面回归
  （genus 必须为 1，留皮当场红），与 ADR-029 同口径。
- **ORI 语义**：`ori_matrix` 与 e3d-io 权威实现 `record::orientation::axes` 逐例对齐（8 组样本）。
- **孔环靠反绕向 + `FillRule::NonZero`**：与 gen-model `manifold_tessellate::extrude_flat_polygons`
  完全同款（继承的假设，不是新引入的）。

## 账本口径缺口（即便在飞修复落地也仍在）

1. `negative_world_solid` 里的 `notes` 建完就丢。`SJUS_NOUNS` 的 doc 明说
   「NXTR 自己的 PLOO 带 SJUS 时不动，只记 note，等 RVM 对拍裁决」——这条 note
   永远进不了 Report，声明的行为没实现。
2. 正体建模 **失败或跳过** 时，它的负体成员既没被消费也不入栈
   （`push_members` 无条件过滤 `is_negative()`），落不进任何一本账。
3. 负体把正体全吃掉 → `Err(EmptyManifold)` 记成 `failed`（数据坏），
   但这是合法结果，会污染 failed 账。
4. `Report::totals_line()` 还是老口径，不含 `consumed`/`orphans`/`notes`。

## 工程卫生

| 项 | 现状 |
|---|---|
| 版本控制 | **不是 git 仓库**（`fatal: not a git repository`）。2007 行零备份，也没进任何 workspace（gen-model 的 Cargo.toml 没引用它） |
| 产物 | `out/ams1112/` 直接落在 crate 目录，无 .gitignore |
| 无用依赖 | `e3d-attlib` 在 Cargo.toml 里声明，源码零引用（attlib 是经 `DbSet::with_attlib_file` 走的） |
| 集成测试 | `tests/` 目录不存在。23 个单测全是纯函数；**没有一个碰 e3d-io**——pipeline / elmodl / `loop_profile` / `world_matrix` 这四个真正读库的模块覆盖为 0，而 0 产出的 bug 恰好全出在这四个里 |
| target 缓存 | `.cargo/config.toml` 写 `target-dir="../../target"` 想复用 gen-model 的 C++ 缓存，但环境变量 `CARGO_TARGET_DIR=D:\Rust\target` 优先级更高，实际落在 `D:\Rust\target`（共享目的仍达成，注释与事实不符） |
| 死代码 | `transform::matrix_relative_to`、`elmodl::world_with_sjus` 无调用者 |

## 验收路径

用户 00:14 拍板的验收是「E3D TTY 导出 RVM 对比」。当前 crate 只出 OBJ + `elements.json`
不变量（体积/AABB/三角数/亏格/负体数），**没有任何 RVM 对拍脚手架**，也没接
gen-model 的 `src/rvm_baseline/` 与 `test_data/` 基准。这一段还是空白。

## 用户拍板（01:12，决策卡 c512-e3dmodel-review）

- q1 = **只出报告不碰代码**。理由：e9ff0ef9（「重构模型生成」）01:10 还在写同一批文件，
  并发写一个没有 git 的 crate，撞车就是真丢代码。→ **本会话不修改 e3d-model 任何源文件。**
- q2 = 轮到本会话动手时，**先堵 panic + 账本闭合**，几何质量往后放。
- q3 = **等里程碑跑通再入库**，现在不 git init。
  → 风险仍在且被明知接受：并发窗口期内所有改动无回滚点。

## 交接清单（按 q2 口径排序，交 e9ff0ef9 照单执行）

### P0 · 会崩的（改完截断后第一次跑就撞）

1. `elmodl.rs::negative_world_solid` 补 `Category::NegUnimplemented(_) => return Ok(None)` 臂。
   现在它落进 `other => unreachable!()`，而 `category.rs` 刚让 `is_negative()` 对
   `NegUnimplemented` 返回 true，`collect_negatives` 会把 NPYR/NRTO/NCTO/NSBO 收进来。
   ams1112 有 NPYR 15 + NRTO 8 + NCTO 4 = 27 个，CWALL 一下钻必然走到。
   返回 `Ok(None)` 后属主的 `negatives_skipped` 会点名 refno，正合 `NegUnimplemented`
   注释里「账要记在属主头上」的设计意图。
2. 顺带：`unreachable!()` 这种「分类表自相矛盾」的兜底，宁可 `Err(ModelError)` 记进
   `failed` 也别 panic——一个坏元素不该打崩整库生成（AGENTS.md 第 108 条口径）。

### P1 · 账本闭合（`pipeline.rs` 半成品收尾）

3. match 体补齐 doc 已承诺的「下钻无条件」：`List` / `NonGraphic` / `Catalog` /
   `Unsupported` 都要 `push_members`；当前 `List | NonGraphic => {}` 连 List 都不下钻了。
4. 删掉重复的 `Category::NonGraphic`（line 91 与 line 132 各一次，`unreachable_pattern`）。
5. 写入新字段：正实体消费掉的 ProfileData / 负体子树整棵计入 `consumed`；
   在非消费属主下遇到的 PLOO / 负体计入 `orphans`。
6. 补 `Report::accounts_for`（doc 已引用但函数不存在，intra-doc 链接会 warn）：
   `visited + consumed == 索引全集元素数`，不等当场红。`tree_census.rs` 已有索引侧全集口径可复用。
7. `totals_line()` 补 `consumed` / `orphans` / `notes`。

### P2 · 已声明未实现 / 口径污染

8. `negative_world_solid` 的 `notes` 建完就丢 → 挂回属主的 `GeneratedElement::notes`。
   `SJUS_NOUNS` 注释承诺的「NXTR 的 PLOO 带 SJUS 只记 note」目前是空话。
9. 正体建模 **失败或跳过** 时，其负体成员既没被消费也不入栈 → 补一条账。
10. 负体全吃掉正体 → 现在返回 `Err(EmptyManifold)` 记成 `failed`（数据坏），
    应区分为合法的「被完全扣除」，否则污染失败账。
11. 孔环与外环同绕向时 NonZero 会把孔静默填实 → 至少记一条 note
    （gen-model 同款假设，但本 crate 自称静默缺件是最高级别缺陷）。

### P3 · 补测试与卫生（不阻塞里程碑）

12. `tests/` 用 ams1112 真语料钉住 `loop_profile` / `world_matrix` / `generate_subtree`
    三条读库路径——23 个单测没有一个碰 e3d-io，0 产出的 bug 全长在这片无测试区。
    `category.rs` 的分类回归钉不住 pipeline 的下钻行为。
13. 去掉无用依赖 `e3d-attlib`；清死代码 `transform::matrix_relative_to`、
    `elmodl::world_with_sjus`；`.cargo/config.toml` 的 target-dir 注释与事实对齐
    （实际被 `CARGO_TARGET_DIR=D:\Rust\target` 顶掉）。
14. RVM 对拍脚手架（用户 00:14 拍板的验收方式）仍是空白，等 generated > 0 再接。

## 工作日志

- 01:06 收任务；定位 crate 在 `../vendor/e3d-model`（不在 gen-model/vendor）。
- 01:0x 通读 11 个源文件 + 产物 + Cargo/config；比对 e3d-io `db_element.rs` API、
  gen-model `manifold_tessellate.rs` / `sweep_mesh.rs` 的同款口径。
- 01:0x 由 report.json 的 160 反推出 DFS 截断根因（数字自洽，无需重跑）。
- 01:10 复查发现 category.rs / pipeline.rs 被在飞会话改过，重新对齐快照；
  发现 `NegUnimplemented` 与 `elmodl` 未同步的 `unreachable!()` panic。
- 01:1x 建本档案，发决策卡（落地方式 / 优先级 / 工程卫生三问）。
- 01:13 用户新任务「结合 ida-bridge 完善模型生成开发计划文档，用 plannotator」→ 建
  `.planning/2026-08-31-core-aligned-model-generation/task_plan.md`（算法线，镜像 core.dll CSG）。
- 01:22 / 01:28 两轮 plannotator 批注（CSG 用 manifold / 图元实例化复用）已吸收；r3 `approved`。
- 01:3x 会话因 `[internal] HTTP 400` 断线（正查 §2.4.1「主力 noun 没进现代路」，doc 改到一半）。

## 接力续接（2026-08-31 恢复会话 2e34fff3-14bf-4a35-8f8b-0a3b96363678）

从 `state.vscdb` 提取 244 条 bubble 通读全程（脚本 `.session-restore/extract_session.py`，
导出件 `.session-restore/conversation-2e34fff3.md`，用后可删）。断点：plannotator r3 已 `approved`，
但门禁后发现的 §2.4.1（ams1112 主力 noun 是否走现代 CSG）只查到一半就断线，doc 半落盘。

**续用活桥 idalib-35724（Core3D.dll.i64）把 §2.4.1 结论坐实**——从「存疑/阻断项」升为「已证」：

1. `getCSGTree`(0x10715b30)：三键 `actualType→type→hardType` 全 miss 即 `return 0`。
2. `GTGEOM`(0x10341d2e)：现代路 `GTGM2`(0x10714e60) miss → `I*COM` 谓词级联 →
   旧几何 builder `sub_10714FC0` / 设计图元 switch `sub_10343B80`。**回退是设计好的正式路**。
3. 结构 noun 全局(`NOUN_PANE` 0x10ae9ff0 等）的全部引用里，唯一「注册」函数 0x1062cce0 用的是
   `DB_PseudoAttPlugger::instance(ATT_THICKN/SJUS/GAREA/NAREA, NOUN_PANE/GWALL/FLOOR,…)`——
   **伪属性插头，非 CSG addPlug**。（附带：结构板厚度 `ATT_THICKN`、对齐 `ATT_SJUS` 是伪属性=算出来的。）
4. `addPlug`/`getCSGTree` 是导出符号(ord 0x9fa/0xde2)，但 `core.dll` **不导入 Core3D**
   （方向是 Core3D 依赖 core.dll；core.dll 自带 libgeom+libifcoremd=FORTRAN 引擎）；
   `D:\ida_scratch` 里也只有 core.dll / Core3D.dll 两个 i64。→ 无任何外部模块能挂结构 builder。

**结论**：ams1112 的 PANE 4275 / 墙 / 楼板 / SCTN 几何走**旧路**，不走现代 `CSG_TreeBuilder` 族。
`MDR_BPanel/WallVisualisationManager`(挂 BPANEL/CTWALL)是 **ACC 建筑模块**的 builder，
对结构 noun 只作几何参考，非权威。**环拉伸方案本身不推翻**（与 gen-model 生产行为一致），
改的是权威引用（→ 旧路 + 伪属性）。计划已同步：状态头、§2.1、§2.4.1、§2.5、§6.1/§6.2、P0、P2、
§7、批注处置记录、Errors 表全部更新。**此修正落在门禁通过之后，若需重过门待用户拍板。**

---

# 接力续接（会话 fable-5-21，2026-08-31 04:4x 接手）

## 做完了 §2.4.1 定的 P2 前置：旧路结构 PANE 语义（§6.2 第 6 条已结）

续用活桥 `idalib-35724`（Core3D.dll.i64）把上一段挂着的「反编译旧路取结构 PANE 语义」做完，
全部可按地址复查，已写进计划 `task_plan.md` 新增的 **§2.4.2**。三层结论：

**① 路由**：`GTGEOM`（0x10341d2e）全文反编译——PANE 现代路 miss（§2.4.1）后落 `I*COM` 谓词级联，
去 `sub_10714FC0`（薄封装 → `create_geometry` 0x1071c3f0，复合递归成员）；**不是** `sub_10343B80`
（CRDESI 设计图元 switch，只造参数化基本体 box/cyl/cone/dish/pyr/…，PANE 的码不在任何 case 里）。
∴ **PANE = 环拥有者（PLOO/PAVE），几何走复合环拉伸旧路，非参数化图元。**

**② 厚度/对齐 = STRU 模块伪属性，委派给第一个 PLOO**：
- `STRU_DB_PseudoGetTHICKNonPANE::getAtt`（0x10642b70）：`firstMember(NOUN_PLOO)` → `getAtt(ATT_HEIG)`。
  **THICKN(PANE) = 第一个 PLOO 的 HEIG**，PANE 自身不存 THICKN、也不回退读 PANE.HEIG。
- `STRU_DB_PseudoGetSJUSonPANE::getAtt`（0x10642aa0）：同构 → **SJUS(PANE) = 第一个 PLOO 的 SJUS**。
- GAREA=GVOL/LOHE（0x106429a0）、NAREA=NVOL/LOHE（0x10642a20）：派生面积量，与网格无关。
- SCTN 附注：`GENSEC JSPOSS/JSPOSE`（0x10643730）从 JLDATU/SNOD 属主 + POS + WRT 算截面端点，非主力线。

**③ e3d-model 口径核对（只读 `vendor/e3d-model/src/profile.rs`，未改源码）**：
SJUS 只在 PLOO 上读 ✅ 一致；HEIG 第一个环优先但**回退 `PANE.HEIG`** ⚠️（core.dll 无此回退）；
首环取第一个 PLOO 文件原序 ✅ 一致。**总判：口径站得住**，唯一偏差是那个无害的 PANE.HEIG 回退
（建议 P2 去掉或标为非权威兜底）。与实现线实测（4275/4275 PANE 出几何、GWALL 17/20 exact）互证。

## 低优先残留（不阻塞 P2，不改技术方向）

exact I*COM 谓词 + PANE 的 db1 数字码定不死：I*COM 全是 core.dll 的 FORTRAN 导入（Core3D 里只有
`__imp_*` thunk）；`NOUN_PANE`（0x10ae9ff0）等 `DB_Noun` 静态镜像全 `0xffffffff`——码由 dabacon 字典
运行时装载。因不复刻 libgeom（§1.1），这层不影响实现。§6.4 的 NOUN→码表随之暂挂。

## 未动的

一行 e3d-model 源码未改（沿用「只出报告/反编译，不碰在飞 crate」的口径）；`vendor/e3d-model` 仍不在 git。
