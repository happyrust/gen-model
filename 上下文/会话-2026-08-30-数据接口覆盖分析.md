# 会话上下文 — 2026-08-30 · 开发计划 vs 模型生成数据接口覆盖分析(结合 ida-bridge)

> 会话:BajieAsk-agent-1-cbc559f4。
> 前置会话:`会话-2026-08-29-模型生成重构审核.md`(方案审核+回写,已完成)、`会话-2026-08-29.md`(ida-bridge 安装,已就绪)。

## 任务

分析现在的开发计划(direct-dbelement-read-api 等)是否能满足**模型生成的所有数据接口**需求;结合 ida-bridge(IDA Pro )验证接口语义完整性。

## 任务状态:进行中 — 盘点阶段

## 计划步骤

1. [ ] 读开发计划文档:direct-dbelement-read-api.md(接口面定义 D0-D6)、direct-mode-model-generation.md、ADR-053/055、pdms-io-v2-core3d-alignment.md
2. [ ] 盘点 gen-model 模型生成流程**实际消费**的数据接口(SurrealDB 读数、CATA、属性、noun 分类、遍历)
3. [ ] 对照:计划接口面 vs 实际需求 → 覆盖矩阵,找缺口
4. [ ] ida-bridge:检查服务器/idb 可用性,用 Core3D.dll 佐证语义覆盖(如 noun 分类、members/climb、CATA 判据)
5. [ ] 产出覆盖分析报告 + 缺口清单

## 已知事实(继承自前会话,已核实)

- 最终接口面(方案定稿口径):
  1. `PagedDbSession::open_at(path, sesno)` — 唯一会话入口(T402 硬前置)
  2. `pdmsdb_engine_v2::db4::Core3dSemantics` — 分类/遍历/攀爬
  3. `core3d_model` 共享 crate — NounBitSource + 参考模型
  4. `dict::NounClassifier` / `refno_index` — 既有复用
  5. EleData→NamedAttrMap 同源转换器(Q4)
- gen-model 尚无 `src/data_interface/direct/`(D1+ 未开工)
- engine-v2 工作副本 db4/core3d.rs 已 564 行(Core3dSemantics trait 实现中)
- ida-bridge 已装好:CLI `ida-bridge` on PATH,IDA 9.2 @ D:\IDA Professional 9.2,skills 就位

## 中间发现(01:0x,盘点中)

### 发现 F1:fast_model 静态调用面 > ADR-053 收口清单(13 类)

对 aios_core rs_surreal 层 170+ 公开函数与 src/fast_model 全部调用取交集,发现 **18 个清单外查询函数**,调用点分类:

**生成主链(direct 上下文内会踩到,fail loud 会炸)**:
- `loop_model.rs:86` query_multi_filter_deep_children、`:113` fetch_loops_and_height
- `query.rs:40` get_ancestor_types
- `gen_model.rs:110/168` query_multi_children_refnos、`:227` query_mdb_db_nums、`:452..792` query_type_refnos_by_dbnum(×7)、`:628` query_use_cate_refnos_by_dbnum、`:886` query_multi_deep_children_filter_spre
- `prim_model.rs:137` get_children_refnos、`:154` query_filter_children_atts
- `cata_model.rs:1094` query_refnos_has_pos_neg_map、`:1491` query_arrive_leave_points_by_cata_hash、`:1816` query_filter_ancestors
- `occ_generate.rs:425/429` query_deep_visible_inst_refnos / query_deep_neg_inst_refnos、`:704/737` query_refnos_by_geo_hash
- `cal_model/equip_model.rs:85` query_neareast_by_pos_dir

**房间管线(ADR-053 声明不动,无需覆盖)**:
- `room_fixture.rs:374` query_insts

### 待查问题

- Q-A:这些函数查「源数据(pe/ATT)」还是「生成产物(inst 表)」?产物查询 direct 文件侧供给不了(inst 在 Surreal/RocksDB),需要另一条路由策略。重点:query_refnos_by_geo_hash、query_*_inst_refnos、query_arrive_leave_points_by_cata_hash、query_group_by_cata_hash(清单内)。
- Q-B:批量收根入口(query_type_refnos_by_dbnum 按 noun 枚举)文件侧无类型索引,需全库扫描或预建(D3 只计划了 name/backref 两个索引,没有类型索引)。
- Q-C:src/cata、src/data_interface、pdms_inst.rs、resolve.rs 的调用面还没盘。
- Q-D:表达式求值(resolve_expression)调用点。

### 发现 F2:查询函数按数据源分三类(01:1x)

- **纯源数据(pe/ATT/pe_owner 图)→ direct 文件侧可等价**:fetch_loops_and_height、get_ancestor_types、query_multi_children_refnos、query_filter_*、query_deep_visible_inst_refnos(名字带 inst 实为 pe 走查)、query_deep_neg_inst_refnos、query_refnos_has_pos_neg_map、query_type_refnos_by_dbnum(但需全库类型枚举!文件侧无类型索引,且 only_history 变体查 history 表)、query_use_cate_refnos_by_dbnum、query_mdb_db_nums(MDB.CURD,D3 才有文件侧)
- **产物簿记(inst_info/inst_relate/inst_geo 表)→ 文件侧不存在,必须另定路由**:query_group_by_cata_hash(**在 D4 清单内但查 inst_info!**)、query_arrive_leave_points_by_cata_hash(inst_info.ptset)、query_refnos_by_geo_hash(inst_geo<-geo_relate)、query_bran_children_point_map(inst_relate.ptset)、get_inst_relate_keys(data_interface 侧)
- **空间索引(spatial)**:query_neareast_by_pos_dir(equip_model FLOOR 最近查找)——待确认数据源

### 发现 F3:D4 fail-loud 设计会让清单外函数在 D5 才现形

D4 只给 13 类函数加路由,「上下文内未覆盖的查询显式报错」——本次静态盘点提前暴露了这批必炸点,避免 D4 完工后 D5 对拍全炸返工。

### 发现 F4:表达式求值实际入口

fast_model 走 `aios_core::expression::query_cata::{query_gm_param, query_axis_params, resolve_cata_comp}` + `expression::resolve::{SCOM_INFO_MAP, resolve_axis_param}`,内部还会发查询,面待挖。

### 新增待查

- Q-E:cata/data_interface 侧新冒出的 get_cat_attmap、get_implicit_named_attmap、get_pe/get_pe_on、get_self_and_owner_type_name、query_deep_children_refnos 是否在 direct 上下文内被踩到。
- Q-F:query_type_refnos_by_dbnum 的 only_history 变体(history 表)在 direct 下的语义——文件侧有会话历史,但「history 表」是库侧摄入的删除簿记。

## 工作日志

- 00:51 收到任务,读两份前会话档案,建本档案
- 00:5x 读 direct-dbelement-read-api.md 全文、ADR-053 全文;确认 ida-bridge server 运行中(PID 48180),.ida_scratch 存量丰富
- 01:0x 求交集发现 F1:18 个清单外查询函数,多数在生成主链
- 01:1x F2 数据源三分类、F3 fail-loud 时序问题、F4 表达式入口;继续挖 expression 内部与产物函数调用链
