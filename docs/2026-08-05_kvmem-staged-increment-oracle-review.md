# ADR-017 暂存写回方案 Oracle 对抗式审核——采纳与驳回记录

- 会话：`kvmem-staged-increment-review`（GPT-5.5 Pro，浏览器模式，2026-08-05）。模型选择证据 verified=yes；**可信度提醒**：36.4k 输入 53 秒返回、输出 2.0k tokens，与 `kv-mem-hierarchy-cache-remediation.md` 记录的两次快速返回同一模式，推理深度可能未跑满。本文对每条结论做了本仓侧独立复核，采纳以复核为准，不以 Oracle 严重度标签为准。
- 附件：ADR-017、开发方案、CONTEXT.md、ADR-001/010/011/013/014。
- 审核前提中已声明业主裁定不可推翻（整批才落盘、死信纯阻断、房间尽力而为、基线豁免、CATA staged、常驻 mem 实例）；Oracle 认可方向，未建议推翻。

## 处置表

| # | Oracle 结论（严重度） | 本仓复核 | 处置 | 落点 |
|---|---|---|---|---|
| A1 | commit-time-only 语义污染：CTO 若在提交末尾执行会看到瞬态（阻断） | 反例前提有误——本方案 CTO 按 journal **原始位置**重放，中间态与今日直写逐语句一致；但「暂存态与提交态可见世界不同」的核心担忧成立：CTO 的写集若与暂存读集相交，暂存内后续读会缺它的效果 | **部分采纳**：读写集分类表（纯终态扫描 / 写读集相交须在暂存内按工作集执行 / 唯一性补洞与删除后状态须显式定序）；按原始位置重放写进 ADR | ADR-017 §3③、方案 T2.5、R4 |
| A2 | 语句日志不是 redo log，幂等重放假设不足（高） | 成立。本仓语句以显式 id 的 UPSERT/DELETE/RELATE 为主、大体 ReplaySafe，但规范必须成文并机器校验（time::now() 限信息性字段等） | **采纳** | ADR-017 §4、方案 T0.5（门槛） |
| A3 | 半提交+重算双路径冲突、恢复优先级未定（高） | 前提部分有误——journal 只活在内存：进程存活=重试重放、崩溃=journal 不存在=整窗口重算，两路径**由构造互斥**；重算的 regen 删除集覆盖半提交行（与今日崩溃重放同一纪律）。需要的是把状态机写明并测试钉住 | **部分采纳**（文档化+测试，无设计变更） | ADR-017 §4、方案 T4.1 |
| A4 | staging 命名含 end_sesno 与吸收扩窗身份不一致（中） | 成立 | **采纳**：改 `staging_{dbnum}_{window_id}`，逻辑区间入元数据 | ADR-017 §2、方案 T0.3/T1.4 |
| A5 | on-demand 并发缺 staging 可见性约束，旧结果可能覆盖新窗口（中） | 成立（写覆盖顺序风险真实） | **采纳**：窗口对生成根在「生成开始→写回完成」全程持锁；on-demand 命中被暂存根时等待或拒绝 | ADR-017 结果/约束、方案 T2.6/R6 |
| A6 | fn::/隔离/record id/DEFINE 顺序等 mem 与 fork 差异被低估（高） | 成立 | **采纳**：T0.1 升级为 mem↔fork 双跑一致性套件 | 方案 T0.1 |
| B1 | staging 内存耗尽只有告警没有动作（阻断） | 成立 | **采纳**：三级资源状态机（告警/拒绝吸收/废弃暂存转资源阻断），journal 字节计入配额 | ADR-017 §2、方案 T0.3/R3 |
| B2 | mesh 孤儿长期累积（高） | 部分成立（同输入同哈希不增长；阻断窗口反复重试且数据变化时会累积） | **采纳为后续项**：独立 mesh GC，不阻塞本方案 | ADR-017 §9、方案 R8 |
| B3 | staging DROP 失败资源泄露、需启动扫描（高） | 前提有误——staging 在进程内存里，进程重启即清空、无持久残留；进程内长期运行的废弃遗漏才是真问题 | **降级采纳**：进程内登记表 + 窗口终态清扫 | ADR-017 §2、方案 T0.3 |
| B4 | journal 无容量上限（中） | 成立 | **采纳**：并入资源配额 | 方案 T0.3 |
| C1 | 「窗口成功清全库 attempts」过粗，历史失败根记录被误清（阻断） | **驳回**：反例不成立——A 语义下窗口能提交**当且仅当全部根成功**，提交时不存在仍在失败的根；跨窗口派生工作的计数在 durable pending 自身字段上，互不相干。正当性一句写进 ADR 防后人再疑 | 驳回（补说明） | ADR-017 §8 |
| C2 | attempts 与来源会话未绑定，修复后计数语义未定（高） | 成立，且比 Oracle 说的更关键——不重置则「纯阻断」永远解不开：吸收扩窗必须重置受影响根的 attempts，这就是阻断解除机制 | **采纳（升级为关键机制）** | ADR-017 §8、方案 T0.4/T1.4 |
| C3 | 阻断解除需 blocked_reason_hash 对比确认修复（中） | **驳回**：过度设计——吸收+重置+重算本身就是确认（成功即解除，失败即再阻断） | 驳回 | — |
| D1 | phase-2 rocksdb 长事务钉住快照/compaction（阻断） | 方向性成立（即便 overlay+短提交事务，长快照仍钉 SST） | **采纳**：写入 phase-2 立项前置压测清单 | ADR-017 §10、方案 R7 |
| D2 | overlay 内存只是从应用挪到服务器（高） | 成立 | **采纳**（同上写入） | ADR-017 §10 |
| D3 | 单写者假设未来失效（中） | 已是 ADR 明示前提 | 无需变更 | — |
| E1 | P0 缺语义等价黄金测试（高） | 成立 | **采纳**：T0.6 mini-window parity harness（门槛） | 方案 T0.6 |
| E2 | 对拍缺 mesh 集合 hash / MQTT 计数 / 空间树 checksum / 缓存失效次数（高） | 前三项成立；缓存失效计数噪音大 | **部分采纳**（前三项入 T5.2） | 方案 T5.2 |
| E3 | 副作用纪律放 P4 太晚，泄漏进空间树/通告（中） | 成立且属正确性问题 | **采纳**：前置为 T2.0，并写进 ADR §9 | ADR-017 §9、方案 T2.0/T4.2 |

## 2026-08-06 实现审核附录

后续代码审核又确认五个实现级缺口，并据此修订 ADR-017 与开发方案：

1. 房间工作集预载失败后仍可能用不完整 staging 拓扑执行；现约束为任一步失败整轮 fail-closed，任务保留 pending。
2. 水位后的空间刷新、删除与房间触发只有内存状态；现由尾事务写入 durable `SpatialReconcile`，worker 在下一批出队前完成内存树与文件持久化，失败跨重启重试。
3. Transform/DeleteCleanup 未覆盖生成根锁；现于任何非 regen 模型修改前解析、排序并持有全部受影响根，锁持续到提交后空间收敛完成。
4. 增量面板重算只维护 `room_relate`；现与 `room_panel_relate` 使用同一面板重算入口，staging 与 durable drain 共用。
5. 窗口未吸收已有 pending revision，成功根可能在提交后再次生成；现合并本库 pending、窗口内恢复 fresh 合批、成功根从 finalize 计划移除，并以 revision 条件在尾事务收口已有行。

A5 当时留的「等待**或**拒绝」二选一，实现阶段已定为**拒绝**：`model/ensure` 用 `try_lock`，命中被窗口持有的根即回 409 conflict，不排队。第 3 条把锁域从单根扩到「窗口触碰的全部根 × 整个窗口时长」之后，这个拒绝面积明显变大，属于本次唯一对外可见的行为变化（口径见 ADR-017 结果 / 约束）。

验收证据以当次 `cargo test --lib`、`cargo test --lib --features http_api`、隔离 room_fixture 与崩溃/持久化故障注入结果为准，不再引用固定测试数量。
| F1-F3 | 三大担心：journal 假 redo / 暂存与提交不等价 / 长窗口资源失控 | 与 A2/A1+E1/B1 同源 | 已由上述条目覆盖（T0.5、T0.6+T5.2/T5.3、T0.3） | — |

## 结论

Oracle 的总评（「把应用层 staging 当成数据库事务，必须补齐确定性 replay、状态隔离等价、资源治理三个性质」）成立，已转化为三个 P0 门槛：T0.5 ReplaySafe 规范 + validator、T0.6 黄金等价 harness、T0.3 资源状态机。两条「阻断」级结论（C1、A1 的反例部分）经复核前提有误被驳回或降级，其余按表落进 ADR-017 与开发方案。
