# Plan 014：有界暂存写回

## Constitution Check

- 水位只在尾事务成功后推进。
- 不建立第二条消费路径；手动与 watcher 继续共用 worker。
- 查询停滞上浮为任务失败，不以 warning 放行。
- 分块写入和重放保持幂等。

## 实现

1. 扩展 journal 元数据并实现多维纯函数分块。
2. 为 journal、窗口批和尾事务增加单查询超时及块级进展。
3. 增加分块、显式事务隔离和源码顺序回归。
4. 构建部署后重启 SurrealDB 终止旧失控事务，重放 239 并验证模型树。
