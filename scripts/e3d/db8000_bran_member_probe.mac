-- db8000 BRAN 成员坐标探针（只读，无 SAVEWORK）。
--
-- 背景：/C-OR-1R345-C 的管子是**显式 FTUB 构件**（AvevaMarine 形制），不是
-- 隐式 TUBI——所以改 BRAN 的 HPOS 只动头点、不动任何已有构件几何（实测：
-- 重生成跑了、包围盒一个字节没变）。要验证几何传播，得移动一个构件本身。
--
-- 本探针取两件事：
--   1. 成员 24384/23262（长管段，库里包围盒 U 2900~2930）的当前 POS——
--      移动腿的 restore 值必须来自这里，离线不可知；
--   2. 顺带验证「按 refno 导航」在 db8000 的 TTY 会话里到底成不成立。
--      db8000_zone_members_probe.mac 的头注说不成立，但那条结论是在别的上下文
--      下得出的（l3_ftub_move_apply.mac 在 L3 项目里用的就是 refno 导航）。
--      成立与否直接决定移动腿怎么写，所以这里当场问一次，不猜。
--
-- 不写 SAVEWORK / QUIT / FINISH：只读探针，会话归 driver 的 wrapper。

ALPHA LOG "D:/work/plant-code/old/gen-model/scripts/e3d/db8000_bran_member_probe.log" OVER
$P CODEX-DB8000-BRAN-MEMBER-PROBE-ALIVE
/C-OR-1R345-C
Q CE
Q NAME
=24384/23262
Q CE
Q TYPE
Q REF
Q OWNE
Q POS
=24384/23266
Q CE
Q TYPE
Q REF
Q POS
$P CODEX-DB8000-BRAN-MEMBER-PROBE-DONE
ALPHA LOG END
